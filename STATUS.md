# KrinikCam — Текущий статус проекта

> Читается ИИ-агентом перед каждой задачей; обновляется после каждой значимой задачи.
> 📏 **Правило размера (ревизия 2026-07-02):** STATUS — КОРОТКИЙ живой срез (~экран текста), НЕ журнал.
> Историю сессий уноси в `researches/status_archive.md` (append в конец), форензику — в `bugs/`,
> детали идей — в `ideas/`. Канон — `AGENT_GUIDE.md`; мышление — `PHILOSOPHY.md` (ПРОСТОТА).

_Обновлено: 2026-07-28 (вечер, пауза) · версия 0.8 · релиз **v0.8**_

## ▶️ С ЧЕГО НАЧАТЬ НОВУЮ СЕССИЮ (пауза 2026-07-28, вечер) — ЧИТАТЬ ПЕРВЫМ

**Состояние:** дерево чистое, всё запушено. Debug-APK стоит на планшете. Устройство —
Headwolf Titan 1 по Wi-Fi `192.168.1.3:5555` (Криник выключил 4G, чтобы маршрут не перескакивал на
соту и не рвал ADB; USB он подключал, но Mac его не увидел — вероятно кабель только для зарядки).
Сборка требует ОТДЕЛЬНЫЙ JDK 17 помимо `JAVA_HOME=JBR` (EXP-0020).

**Приоритет Криника прежний: сеть и стабильный стрим.** Уровни обороны 0 и 1 СДЕЛАНЫ и приняты
живьём. Дальше — по документу **`plans/21_stream_resilience_levels_2_4.md`** (собран 11 агентами с
состязательной проверкой; там же порядок работ и обоснование порядка).

**✅ Ш0 из `plans/21` СДЕЛАН 2026-07-28 (числа — `researches/network_resilience.md` §14.1).**
`freeze`/`thaw` в полигоне готовы; замер «зомби-сервера» K5 дал: отправка встаёт через **4 с**, очередь
забивается за **3 с**, с **7-й секунды теряется 100% потока** (`dropV` растёт со скоростью fps), а
**`onConnectionFailed` не приходит ВООБЩЕ** — 3.6 минуты фаза `Live`, `attempt=0`. Разморозка воскрешает
эфир сама за 2 с, но выплёвывает очередь **залпом 11887 kbps** (путь B из §13.1 — риск дисконнекта на
живом ingest). **Главный риск работы B снят досрочно:** `cmd stop` при заблокированной записи отработал
за **1.5 с** → watchdog сможет прервать зомби-сессию. Порог по замеру: `WATCHDOG_STALL_MS = 6000`.

**✅ РАБОТА A СДЕЛАНА И ПРИНЯТА НАБЛЮДЕНИЕМ 2026-07-28** (журнал — §14.2 разведдока): пол адаптива
переехал из хардкода `ADAPTIVE_FLOOR_BPS = 1_000_000` в **профиль кодера** (`minVideoBitrateBps`,
Room **v6→v7**, дефолт 250 кбит/с — В5 Криника; дропдаун 150…1000 кбит/с в диалоге профиля, виден
только при включённом адаптиве). Миграция принята на ЖИВОЙ базе Криника: v7, три профиля/2 платформы/
3 сцены целы. Пол принят ЭНКОДЕРОМ, а не только логом: полка фактического битрейта **373–404 кбит/с**
(250 видео + 128 звук) при `dropV=0` и живом `is publishing` — инвариант И1 соблюдён. Сквозной путь
UI→Room→эфир доказан отдельно (500 kbps → полка 624–646). Smoke PASS, 0 крашей.
⚠️ Честная граница: пила восстановления на РЕАЛЬНОМ узком канале не проверена (`simulate-congestion`
её не воспроизводит по построению) — нужен дроссель `tools/net-chaos.mjs`.

**Первое действие новой сессии — работа B (watchdog)**, порог уже обоснован замером
(`WATCHDOG_STALL_MS = 6000`), главный риск снят. Затем C (слейт) → D (индикация: пульсирующая
обводка) → E (сетевой колбэк, последним — он переписывает единственный уже принятый живьём путь).

**🆕 bugs/72 (побочная находка Ш0):** приложение упало в простое 2026-07-28 21:08 — `SIGABRT`
`drawRenderNode called on a context with no surface!` в RenderThread, через 22 минуты после остановки
эфира. Плюс вскрыт дефект наблюдаемости: **FileLogger теряет хвост при аварийном завершении** (в файле
нет последних 22 минут перед крашем) — любой краш-репорт приходит без самого нужного контекста.

**🔴 Три поправки, добытые байткодом — НЕ перепроверять, они верны:**
1. Энкодер работает в **CBR** → замерший кадр САМ ПО СЕБЕ полосу не экономит (ломает §7 разведдока).
2. `setVideoBitrateOnFly` молча выходит при незапущенном энкодере → наш лог = намерение, не факт.
3. `getSentVideoFrames()` растёт ДО записи в сокет → доставку им доказывать нельзя.

**✅ `interviews/interview_013` ОТВЕЧЕН 2026-07-28** (обе развилки закона догона):
**В1** — догоняем всё; при упоре в потолок «дропаем старое и догоняем рывками, продолжая писать
буфер» (решение агента о вытеснении самого старого подтверждено владельцем; вытеснять — по границам
IDR, потерю сообщать числом). **В2** — вариант (б): целимся в ноль, но при систематических обрывах
система сама держит запас и периодически пробует вернуться к нулю → **Р9.1 из интервью 011 смягчён
владельцем, в чистом виде больше не действует.** Догон по-прежнему заблокирован E4.

**🆕 Вижн, дополнение 6 (idea 43, чат 2026-07-28):** отсутствие прио-арта по догону на стороне
стримера — **осознанная ниша, а не риск** («этой болью я и страдаю на трансляциях из парка… для этого
KrinikCam и делаем»). Довод «так никто не делает» закрыт и больше не поднимается. Плюс второй рычаг
догона: **«выплёвывать хоть и с низким битрейтом, но контент»** — при нехватке полосы уступает
КАЧЕСТВО, а не контент. 🔴 **Поправка Криника тем же вечером (моя ошибка, EXP-0031):** ускорение 1.5×
**НЕ требует полосы ×1.5** — «мы ускоряем плейбеком, у нас остаётся 30 кадров в секунду, но мы дропаем
промежуточные кадры! а звук ускоряем». Полоса нужна та же, что обычному эфиру; низкий битрейт — ответ
на узкий канал, а не плата за ускорение (это же было верно в разведдоке §13.2/§13.3 — я вывел формулу
заново вместо чтения).

**🟡 Ждёт Криника:** `homeworks/09` (эксперимент E4 на его YouTube — блокер догона);
`ideas/44` (состав звуковых паттернов виртуалки); 4 мокапа плашки до кода работы C.

**🟢 Лёгкое, если нужен быстрый результат:** bug 70 (битрейт мигает в «0,0» на бейдже записи),
гигиена смоука (работать на своей сцене — сегодня делал это руками), `/check-backlog`.

## 🛡️ 2026-07-28 — ЖИВУЧЕСТЬ ЭФИРА: УРОВНИ 0 и 1 СДЕЛАНЫ (приоритет Криника «сеть и стабильный стрим»)
Реализовано ПО разведдоку `researches/network_resilience.md` (код писался по документу, не по памяти).
- **Блокер разведдока снят по байткоду:** `MultiStream.getStreamClient(RTMP, i)` → `rtmpStreamClients.get(i)`
  — все ручки (`setReTries`/`reTry`/`setLogs`/счётчики) раздаются **строго по индексу выхода**, мультистрим
  их не путает. Заодно опровергнут комментарий в коде: `doingRetry` ставится в `connect(url, isRetry=false)`,
  то есть при ПЕРВОЙ попытке, а не «при установленном коннекте» — комментарий исправлен.
- **Уровень 0 (гигиена+пульс):** `setLogs(false)` и `setBitrateExponentialFactor(0.5)` на каждый выход ДО
  подключения; секундная строка-пульс `пульс[i] фаза bitrate cache sentV dropV congested attempt`;
  `droppedFrames` ожил (годами был константным нулём); крашевый `startPreview(tv)` на пути гибели эфира
  заменён на `restorePreviewIfDetached()` (класс bugs 48/63).
- **Уровень 1 (никогда не сдаваться):** потолок `maxReconnectAttempts = 5` СНЯТ — реконнект идёт до кнопки
  Стоп; бэкофф стал экспонентой 1.5 с капом 15с и джиттером ±10% (против «стада» на 4 выходах); **FGS
  переведён со СОСТОЯНИЯ на СЕССИЮ** (`sessionActive`) — обрыв больше не гасит сервис и не отпускает wake
  lock в момент восстановления; гард «нет сессии → нет реконнекта» (Стоп не воскрешает эфир);
  громкая индикация: оранжевая пилюля «ВОССТАНОВЛЕНИЕ • эфир не идёт NN:NN · попытка N», пилюля отказа
  вместо ИСЧЕЗНОВЕНИЯ виджета, и текст FGS-нотификации теперь докладывает правду (канал при погашенном экране).
- ✅ **ПРИЁМОЧНЫЙ ТЕСТ ВИЖНА ПРОЙДЕН ЖИВЬЁМ** (полигон MediaMTX, непрерывный logcat в файл): эфир →
  `rtmp-server.mjs stop` 20:28:47 → попытки **за пределами 5-й** (дошли до 16), бэкофф 1.0→15с с видимым
  разбросом (15874/14187/13598мс), **FGS жив, wake lock держится 3м03с** → `start` 20:31:28 → **20:31:36
  клиент подключился САМ, 20:31:38 сервер: `is publishing`, 2 дорожки** — эфир воскрес без касания
  приложения. Стоп: FGS погас, wake lock отпущен, зомби-реконнекта нет. **0 крашей.**
- 🔴→✅ **JUDGE-ПРОХОД ПОЙМАЛ ЛОЖНУЮ ЗАЯВКУ (до пуша):** «виджет больше не исчезает при аварии» было
  НЕВЕРНО — пилюля отказа существовала, но виджет целиком гасил ВНЕШНИЙ гейт
  `AnimatedVisibility(visible = streamState.isActive)` (`MainScreen.kt:519`; `isActive` = Live|Connecting →
  Error всегда невидим). Я правил не ту строку, и на экране при отказе не появлялось НИЧЕГО. Исправлено
  и перепроверено скриншотом: `go-live rtmp://badhost` → красная пилюля «ЭФИР НЕ ИДЁТ • Endpoint
  malformed…». Урок: место правки состояния и место его гашения — разные; проверять глазами, не по диффу.
- 🧹 Попутно: починен спящий дефект полигона (`tools/rtmp-server.mjs` — `createWriteStream` не был
  импортирован, на чистой машине первый `start` упал бы); битрейт в телеметрии обнуляется при обрыве
  (замерший «1539 kbps» при нулевом потоке — тот же класс лжи, что bug 45); User Manual §7 EN+RU дополнен.
- ⚠️ **Честная оговорка:** `droppedFrames` теперь читается из библиотеки, но НЕНУЛЕВОГО значения на
  чистом Wi-Fi увидеть не удалось (дропов не было) — проводка проверена, отображение ненулевых потерь
  ждёт плохого канала. Уровень 3 (watchdog) как раз про это.
- **Дальше по разведдоку:** уровень 3 (watchdog замершего отправителя — закрывает K4/K5, самый опасный
  незамеченный класс) и уровень 2 (сетевой колбэк) — решений Криника не требуют.

## 🔬 2026-07-28 (вечер) — ДВЕ РАЗВЕДКИ + НОВОЕ ПРАВИЛО КАНОНА
- 🆕 **ПРАВИЛО КРИНИКА В КАНОН (`AGENT_GUIDE.md` шаг 9а):** перед КРУПНОЙ фичей обязателен гуглёж
  отраслевых золотых стандартов и научной базы + разведдок в `researches/`; код и детальный план
  запрещены, пока документа нет. Дословно: «почти на всё в индустрии есть золотые стандарты и научные
  работы». Даны триггеры «что считать эпиком», состав документа и дисциплина поиска.
- 📄 **`researches/stream_catchup_gold_standards.md`** (931 строка, 81 источник, 13 агентов) — правило
  9а в действии. Главное: **гистерезис, который Криник сформулировал интуитивно, совпал с каноном
  БУКВАЛЬНО** (13 подтверждений, 4 из продакшн-кода: NetEQ, Liang 2003, Moon 1998, dash.js). Плохая
  новость: весь канон — про ПРИЁМНИК, догона на ОТПРАВИТЕЛЕ нет ни у кого; Telegram в трансляциях
  (наш класс задач) долг не догоняет, а сбрасывает. 15 опровергнутых скептиками утверждений выброшены.
- 📄 **`plans/21_stream_resilience_levels_2_4.md`** — пять работ после интервью 011 (11 агентов,
  4× SOUND_WITH_FIXES + 1× BROKEN).
- ✅ **Интервью 012 ОТВЕЧЕНО** — работа D переписана целиком: **ни звука, ни вибро**; вместо них
  пульсирующая обводка экрана (жёлтая/красная/зелёная), пуш только в фоне или на локскрине,
  настройка интенсивности. `sudo` для дросселя канала ОТМЕНЁН — вместо `pfctl` свой TCP-прокси
  в userspace (`tools/net-chaos.mjs`), точнее и без root (идея самого Криника).
- 🆕 **Вижн, дополнение 5 (idea 43):** «счётчик потерянных кадров у нас должен быть 0 всегда» —
  инвариант **И3**: кадры не выбрасываются, а откладываются и догоняются. Это главное отличие от OBS
  и приёмочный критерий буфера. **Ёмкость буфера: 10 минут контента, но не более 2 ГБ** (его решения
  того же вечера); при 2 ГБ лимит по объёму не срабатывает ни на одном реальном битрейте.
- 🆕 **`ideas/44`** — настройки виртуальной камеры, начиная со звука: 7 синтетических паттернов для
  отладки догона, спроектированных так, чтобы дефект превращался в ЧИСЛО (агент не слышит, но
  измеряет); ухо Криника нужно ровно в одном месте — приемлемость ускоренной речи.
- 🆕 **`homeworks/09`** — эксперимент E4 на живом YouTube (Криник согласился, Р11).

## ✅ 2026-07-28 (вечер) — КРИНИК ОТВЕТИЛ НА ИНТЕРВЬЮ 011 (все 11 развилок закрыты)
Первоисточник закоммичен дословно ДО правок (`6fa7c00`), сводка агента — отдельно с марками `[AI]` (`8aeafce`).
- **Р1 гибрид:** зритель видит ЗАМЕРШИЙ ЖИВОЙ КАДР + небольшую плашку «связь восстанавливается»
  (брендовая заглушка на весь экран для сетевой аварии НЕ используется).
- **Р2 (в):** не сдаёмся никогда, но после ~2 минут — экономный режим + честное «платформа могла уже
  завершить трансляцию». **Р3 (а):** мультистрим консервативно, один энкодер на всех.
- **Р4 (в) + обратный сигнал:** звук/вибро И на аварию, И на восстановление, тумблер в настройках.
- **Р5 — поправка вижна:** «запись в файл и буфер догона — РАЗНЫЕ функции, не связывать». Предложение
  агента «страховочная запись во время эфира» СНЯТО.
- **Р6 (б):** релей позже. **Р7:** пол адаптивного битрейта — в НАСТРОЙКИ ПРОФИЛЯ КОДЕРА (сейчас
  хардкод 1 Мбит/с, который выше полосы плохого 3G).
- **Р9.1:** догоняем ВЕСЬ долг до live (пол «последние 10с не догоняем» отклонён), но через
  **гистерезис** — «не гоняем часто, иногда немного копим», чтобы не было микро-рывков.
- **Р10:** буфер **1 ГБ** (Криник стримит 4К/10 Мбит/с) → в ОЗУ столько нельзя (largeHeap 512 МБ уже
  выбран), значит буфер ДИСКОВЫЙ сегментами. **Р11:** согласен на эксперимент E4 на живом YouTube
  («скажешь — настрою») — ставим ДО разработки догона.
- 🔴 **Урок процесса (EXP-0030):** я едва не стёр ответы Криника `git checkout --` (он писал их в
  некоммиченный файл). Первоисточник владельца коммитить ПЕРВЫМ действием — это не бюрократия, а бэкап.

## 🛡️ 2026-07-26 (вечер) — ВИЖН КРИНИКА «ЭФИР УМИРАЕТ ТОЛЬКО ПО КНОПКЕ СТОП» → разведка Блока Б
Криник в чате задал направление: KrinikCam должна быть уважительна к платформам и держать эфир живым
изо всех сил на плохом интернете (заглушка вместо разрыва), а потом — «ничего из того, что говорит
стример, не должно теряться» (буфер + догон ускоренным плейбеком, коэффициент по размеру долга,
потолок 1.5). Все 4 его реплики закоммичены ДОСЛОВНО в `ideas/43_stream_keepalive_respectful.md`.
- **Разведартефакт по канону 1.6 создан ДО кода:** `researches/network_resilience.md` — 5 агентов
  (код проекта, доки платформ, исходники+байткод RootEncoder 2.4.7, опыт проекта) + синтез.
- 🔴 **ГЛАВНАЯ НАХОДКА (верифицирована грепом):** вижн сейчас нарушается по умолчанию. Эфир умирает
  НАВСЕГДА и МОЛЧА через ~23 с пропажи сети (`maxReconnectAttempts = 5` + бэкофф с капом 8 с);
  сетевого мониторинга нет вообще (`ConnectivityManager`/`NetworkCallback` — **0 совпадений** по
  проекту, в манифесте только `INTERNET`); **ни одна из 9 ручек устойчивости RootEncoder не
  используется** (`setLogs`/`resizeCache`/`setForceRender`/`getItemsInCache`/… — 0 вызовов каждой);
  пол адаптива 1 Мбит/с ВЫШЕ реальной полосы плохого 3G; FGS гаснет вместе с состоянием (приложение
  само отрезает себе шанс восстановиться); путь гибели эфира зовёт `startPreview` (крашевый класс
  bugs 48/63); комментарий на `RtmpStreamer.kt:567-573` про `doingRetry` **фактически неверен**.
- **Бюджет тишины платформ (офиц. доки):** YouTube с авто-стопом ~60 с (broadcast → `complete`,
  возврата нет) · Twitch Disconnect Protection 90 с · Kick 60 с · Facebook 2–3 мин (детект «нет
  данных» за 4 с) · VK 5 мин · Instagram замирает на последнем кадре · Telegram — данных нет.
- **6 уровней обороны, каждый самодостаточен.** Уровни 0–3 (пульс/телеметрия, бесконечный реконнект,
  FGS от сессии, громкая индикация, watchdog замершего отправителя, сетевой колбэк) **не зависят от
  решений владельца — можно брать сразу**. Уровень 4 (слейт) и догон — после интервью.
- **По догону:** «сжать таймстемпы» ≠ «слать быстрее реального времени» (второе Azure документирует
  как причину дисконнекта). Замысел Криника протокольно безопасен. Блокер `VideoEncoder.java:545`
  (surface-режим перезаписывает PTS стенными часами — **проверено по исходникам**) снимается его же
  уточнением «просто плейбек ускоренный»: догон через воспроизведение буфера композитором не трогает
  таймстемпы, держит битрейт и fps штатными и не требует второго стримера.
- **Дешёвая альтернатива той же ценности:** звук = 128 кбит/с (2% потока) → замораживаем картинку,
  роняем видеобитрейт в пол, **звук идём живьём 1.0×** → «что говорит стример» не теряется вообще
  (M вместо XL) + снять запрет «или эфир, или запись» (S) → картинка цела в мастер-файле.
- 🟡 **`interviews/interview_011_network_resilience.md` — 11 развилок ЖДУТ КРИНИКА** (Р9 уже отвечен
  им в чате; открыта под-развилка Р9.1 про пересчёт коэффициента по ходу догона).

## 🎬 2026-07-26 — ЖИВАЯ УХОДЯЩАЯ СЦЕНА во время перехода (`plans/20` ЗАКРЫТ, правка Криника)
Криник: «при переходе текущая сцена замирает — сделай, чтобы уходящий слой был жив, пока он полностью
не зафейдится, чтобы его источники стримили, пока идёт переход». Плюс его развилка: «если две не тянет
— исключение: в текущей сцене замирает старый кадр, а в новой запускается новая камера».
- **Как сделано:** снимок уходящей сцены стал лишь БАЗОЙ, поверх неё каждый кадр перерисовываются её
  ЖИВЫЕ слои (`drawLayerSet` + `outgoingLayers`); слоты уходящих слоёв отцепляются в `retiringSlots`
  (вне `cameraSlots`, поэтому гейт готовности новой сцены не отравляется), а их продюсеры гасятся
  ТОЛЬКО по сигналу `onTransitionFinished` (+watchdog). Вердикт живости: виртуалка — всегда живая,
  UVC — живая, если новая сцена не целит в UVC; **любая встроенная — всегда замирает** (HAL Titan 1,
  bug 60) = развилка Криника. Последняя линия обороны — единая точка открытия продюсера с проверкой
  конфликта физключа (обрывает переход вместо блокировки новой камеры).
- **Попутно:** гигиена опенеров (идемпотентный `close()` UVC — класс SIGABRT bug 28, честный `isAlive`,
  `alive=false` у Camera2, новый `cancelPendingReopen()`); харнес-команда **`scene-transition <id>
  <none|fade|slide> <мс>`** (+ `scene-list`/`scene-transition` в контракте смоука); **телеметрия
  перехода** (удержание/эффект/худший кадр/«+N кадров» по каждому слоту — именно она доказывает живость).
- ✅ **ПРОВЕРЕНО ЖИВЬЁМ** (таблица в `plans/20`): вебка→селфи — уходящий слой **+42 кадра** за переход
  (живой), селфи→вебка — замирает по развилке, одна вебка в обеих сценах — живая без касания камеры,
  стресс 4 переключений — 4 begin/4 finish, сироты погашены, 0 крашей; худший кадр 10мс; smoke PASS.
- 🔴→✅ **РЕГРЕСС, пойманный Криником сразу («переход резкий, некрасиво») — ИСПРАВЛЕН в тот же заход:**
  в ветке ЗАМИРАНИЯ буфер эффекта не заполнялся вообще (условие кадра требовало живых слоёв, а снимок
  ушёл в отдельную базовую текстуру) → фейду нечего было показывать. Фикс: база кладётся в буфер
  эффекта ВСЕГДА на время перехода. Проверено видеозахватом: плавная кривая яркости 130→81→103 за 1.5с.
- ✅ **Криник: «выбранные пункты в диалоге красить в розовый»** — выбранные чипы типа/длительности
  перехода теперь брендовый `#FF1A8C`, невыбранные с приглушённой розовой обводкой (скриншот снят).
- 🧹 Вернул сценам Криника переход «Плавно 400мс» — в тестах я переставлял их на «Выезд 1500мс».
- 🔴→✅ **ВТОРОЙ РЕГРЕСС, пойманный Криником («селфи замирает на одном кадре во время анимации») —
  ИСПРАВЛЕН:** моё правило «любая встроенная камера замирает» оказалось СЛИШКОМ ШИРОКИМ. Развилка
  Криника была про случай, когда камеры физически не тянутся вместе, а селфи + вебка работают
  одновременно (это наша боевая фича мультиисточников). Теперь встроенная замирает ТОЛЬКО если новая
  сцена тоже просит встроенную (двух HAL не тянет, bug 60) либо источник новой сцены неизвестен
  (авто-сев). Проверено: селфи→вебка — уходящая селфи `+54 кадра` за переход (живая), входящая вебка
  `+46`, удержание 374мс, худший кадр 5мс, 0 крашей.
- ✅ **Заодно закрыта гонка гейта готовности:** между началом перехода и приходом новых слоёв в GL набор
  слотов пуст (слоты старой сцены уже отцеплены), и гейт принимал это за «новая сцена готова» —
  удержание схлопывалось в 2мс, входящая камера не успевала отдать кадры (`+2` за 1.5с). Введено
  поколение набора слоёв: сцена не считается готовой, пока её слои не применены.
- 🟡 **Ждёт глаз Криника:** приёмка живости уходящей сцены (что её картинка ДВИЖЕТСЯ во время фейда).
- **Остаток Фазы 2:** переключение сцен ПОД ЖИВЫМ ЭФИРОМ на полигоне (стресс 5×, непрерывность publish).

### ▶️ С чего продолжить СЛЕДУЮЩУЮ сессию (пауза 2026-07-26, ВЕЧЕР)
Состояние: дерево чистое, всё запушено. APK debug стоит на планшете (`192.168.1.3:5555`, `device`).
Сборка требует ОТДЕЛЬНЫЙ JDK 17 (`~/Library/Java/JavaVirtualMachines/jdk-17.0.19+10`) помимо
`JAVA_HOME=JBR` — EXP-0020. За эту сессию кода НЕ трогали: вся работа — разведка Блока Б (сеть).

**🔥 ПРИОРИТЕТ КРИНИКА, назван 2026-07-26 вечером — СЕТЬ И СТАБИЛЬНЫЙ СТРИМ.**
Дословно: «в приоритете на дальнейшие работы - сеть и стабильный стрим».
Это ПЕРЕБИВАЕТ утренний приоритет (менеджер UVC-контролов, `plans/08`) — тот остаётся следующим.

**Первое действие новой сессии:** прочитать `researches/network_resilience.md` (разведдок, там всё —
классы отказа K1-K6, 6 уровней обороны, бюджеты тишины платформ, ловушки) и брать **уровень 0 + уровень 1**
— они не требуют ответов Криника:
- **Ур. 0 (S):** `setLogs(false)`, `setBitrateExponentialFactor(0.5f)`, строка-пульс раз в секунду в
  `startLiveTicker` (`phase|bitrate|itemsInCache|sentVideoFrames|droppedVideoFrames|congested|attempt`),
  оживить `StreamState.droppedFrames`, заменить `startPreview(tv)` → `restorePreviewIfDetached()` в
  `onOutputFailed` (`RtmpStreamer.kt:595`, крашевый класс bugs 48/63), починить лживый комментарий
  на `RtmpStreamer.kt:567-573` про `doingRetry`.
- **Ур. 1 (S):** снять потолок `maxReconnectAttempts = 5` для retriable-причин (реконнект до кнопки
  Стоп), бэкофф с джиттером, **FGS перевести с СОСТОЯНИЯ на СЕССИЮ** (`MainActivity.kt:147-163`),
  громкая индикация аварии (виджет больше не исчезает — `MainScreen.kt:714`).
- **Блокер перед правкой `onOutputFailed`:** сверить javap `MultiRtmpStreamClient` — как `MultiStream`
  2.4.7 раздаёт `reTry`/`reTries` ПО ИНДЕКСАМ (весь анализ сделан по одиночному `RtmpClient`).
- **Приёмочный тест вижна:** `rtmp-server.mjs stop` на живом эфире → попытки идут за пределами 5-й,
  FGS жив, экран гаснет — процесс не мёрзнет → через 3 минуты `rtmp-server.mjs start` → эфир воскресает сам.

**📋 ДЗ Кринику заведено:** `homeworks/08_bugs_acceptance_pack.md` — 8 пунктов приёмки (~15 мин):
заглушка не сжата (61), нет мигания на старт/стоп эфира (62), честная индикация записи (45), ЖИВАЯ
уходящая сцена (plans/20), переходы сцен, снэкбар задней камеры (60), возврат камеры после другого
приложения (64), мультистрим-стоп на реальных ключах (63). Плюс открытое `homeworks/07` (bug 33, USB).

**Следующим после сети — МЕНЕДЖЕР НАСТРОЕК UVC-КАМЕР** (`plans/08_uvc_source_controls.md`, idea 36).
Приоритет Криника от 2026-07-26 утра: «это плохая работа чипа усиления Pico — нужно срочно планировать
менеджер настроек параметров UVC камер». Симптом: автоусиление/экспозиция Pico+ плывут на старте фида.
Первыми — **gain / exposure (авто↔ручной) / brightness**, значения обязаны переживать переоткрытие
камеры (иначе слетят на каждом переключении сцены). Детали и методика замера — в конце `plans/08`.

**🟢 ЛЁГКИЕ задачи (можно брать сразу, тестируемы на харнесе):**
1. **bug 70** — на бейдже записи битрейт мигает в «0,0 Mbps» (тикер считает сумму по RTMP-выходам, а при
   записи выходов нет → затирает значение). Фикс на пару строк в `startLiveTicker`. `bugs/70`.
2. **Мой мусор в сцене Криника:** в «Сцена — копия» (id 8) висят тестовые оверлеи `contract_ov` /
   `overlay_cmd_*` от смоук-прогонов. Убрать + научить `smoke.mjs` работать на СВОЕЙ сцене
   (`scene-new` в начале, `scene-delete` в конце), чтобы харнес не гадил в сцены Криника.
3. **`/check-backlog`** — груминг: пометить DONE накопившееся (45/15/71 уже помечены), пересобрать список.

**🔴 Крупное (требует обсуждения/полигона):**
- **Остаток Фазы 2 сцен:** переключение сцен ПОД ЖИВЫМ ЭФИРОМ (полигон `node tools/rtmp-server.mjs
  start` → `cmd go-live-rtmp rtmp://<мак>:1935/live/test` → 5× `scene-switch` → сервер держит publish,
  0 крашей). Именно там проверяется, что живая уходящая сцена не рвёт эфир.
- ✅ **Блок Б — сеть: РАЗВЕДКА СДЕЛАНА** (`researches/network_resilience.md` + `interviews/interview_011`,
  2026-07-26). Уровни обороны 0–3 берутся сразу, без ответов Криника — см. срез вверху файла.
- **Фаза 2 сцен, часть «shared-слои»** — Криник отложил («слишком сложно, пока в сторону»).

### 📋 Ревизия беклога 2026-07-26 (полный проход по bugs/ plans/ ideas/)
**Берётся прямо сейчас, ничего не мешает** (по возрастанию объёма): bug 70 (S) → гигиена смоука (S) →
idea 42 крестик очистки полей (S) → idea 27 нумерация interviews/homeworks (S) → **уровни 0–3 сети
(S+M, вижн Криника)** → bug 65 локализация (M) → idea 41 кнопки [i] (M) → bug 44 даунмикс (M) →
plans/18_phase2 живой эфир на полигоне (M) → **plans/08 менеджер UVC-контролов (L) — приоритет Криника**.

**19 кандидатов на тег DONE** (файл без тега, но по факту сделано): `plans/20`, `plans/18_phase0`,
`plans/18_phase1`, `plans/02`, `plans/05`, `plans/06`, `plans/graphics_tool`, `plans/automation`,
`plans/phase_0_foundation`, `plans/phase_1_mvp`, `plans/phase2_standby_frame`, `plans/sourses_timeout`,
`plans/idea21_camera_as_layer_worklog`, `bugs/16`, `bugs/50`, `bugs/52`, `bugs/53`, `bugs/23`,
`ideas/39`. Дубли-пары: `ideas/36`↔`plans/08`, `ideas/40`↔`plans/18_scene_profiles`.

**Секция «🐛 Открытые баги» ниже УСТАРЕЛА:** реально из неё открыты только 33, 35 и хвост 29 —
остальные (34/36/37/38/39/40/17/19/22/24/20/25 и пачки 41-47, 48-53) уже DONE-файлы.
Также устарело: п.0 «Ждёт КРИНИКА» (interview_008 снят — радиалку заменили панелью, idea 34 DONE);
п.3 bug 26 (файл уже `26_DONE_*`); «Сборка: ТОЛЬКО JBR» противоречит EXP-0020 (нужен отдельный JDK 17).

**Ждёт ГЛАЗ Криника (фикс есть, харнес зелёный):** bugs 60, 61, 62, 63, 64, 33 — шесть багов за один
проход. **Ждёт слова Криника:** idea 05 (бренд), idea 38 (4 развилки), idea 16 п.2 (делаем ли N
энкодеров), plans/18_phase3 (интервью), idea 33 (UX кропа), **interview_011 (сеть, 11 развилок)**.

## 🎞️ 2026-07-25 (вечер, заказ Криника в чате) — ПЕРЕХОДЫ МЕЖДУ СЦЕНАМИ + чёрный кадр убран (bug 71)
Криник живьём: «переключаюсь со сцены с вебкой на сцену с селфи — селфи словно с нуля инициализируется,
чёрный прямоугольник висит» + «давай сделаем переходы: в модалке редактирования сцены выбор типа
перехода и его длительность». Обе задачи закрыты ОДНОЙ машинерией.
- **Диагноз bug 71 (данные с устройства):** удержание кадра жило в СЛОТЕ камеры (по id СЛОЯ). Сцена с
  вебкой держит слой `camera`, сцена с селфи — `camera_1` → при переключении старый слот удаляется,
  новый создаётся ПУСТЫМ → чернота, пока Camera2 открывается (~0.6с и дольше). Идея Криника «держать
  предпоследний кадр» была уже реализована, но НЕ переживала смену сцены — правильный уровень
  удержания оказался ВЕСЬ КОМПОЗИТ, а не слой.
- **Сделано:** снимок старой сцены целиком (`beginTransition` → свой FBO) ДО отдачи новых слоёв;
  снимок ДЕРЖИТСЯ, пока новая сцена не готова (каждый слот-камера забрал ≥2 кадра — видимый кадр
  отстаёт на один из-за пинг-понга), кап 3с; затем играет эффект. Типы: **Мгновенно / Плавно (фейд) /
  Выезд**, длительность 0.2/0.4/0.8/1.5с — в модалке редактирования сцены (диалог переименования
  расширен). Переход принадлежит сцене, НА которую переключаются (семантика OBS).
- **Модель:** Room **v5→v6** (`MIGRATION_5_6`, аддитивно с дефолтами FADE/400мс, schema 6.json в git),
  `SceneTransition {NONE,FADE,SLIDE}`, проводка репозиторий→VM→UI, User Manual §4 EN/RU (заодно закрыт
  мой долг «раздел про профили сцен»).
- ✅ **Проверено живьём:** миграция на РЕАЛЬНОЙ базе Криника (3 сцены целы); переключение вебка→селфи:
  `снят кадр старой сцены` → `новая сцена готова через 611мс` → `эффект FADE 400мс` → `завершён`.
- 🟡 **Ждёт глаз Криника:** визуальная сверка финальной доводки (требование «2 кадра»). Переснять
  видеозахват не смог — планшет был занят Криником, DRM-контент блокирует `screenrecord`.
- 🧹 **Мой мусор в сцене Криника:** смоук-прогоны (`smoke.mjs`) добавляют оверлеи в АКТИВНУЮ сцену — в
  «Сцена — копия» накопились `contract_ov`/`overlay_cmd_*`. Убрать (и научить смоук работать на своей
  сцене) — задача в беклог.
- **Остаток Фазы 2:** переключение сцен ПОД ЖИВЫМ ЭФИРОМ на полигоне (стресс 5×, непрерывность publish).

### ✅ `plans/20` (живая уходящая сцена) — ЗАКРЫТ 2026-07-26, см. срез выше


## ⏺️ 2026-07-25 (после паузы, без вебки) — bug 45 ЗАКРЫТ: честная индикация записи + старт в 4-5× быстрее
Задача №1 из списка «лёгкие-но-полезные» (тест на виртуалке, Pico+ у Криника). Прошло по всем 4 шагам
`plans/15` (теперь `15_DONE`).
- **S1 замер опроверг гипотезы тикета:** синхронная настройка старта — всего ~470мс, а до первого
  реально записанного семпла проходило **2769/2468мс** → ~2.3с кадров и звука кодировалось и
  ВЫБРАСЫВАЛОСЬ (отсюда «отрезало начало фразы»).
- **Root cause (байткод RootEncoder 2.4.7):** `AndroidMuxerRecordController.recordVideo` открывает
  видеотрек ТОЛЬКО при `STARTED` + известных ОБОИХ форматах + **ключевом кадре**. Первый IDR
  проскакивает, пока не пришёл аудиоформат, а следующий — через `iFrameInterval = 2с`. Это ожидание
  по расписанию GOP, а не «медленная инициализация».
- **Фикс:** (1) `StreamState.Connecting` стал `data class(isRecording)`; бейдж «ЗАПИСЬ» и таймер
  поднимает ТОЛЬКО колбэк `RECORDING` (первый семпл), до этого — янтарная пилюля «● ПОДГОТОВКА»
  («ПОДКЛЮЧЕНИЕ» для эфира), меню в этой фазе даёт «Стоп». (2) серия `StreamBase.requestKeyframe()`
  после `startRecord` — не ждём следующий IDR по GOP. `iFrameInterval` не трогали.
- ✅ **ПРОВЕРЕНО ЖИВЬЁМ:** прогрев **2769/2468мс → 559/605/848мс**; видеозахват экрана покадрово
  (пусто → ПОДГОТОВКА → ЗАПИСЬ • 00:00, таймер с нуля); пилюля эфира на кривом URL; ffprobe файла
  (h264 1280×720 933 кадра + aac, обе дорожки с нуля); smoke PASS; юниты зелёные; раздел User Manual
  §7 EN/RU дописан и виден на устройстве. `bugs/45_DONE`, EXP-0025, judge пройден.
- 🆕 **bugs/70 (низкий, побочная находка)** — на бейдже записи битрейт мигает в «0,0 Mbps»: секундный
  тикер считает битрейт суммой по RTMP-выходам, а при записи выходов нет → затирает значение от
  `onNewBitrate`. Пре-существующее (idea 37), НЕ регресс. Дешёвый фикс, кандидат в пул.

## 🎬 2026-07-25 — ЭПИК «Профили сцен» ФАЗА 1 СДЕЛАНА (набор именованных сцен + менеджер) + разблок сборки
По указанию Криника («А) сделать сцены, Б) сеть»). Сделан блок А (сцены). Плюс новый вижн Криника:
**у сцен — эффекты перехода (фейд/выезд, как в OBS)** → зафиксировано в idea 40 / plans/18 как **Фаза 2**.
- **🔧 РАЗБЛОКИРОВАНА СБОРКА (была сломана):** Android Studio обновил JBR до Java 21, а проект компилится
  тулчейном **17** → любая сборка падала `No locally installed toolchains match {languageVersion=17}`.
  Поставил Temurin 17 в `~/Library/Java/JavaVirtualMachines/` (tar.gz с Adoptium, без sudo). `JAVA_HOME=JBR`
  теперь НЕ самодостаточен — нужен отдельный JDK 17. Детали: EXP-0020, AGENT_GUIDE «Сборка». Красный
  эрор Gradle в VS Code — устаревший, уйдёт после релоада Gradle-проекта в IDE.
- **Фаза 1 (interview_010: панель-список, полный CRUD, диалог + «Сцена N»):** таблица Room `scene_profiles`
  (БД v4→**v5** + `MIGRATION_4_5`, schema в git), активный id в DataStore, `SceneProfileRepository`
  (сев из легаси Ф0 — работа Криника не теряется; автосейв активной; create/duplicate/rename/delete/switch;
  **изоляция оверлеев по сцене** `overlays/scene_<id>/`), проводка стримера, панель `ScenesManagerOverlay`,
  харнес `ui.mjs cmd scene-*`. Файлы: `data/profiles/.../db/SceneProfile*`, `feature/streaming/.../scene/persist/SceneProfileRepository`, `app/.../ui/overlay/ScenesManagerOverlay.kt`.
- ✅ **ПРОВЕРЕНО ЖИВЬЁМ:** миграция v4→v5 без краха; сев из Ф0 (2 слоя); CRUD+нумерация; **персист через
  рестарт** (активная восстановлена, сцены целы); delete active→fallback, delete последней→авто-дефолт
  (никогда не «ноль сцен»); **изоляция оверлеев** (scene_4 с PNG / scene_5 пуст, switch сохраняет per-scene);
  UI-панель рендерится и переключает живьём (подсветка активной реактивна).
- ✅ **bug 66 (ВЫСОКИЙ) — НАЙДЕН+ИСПРАВЛЕН:** переключение сцены со встроенной камерой роняло приложение
  (`CameraDevice was already closed` — гонка async-колбэка Camera2-опенера: close() между onOpened и
  onConfigured). Фикс: `@Volatile closed`-гард + runCatching в колбэках `DeviceCamera.kt`. Стресс 5× — 0 крашей.
- 🟢 Закоммичено+запушено **0.8(7)** (judge зелёный). 🟡 **Ждёт живой приёмки Криника** UI сцен (реальная вебка).
- ✅ **bug 67 (ВЫСОКИЙ, Криник живьём) — ИСПРАВЛЕН:** источник БАЗОВОГО слоя протекал между сценами (жил в
  глобальном `DeviceManager`, не в сцене — дуализм из плана, при Ф1 упущен). Фикс: база стала scene-owned
  (единая реконсиляция по `scene.source`, пикер/подсветка/харнес → в сцену; проактивность — bootstrap-сев
  None-базы В СЦЕНУ). **Проверено живьём:** scene A=virtual, scene B=uvc раздельно в БД, переключение
  бесшовно меняет фид базы, протечки нет. `bugs/67_DONE`, EXP-0022. Запушено (0.8(9)).
- ✅ **bug 68 (ВЫСОКИЙ, Криник живьём) — ИСПРАВЛЕН:** переключение сцен переинициализировало UVC (чёрный/
  заглушка Pico+), даже когда обе сцены на ТОМ ЖЕ Pico+. Фикс: гард в `setCameraOpener` — тот же `sourceKey`
  + продюсер жив → НЕ переоткрывать физкамеру (оставляем живой фид). **Проверено живьём:** переключения на
  одном Pico+ → 0 физических close/open UVC, фид живой; при разной структуре сцен база не рвётся (лог «БЕЗ
  переоткрытия (bug 68)»). `bugs/68_DONE`, EXP-0023. Коммичу этим заходом (0.8(10)).
- 🆕 **Вижн Криника (чат 2026-07-25): именованные ПЕРЕИСПОЛЬЗУЕМЫЕ слои** (OBS-shared: слой = именованная
  сущность, сцена ссылается; шарить между сценами для бесшовности ИЛИ независимо). Бесшовность на уровне
  физ-источника уже есть (bug 68); МОДЕЛЬ shared-слоёв → **Фаза 2 через интервью** (plans/18 развилка 5).
  Криник (чат): «слишком сложно, пока в сторону» — модель shared-слоёв ОТЛОЖЕНА.
- ✅ **bug 69 (ВЫСОКИЙ, Криник живьём) — РЕГРЕСС от bug 66, ИСПРАВЛЕН:** селфи (встроенная камера) навсегда
  в заглушке. Причина: гард bug 66 (`@Volatile closed`, ставится в `close()`) отравлял close-before-open в
  `open()` → `onOpened` видел `closed=true` → закрывал камеру до конфигурации. Фикс: сброс `closed=false`
  после внутреннего close() в `open()`. **Проверено живьём 3/3** (селфи live ~207КБ vs заглушка ~97КБ).
  `bugs/69_DONE`, EXP-0024. Остаток: транзиентные `No such device` при переходе uvc→builtin (пре-существующий
  churn пересоздания ST, фид восстанавливается) — мелкая полировка при желании. Коммичу (0.8(11)).

### ▶️ С чего продолжить СЛЕДУЮЩУЮ сессию (пауза 2026-07-25, вечер)
⚠️ **Pico+ (UVC-вебка) ЗАБРАНА Криником** — на след. сессии для теста доступны только **виртуалка**
(`ui.mjs cmd set-layer-source "camera virtual"`) и **встроенные камеры** (front/rear). UVC-зависимые задачи
(bug 25/33/35, живой мультистрим) НЕ брать без вебки.

**🟢 ЛЁГКИЕ-НО-ПОЛЕЗНЫЕ задачи (тестируемы без UVC — виртуалка/встроенные; брать по одной):**
1. ✅ **bug 45 — честная индикация записи — СДЕЛАНО 2026-07-25** (см. срез выше: `bugs/45_DONE`,
   `plans/15_DONE`, EXP-0025). Заодно прогрев старта записи 2.5с → ~0.6с.
1a. 🆕 **bug 70 — битрейт на бейдже записи мигает в «0,0»** (низкий, косметика, ДЕШЁВЫЙ): тикер
   считает битрейт суммой по RTMP-выходам, при записи выходов нет → затирает значение от записи.
   Тест виртуалкой + покадровая нарезка видеозахвата. `bugs/70`.
2. **User Manual — раздел «Профили сцен»** (часть definition-of-done, я пропустил): описать Фазу 1 сцен
   (набор именованных сцен, переключение, дублирование/переименование/удаление) нейтральным энц. языком
   EN+RU. Без устройства. Конвенция — AGENT_GUIDE «User Manual».
3. **`/check-backlog`** — груминг: пометить DONE закрытые (bugs 66/67/68/69 уже DONE-файлы; пройтись по
   bugs/ и plans/), пересобрать открытый беклог. Без устройства.
4. **Полировка transition-churn** (хвост bug 68/69): при смене физ-источника на слое (virtual→builtin и
   наоборот) мелькают транзиентные `No such device` из-за лишних `recreateCameraSurface`. Свести к одному
   пересозданию. Тест виртуалка↔встроенная (без UVC). Средняя сложность — брать, если хочется технической.
5. **«Истинно пустая база»** (мелкая фича из bug 67): дать возможность базовому слою быть None без
   bootstrap-дозасева (для «BRB»-сцены без камеры) — напр. флаг «пусто намеренно». Тест виртуалкой.

**🔴 НЕ лёгкое (не брать без обсуждения / без вебки):**
- **Блок Б — сеть** (Криник хочет, но БОЛЬШОЙ модуль): разведдок `researches/network_resilience.md`
  (idea 37 адаптив уже боевой, RootEncoder cache/queue по байткоду, RTMP vs SRT, догон из буфера) →
  интервью по стратегии → план. Фоновый агент-разведка в этой сессии НЕ долетел — переснять.
- **Фаза 2 сцен** — именованные shared-слои (Криник: «слишком сложно, пока в сторону») + эффекты перехода
  (фейд/выезд). Только через интервью. `plans/18` развилки 4-5.

### 🟡 Ждёт живой приёмки Криника (когда вернёт вебку)
Переключение сцен на живой Pico+ (bug 68 бесшовность), UI-панель сцен. Селфи (bug 69) — можно принять и
без вебки (встроенная камера).


## 🧰 2026-07-25 — KAIF обновлён до 1.6 «Homeostatic KAIF» (`/kaif-update`, миграция уважительная)
Канонический дифф 1.5→1.6 (15 файлов, всё аддитивно; машинерия KAIF-CORE не менялась) перевыведен в
русскую обвязку, кастомизации целы, зеркала навыков 5 агентских систем синхронизированы, контент-артефакты
не тронуты. Главное для работы: **разведартефакты до кода** (разведдок/канон-карта/инвентарь паритета,
шаг 9 чеклиста) + «правило трёх дверей» (PHILOSOPHY); **`/fable-judge` перед КАЖДЫМ push/деплоем** + 6
guardrail-охот; **правило одного шага** в лупах + git-гигиена (`diff --stat`, ignore-first, первоисточники
Криника неприкосновенны); секция **«Решения, принятые без владельца»** при закрытии задач; провенанс-марки
`[AI]…[/AI]`; Repro/Not for в записях опыта. Попутно починен легаси-валидатор `tools/kaif.mjs check`
(ждал скаляр `agent`, маркер 1.5+ несёт массив `agents`; проверен на здоровом и сломанном маркере).
Оба валидатора зелёные. Детали: `KAIF_FRAMEWORK.md` → «Запись о развёртывании».

## Где мы (одним взглядом)

| Веха | Статус |
|------|--------|
| Phase 0 — фундамент (Gradle, CI, tools) | ✅ v0.1 |
| Phase 1 — USB UVC превью | ✅ v0.2 |
| Phase 2 — RTMP-стрим (YouTube, ландшафт+портрет) + дев-харнесс | ✅ v0.3–v0.5 |
| **GL-композитор «камера = слой» (мобильный OBS)** | ✅ дефолт (Phase 3) |
| KAIF 1.1→1.2 (Idea 30/31, /kaif-update) | ✅ 2026-07-06 |
| **Phase 3 — композитор = ДЕФОЛТ + единая модель поворота** | ✅ ЯДРО ГОТОВО 2026-07-06 (plans/02); хвост: bug 25 + DONE-теги |

Дорожная карта: `MASTER_PLAN.md` (ревизия 2026-07-02 — перевыведен из обновлённого `GOAL.md`
«мобильный OBS»). Полная история сессий — `researches/status_archive.md`.

---

## ❓ Ждёт КРИНИКА

0. **❓ на ревью: idea 34 (слои → радиальное меню)** — заменить bottom-sheet слоёв на радиал
   (как FAB/углы): пункты-слои с мини-кнопками (👁/🗑/z), «＋Добавить слой», детальные настройки → модалка.
   Оформлено со слов Криника (офис 2026-07-07). Вопросы эргономики → **interview_008** (Q1-Q6, ждут
   ответов Криника). Реализацию начинаем ПОСЛЕ ответов интервью.
1. **idea 05 (SEO README)** — публикация = публичный бренд, черновик готов
   (`researches/readme_seo_draft.md`) — на бренд-ревью.
2. UX-дизайн от Криника (когда дойдут руки): idea 20 (выбор источника), кнопки записи видео/фото
   (механизм готов), app icon / notification icon.
3. bug 26 (VS Code «phased action failed 25.0.3») — фикс применён, ждёт подтверждения после
   перезагрузки окна VS Code.

> interview_006 (модель поворота) — ✅ ОТВЕЧЕН, реализован в Phase 3.
> homeworks/04 — ✅ Криник прошёл (виртуалка отлично; физкамеры — косяки ориентации, закрыты моделью).

## 🐛 Открытые баги

- ✅ **Пачка Криника (мульти-источники) — bugs 57/58/59 ВСЕ DONE (0.7, 2026-07-19):**
  **57 DONE** — «Добавить слой → Устройство захвата видео» открывает модалку выбора источника, слой
  создаётся сразу с ним (приёмка живьём). **58 DONE — ШАРИНГ ФИДА (Вариант 2 по решению Криника):**
  один источник можно класть на НЕСКОЛЬКО слоёв (устройство открывается ОДИН раз, кадр раздаётся в
  слои-зеркала `mirrorOf`, как OBS «дублировать источник»); краш двойного open исчез как класс.
  Приёмка живьём для фронталки И для UVC (Piko+): full-frame + PiP-копия одновременно, оба живые
  (PSNR зеркала конечный); UVC-нюанс — зеркало нельзя закрывать (делит физ-объект с первичным),
  фикс `openedLayers`+гард. Разные источники остаются независимыми (idea 21 цела). **59 DONE** — дедуп
  UVC по VID+PID в `DeviceManager`; физприёмка Криника пройдена («в списке не плодится»).
- 🆕 **Пачка Криника 2026-07-18 (bugs 41-47) — ревью фичи «профиль кодера» + живые находки. Планы
  готовы, реализация — вечером 2026-07-18.** **41-44 → `plans/14`** (профиль кодера в ОТДЕЛЬНЫЙ
  менеджер: платформа лишь ссылается на профиль по id; +AV1, битрейт в Мбит/с, три режима каналов
  звука Стерео·Моно·Объединённое — развилки закрыты Криником). **45 → `plans/15`** (запись стартует с
  задержкой, режет начало фразы). **46 → `plans/16`** (ручной выбор языка + «следовать системе»).
  **47 → `plans/17`** (нет заглушки при hot-detach — связан с bug 17/35). Правки 42/43/44 = той же
  вчерашней фичи → делаем один раз уже в структуре plans/14, старую НЕ коммитим как финал.
- 🆕 **Вторая пачка Криника 2026-07-18 (bugs 48-53 + рефреш 16), планы фикса в документах:** **48**
  (КРАШ при остановке записи — нужен стектрейс), **49** (КРАШ при возврате из Settings назад — нужен
  стектрейс), **16** (у слоя камеры нет кнопки удаления — подтверждён живьём; ВСЕ слои удаляемы) +
  **50** (нельзя добавить несколько слоёв камеры) → оба = idea 21 + plans/05 Фаза B, **51** (запись в
  файл кодируется выбранным профилем кодера) → plans/14, **52** (рамка выбранного слоя тоньше+alpha),
  **53** (плашка эфира «пляшет»/битрейт мерцает — стабилизировать лейаут + сгладить битрейт).
- 🔄 **bug 34 (мультистрим, КРИТ)** — **S1 ГОТОВ+ПРОВЕРЕН** (стоп/рестарт чинится, 0.6(17)); осталось
  S2-S5 (изоляция сбоя/реконнект/приёмка). ✅ **bug 40 DONE** (поворот, сверен живьём).
- 🔴 **bug 35 (USB-мост, фантомы) — ПОДТВЕРЖДЁН ЖИВЬЁМ Криником 2026-07-18** (3 дубля Piko+ в списке
  источников при каждом подключении). Фикс — `plans/11`. Остальное из ревизии 2026-07-12: bug 36
  (эфир/экран — DONE ночью), bug 37/38/39 (DONE ночью). Отчёт: `researches/code_audit_2026-07-12.md`.
- 🔴 **bug 33** — USB-диалог на каждом hot-plug; root cause уточнён (см. выше), ждёт лога Криника.
- 🔴 **bug 25** — noname «2K USB Camera» чёрный экран: negotiation `err=-99 unsupported preview size`
  (хардкод 1920×1080 ей не подходит). Root cause найден; фикс — `getAllPreviewSizes` (после Phase 3).
- 🟢 **bug 20/23** — краш превью при отрыве источника — АРХИТЕКТУРНО ЗАКРЫТ Phase 3 (превью всегда
  живо, ветвление when(activeSource) убрано; проверено — крашей нет). → DONE-тег после сверки Криника.
- 🟡 **bug 19/22/24** — ориентация/чёрный экран физ-камер — модель поворота реализована (сырой поток
  + поворот слоя); перепроверить каждый на едином пайплайне и закрыть.
- 🟡 **bug 16** — камера-слой не удаляется из UI — закрывается idea 20 (выбор источника).
- ⏸️ **bug 17** — качество standby-заглушки — заглушка-источник снесена; пересмотреть как fallback-СЛОЙ.

## 🤖 Автономный пул (без камеры и Криника)

**Пул ПОПОЛНЕН ревизией 2026-07-12** (всё проверяемо без камеры/Криника — виртуалка + полигон):
bug 38 (debug-gate ресиверов), bug 39 (CMD-протокол + контрактный тест в smoke), plans/12 S1/S3/S4
(Room-схемы, redactRtmp, versionCode), plans/11 S1-S2 (мост+дедупы), plans/09 (мультистрим на
полигоне), plans/10 (keep-screen-on + FGS, приёмка через KEYCODE_POWER). Порядок — по interview_009 Q1.

## Ключевые константы проекта

- **Устройство:** Headwolf Titan 1 (serial `DHF8256GB25442874`, Android 16/SDK 36), ADB WiFi — IP
  динамический (переподнятие через USB: `adb tcpip 5555` → `adb connect <ip>:5555`).
- **Камера-тест:** Разные. Иногда UVC Emeet Piko+ 4K, иногда noname 2K USB Camera, иногда виртуальная камера - больше это не играет значимой роли, так как реализуем слои, и слоем может быть источник видео - в нём источником видеопотока может быть любая камера, хоть виртуалка, хоть UVC, хоть одна из встроенных камер устройства - настройка источника. Тестировать нужно на разных в зависимости от задачи.
- **Сборка:** ТОЛЬКО JBR (`JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"`)
  + `node tools/build.mjs --no-ui`.
- **Git:** только `main`, коммит+пуш = `node tools/commit.mjs "..."`.
- Решения Криника по продукту — `MASTER_PLAN.md` §10 и архив (`researches/status_archive.md`, секция
  «Важные решения»).
