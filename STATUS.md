# KrinikCam — Текущий статус проекта

> Читается ИИ-агентом перед каждой задачей; обновляется после каждой значимой задачи.
> 📏 **Правило размера (ревизия 2026-07-02):** STATUS — КОРОТКИЙ живой срез (~экран текста), НЕ журнал.
> Историю сессий уноси в `researches/status_archive.md` (append в конец), форензику — в `bugs/`,
> детали идей — в `ideas/`. Канон — `AGENT_GUIDE.md`; мышление — `PHILOSOPHY.md` (ПРОСТОТА).

_Обновлено: **2026-07-29, вечер** · версия 0.8 (31) · релиз **v0.8**_

## ▶️ С ЧЕГО НАЧАТЬ НОВУЮ СЕССИЮ — ЧИТАТЬ ПЕРВЫМ (срез 2026-07-29, вечер)

### 1. Обстановка

- Все коммиты запушены в `main`. На планшете стоит debug-APK **0.8 (31)** (поставлен 2026-07-29 вечером);
  версия не различает сборки — **перед любой проверкой пересобери и переустанови**, иначе тестируешь
  неизвестно что.
- Устройство: **Headwolf Titan 1**, ADB по Wi-Fi `192.168.1.3:5555`. Вебка **EMEET Piko+
  подключена** к USB планшета (Криник подключил её 2026-07-28 вечером).
- 🔴 **Планшет НЕ подключён к Маку кабелем** (`adb shell dumpsys usb` → `connected=false`; единственный
  транспорт — Wi-Fi). И 🔴 **в планшете активна SIM**: Android вправе увести дефолтную сеть на LTE
  в любой момент, и тогда прогон на LAN-полигон даст ложный «дефект приложения» (`bugs/74`).
  Для сетевых прогонов — кабель ИЛИ выключенные мобильные данные.
- Сборка: `JAVA_HOME=JBR` **И отдельный JDK 17** (EXP-0020), иначе падает на тулчейне.
- Полигон: `node tools/rtmp-server.mjs start|stop|status|url|freeze|thaw`. IP Мака **меняется** —
  бери из `node tools/rtmp-server.mjs url`, не из старых записей (сейчас был `192.168.1.223`).

### 2. Приоритет Криника (не меняется): СЕТЬ И СТАБИЛЬНЫЙ СТРИМ

Рабочий документ — **`plans/21_stream_resilience_levels_2_4.md`**. Порядок работ и обоснование там.
Состояние: **уровни 0, 1 сделаны; Ш0 сделан; работы A и B сделаны; работа C — шаги C1, C2, C3 сделаны
и приняты живьём 2026-07-29** (заморозка кадра · лента-плашка в эфире · автоматическая политика).
Честные остатки: у **A** не проверена форма восстановления на РЕАЛЬНОМ узком канале (нужен дроссель
`tools/net-chaos.mjs`, которого ещё нет), у **B** открыты **B7** (мультистрим/изоляция по индексу) и
**B8** (прямой вердикт «выходит ли `reTry` из заблокированной записи»).
**Следующий шаг: C4** (экономный режим после ~2 минут, ответ Криника Р2), затем C5–C7, потом
**D (индикация) → E (сетевой колбэк)**.

> ⚠️ **Дверь (б) политики слейта** («пол адаптива + затык») и главный сценарий работы C — узкий, но
> живой канал — **не проверены**: убийство приёмника их не моделирует, нужен дроссель
> `tools/net-chaos.mjs` (Криник одобрил userspace-прокси вместо `sudo`, интервью 012 В3).

### 3. ~~ПЕРВОЕ ДЕЙСТВИЕ~~ — `bugs/74` ✅ РАЗОБРАН 2026-07-29

**Причина: планшет уходит с Wi-Fi на сотовую посреди эфира.** Через 30 с «linger» Android отпускает
Wi-Fi-сеть, RTMP-сокет умирает, а реконнект стучится на LAN-адрес Мака `192.168.1.223` **уже через
LTE**, где тот недостижим в принципе — отсюда `SocketTimeoutException after 5000ms` при «живой сети».
Таймлайн с логами — в `bugs/74`. **Это не дефект приложения:** эфир воскрес САМ, когда Wi-Fi вернулся
(`dropV=0`, `attempt=0`) — то есть уровень 1 отработал в боевом сценарии.

Прежняя проверка «ушёл на сотовую» дала ложное «исключено», потому что `dumpsys connectivity`
снимался ПОСЛЕ возврата Wi-Fi. **Методика:** сетевую гипотезу проверять только непрерывным логом,
покрывающим момент отказа, — не срезом состояния.

**Следствие для прогонов:** пока в планшете активна SIM, любой прогон на LAN-полигон может получить
ложный «дефект приложения». Гонять сеть по USB **или** с выключенными мобильными данными.

Работа C больше этим НЕ заблокирована — полигон держит эфир (проверено: `bitrate≈4.6–5.4 Мбит/с`,
`cache=0`, `dropV=0`).

### 4. Что ждёт КРИНИКА (без него не двинется)

| Что | Зачем |
|---|---|
| **`homeworks/09` — эксперимент E4** на его YouTube | принимает ли живая платформа ускоренный поток. **Блокер всего догона** |
| ~~Выбрать мокап плашки~~ ✅ **ЗАКРЫТО 2026-07-29** | Криник выбрал **вариант C («нижняя лента»)** + правки «фон темнее, лента крупнее» — внесены. Эталон для кода: `assets/graphics/mockups/network_slate/slate_C.svg`. **Работа C разблокирована** |
| `ideas/44` — состав звуковых паттернов виртуалки | отладка догона числами |
| Приёмка глазами: bugs 60/61/62/63/64/33, UI сцен, живучесть уходящей сцены | накопилось с прошлых сессий |

### 5. Грабли этой сессии — не наступать снова

1. **ADB по Wi-Fi умирает при гашении экрана**, и это выглядит как «приложение замерло». Прежде чем
   объявить тишину поведением системы — проверь, жив ли `adb logcat` (EXP-0033). Сетевые прогоны —
   **по USB** (§10 плана 21).
2. **Не выводить арифметику заново, если она уже разобрана в разведдоке** — Криник поймал меня на
   ошибке про полосу догона (EXP-0031). Числа сверять грепом по `researches/`.
3. **Три факта по байткоду — верны, НЕ перепроверять:** энкодер в CBR (замерший кадр сам по себе
   полосу не экономит); `setVideoBitrateOnFly` молча выходит при незапущенном энкодере (наш лог =
   намерение, не факт); `getSentVideoFrames()` растёт ДО записи в сокет (доставку им не доказать).
4. **`git checkout -- <файл>` на документе с ответами Криника = уничтожение его работы** (EXP-0030).
   Первоисточник владельца коммитить ПЕРВЫМ действием.

### 6. Лёгкое, если нужен быстрый результат

`bugs/53` (плашка эфира пляшет) · `bugs/52` (толщина рамки слоя) · `bugs/65` (локализация UI по
системной локали) · гигиена смоука (работать на своей сцене, а не в сценах Криника) ·
`/check-backlog` · `bugs/72` (краш в простое — начать с починки FileLogger, он теряет хвост перед крашем).
Ещё один кандидат — **написать `tools/net-chaos.mjs`** (userspace TCP-прокси-дроссель, Криник одобрил
в интервью 012 В3): без него не проверить ни главный сценарий работы C, ни форму восстановления в A.

## 📌 СЕССИЯ 2026-07-28 (день + ночь) — что сделано

Полные записи этой и прошлых сессий — `researches/status_archive.md` (архивировано при этой паузе).

- **Уровни обороны 0 и 1** (пульс/телеметрия, бесконечный реконнект, FGS от СЕССИИ, громкая
  индикация) — сделаны и приняты живьём. Журнал: `researches/network_resilience.md` §14.
- **Ш0** — `freeze`/`thaw` в полигоне + замер «зомби-сервера» K5 → §14.1.
- **Работа A** — пол адаптива переехал в профиль кодера (Room v7) → §14.2.
- **Работа B** — watchdog замершего отправителя → §14.3.
- **Две разведки:** `researches/network_resilience.md` (наша архитектура, уровни обороны 0–6,
  бюджеты тишины платформ) и `researches/stream_catchup_gold_standards.md` (золотые стандарты
  догона, ~78 проверяемых ссылок). **Новое правило канона (Криник):** перед КРУПНОЙ фичей — обязательный
  гуглёж прио-арта + разведдок (`AGENT_GUIDE.md` шаг 9а).
- **Интервью 011, 012, 013 — все ОТВЕЧЕНЫ Криником.** Догон спроектирован по его решениям,
  но заблокирован экспериментом E4 (`homeworks/09`).
- **Вижн, дополнения 5 и 6** (`ideas/43`): инвариант И3 «кадры не выбрасываются, а откладываются»;
  догон на стороне стримера — осознанная ниша («этой болью я и страдаю на трансляциях из парка»);
  при нехватке полосы уступает КАЧЕСТВО, а не контент.
- **Заведены баги:** 72 (SIGABRT в RenderThread на простое), 73 (краш старта эфира из фона —
  ИСПРАВЛЕН), 74 (эфир рвётся через 2–4 с, реконнект не может установить TCP — ОТКРЫТ).
- **Уроки:** EXP-0031 (не выводить арифметику заново — читать разведдок), EXP-0032 (отказ,
  который «нечем воспроизвести», ломается сигналом процессу), EXP-0033 (тишина в логе ≠ тишина
  системы: сначала проверь, жив ли наблюдатель).

## Где мы (одним взглядом)

| Веха | Статус |
|------|--------|
| Phase 0 — фундамент (Gradle, CI, tools) | ✅ v0.1 |
| Phase 1 — USB UVC превью | ✅ v0.2 |
| Phase 2 — RTMP-стрим (YouTube, ландшафт+портрет) + дев-харнесс | ✅ v0.3–v0.5 |
| **GL-композитор «камера = слой» (мобильный OBS)** | ✅ дефолт (Phase 3) |
| KAIF 1.1→1.2 (Idea 30/31, /kaif-update) | ✅ 2026-07-06 |
| **Phase 3 — композитор = ДЕФОЛТ + единая модель поворота** | ✅ ЯДРО ГОТОВО 2026-07-06 (plans/02); хвост: bug 25 + DONE-теги |
| Phase 4 — мультиисточники, сцены, переходы | ✅ в проде (шаринг фида, профили сцен, эффекты перехода) |
| **Phase 5 — мультистрим, кодеки, ЖИВУЧЕСТЬ СЕТИ** | 🔥 ТЕКУЩАЯ. Мультистрим и адаптив боевые; живучесть: уровни 0,1 + Ш0 + работы A и B сделаны (`plans/21`); буфер+догон ждут E4 |

Дорожная карта: `MASTER_PLAN.md` (ревизия 2026-07-02 — перевыведен из обновлённого `GOAL.md`
«мобильный OBS»). Полная история сессий — `researches/status_archive.md`.

---

## ❓ Ждёт КРИНИКА

1. **`homeworks/09` — эксперимент E4** на живом YouTube: принимает ли платформа ускоренный поток.
   **Блокер всей фичи догона** — до него проектировать нечего.
2. **Выбор 1 из 4 мокапов плашки** (мокапы рисует агент) — до кода работы C.
3. **`ideas/44`** — состав звуковых паттернов виртуальной камеры (отладка догона числами).
4. **`ideas/05` (SEO README)** — публикация = публичный бренд; черновик `researches/readme_seo_draft.md`.
5. **UX-решения, когда дойдут руки:** `ideas/20` (выбор источника), иконки приложения и уведомления.
6. **Приёмка глазами** (фикс есть, харнес зелёный): bugs 60, 61, 62, 63, 64, 33 + UI сцен и живость
   уходящей сцены при переходе.

> `interview_006` (модель поворота) и `interview_008` (радиальное меню слоёв) — ✅ отвечены и
> реализованы; `ideas/34` закрыта тегом DONE. `homeworks/04` Криник прошёл.

## 🐛 Открытые баги (файлы без тега DONE, срез 2026-07-29)

**74** — ✅ **причина найдена 2026-07-29** (смена сети Wi-Fi→LTE, не дефект приложения); открытым
остался только вопрос «почему планшет уходит с исправного Wi-Fi» — вне кода приложения ·
**73** (краш старта эфира из фона — ✅ исправлен, ждёт только тега DONE после повторной живой сверки) ·
**72** (SIGABRT в RenderThread на простое + FileLogger теряет хвост перед крашем).

**Ждут глаз Криника** (фикс есть, харнес зелёный): 60, 61, 62, 63, 64, 33.

**Дешёвые:** ~~70~~ ✅ **закрыт 2026-07-29** (битрейт на бейдже записи больше не падает в «0,0») ·
52 (толщина рамки слоя) · 53 (плашка эфира пляшет) · 65 (локализация UI по системной локали).

**Требуют железа/условий:** 35 (фантомы USB-моста) · 54, 55, 56 · 44 (даунмикс моно) · 48, 50 · 16, 23, 29.

> Полные описания — в `bugs/`. Список собран `ls bugs/ | grep -v DONE`; если файл выглядит закрытым —
> прогнать `/check-backlog` и проставить теги, а не держать это в голове.

## 🤖 Автономный пул (без камеры, стрима и Криника)

Прежний список (ревизия 2026-07-12) **протух — пять из шести позиций закрыты тегом DONE**. Актуальное,
что можно грайндить в одиночку на виртуалке и полигоне:

- `bugs/72` — начать с починки **FileLogger** (теряет хвост перед крашем): без этого следующий краш
  снова придёт без контекста.
- `bugs/70` (битрейт «0,0» на бейдже записи) · `bugs/52` (толщина рамки слоя) · `bugs/53` (плашка эфира
  пляшет) · `bugs/65` (локализация UI по системной локали).
- Гигиена смоука: `smoke.mjs` должен работать на СВОЕЙ сцене (`scene-new` в начале, `scene-delete`
  в конце), а не гадить оверлеями в сцены Криника.
- `plans/11` S1-S2 (USB-мост и дедупы) — автономная часть.
- Мокапы плашки для работы C (4 варианта, `tools/graphics`).

> Перед взятием задачи прогони `/check-backlog` — список собирается из файлов без тега `DONE`,
> держать его в голове не нужно.

## Ключевые константы проекта

- **Устройство:** Headwolf Titan 1 (serial `DHF8256GB25442874`, Android 16/SDK 36), ADB WiFi — IP
  динамический (переподнятие через USB: `adb tcpip 5555` → `adb connect <ip>:5555`).
- **Камера-тест:** Разные. Иногда UVC Emeet Piko+ 4K, иногда noname 2K USB Camera, иногда виртуальная камера - больше это не играет значимой роли, так как реализуем слои, и слоем может быть источник видео - в нём источником видеопотока может быть любая камера, хоть виртуалка, хоть UVC, хоть одна из встроенных камер устройства - настройка источника. Тестировать нужно на разных в зависимости от задачи.
- **Сборка:** `JAVA_HOME=JBR` (`/Applications/Android Studio.app/Contents/jbr/Contents/Home`)
  + `node tools/build.mjs --no-ui`. ⚠️ ЭТОГО МАЛО: проект компилится тулчейном 17, а JBR уехал на 21 —
  нужен ОТДЕЛЬНЫЙ JDK 17 в `~/Library/Java/JavaVirtualMachines/` (EXP-0020).
- **Git:** только `main`, коммит+пуш = `node tools/commit.mjs "..."`.
- Решения Криника по продукту — `MASTER_PLAN.md` §10 и архив (`researches/status_archive.md`, секция
  «Важные решения»).
