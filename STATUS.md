# KrinikCam — Текущий статус проекта

> Читается ИИ-агентом перед каждой задачей; обновляется после каждой значимой задачи.
> ⚠️ **STATUS — СВОДКА НАСТОЯЩЕГО, а не летопись (конвенция KAIF 2.1).** Файл, который только растёт,
> превращается в книгу истории проекта, и агент, пришедший за быстрым «где мы», в ней тонет. Правила,
> которые держат его сводкой:
>
> - **Каждая строка проходит два теста:** «*уберу эту строку — ошибётся ли следующий агент?*» и
>   «*прочитает ли новичок весь файл за один присест?*». Мягкий ориентир — **~200 строк**; это
>   предупреждение, а не стена, но пересечение означает, что стрижка просрочена.
> - **Закрытое ВЫНОСИТСЯ, а не копится:** запись перестала быть «сейчас» — переноси её ДОСЛОВНО в
>   `PROJECT_HISTORY.md` (летопись, новейшее сверху; переносить, а не переписывать). Шаг «бонсай-стрижка»
>   живёт в `/end-chat`; `/pause` по замыслу остаётся без церемоний.
> - **Оставляй файл таким, каким сам хотел бы его найти:** свежая сводка того, что работает, что в
>   работе, что дальше, грабли — и КУДА СМОТРЕТЬ за деталями (планы, баги, летопись): указатели, а не
>   пересказы.
>
> Форензику держи в `bugs/`, детали идей — в `ideas/`. Канон — `AGENT_GUIDE.md`; мышление —
> `PHILOSOPHY.md` (ПРОСТОТА). Историческая летопись до 2026-07-31 лежала в
> `researches/status_archive.md` — перенесена в `PROJECT_HISTORY.md` при обновлении на KAIF 2.1.

_Обновлено: **2026-08-02, 15:00** · версия 0.8 (50) · релиз **v0.8**_

> 🌳 **Долг по стрижке — 321 строка при ориентире ~200.** Вес — в разделах «ЭСТАФЕТА» и «Обстановка».
> **Что переносить в `PROJECT_HISTORY.md` на СЛЕДУЮЩЕМ `/end-chat`:** блоки «Что сделано в этом чате»
> и «Грабли этой сессии» целиком — ДОСЛОВНО, ПЕРЕД тем как писать свои (правка на месте = удаление,
> EXP-0050). Уже вынесено: срез дня и вечерний срез 2026-08-02, таблица вех фаз 0–4, дубль раздела
> «Ждёт КРИНИКА».

## ▶️ С ЧЕГО НАЧАТЬ НОВУЮ СЕССИЮ — ЧИТАТЬ ПЕРВЫМ (срез 2026-08-02, 15:00)

### 0. 🤝 ЭСТАФЕТА — с чего начать, если ты пришёл в пустом контексте

Этот чат закрыт **2026-08-02 в 15:00**. Он был коротким и целиком про ИНСТРУМЕНТ: контур согласований
с Криником больше не имеет таймаута — **ждёт ответа сколько понадобится** (инвариант I9), сторож
пульса не путает закрытую страницу с усыплённой машиной, зов повторяется раз в час. Кода приложения
эта сессия НЕ касалась: беклог, баги и приоритеты ровно там же, где их оставил вечерний чат.
**Гард владельца ЧИСТ — вопросов на Кринике нет, можно сразу брать код.**

> ⏳ **Первое, что тебе нужно знать про изменившийся инструмент:** `node tools/owner.mjs ask <док>`
> теперь **не возвращается сам** — запускай его ФОНОВЫМ процессом. Побудка = завершение процесса
> (I8): Криник ответил → контур записал в три места, умер кодом 0, и это твой сигнал продолжать.

Порядок действий:

1. **`node tools/owner.mjs guard`** — что висит на Кринике. Начинай с этого, а не с кода.
   Ожидаемый ответ сегодня: чисто (одна информационная строка про `interview_020` — документ объявлен
   закрытым, но один слот пуст; это НЕ долг).
2. 🆕 **`ideas/46` — ОДОБРЕНА Криником, срока нет: запись в файл ОДНОВРЕМЕННО с эфиром.** Он сам
   назвал её эпиком с ресёрчем. Вход в работу ТОЛЬКО через `/plan-epic` (разведка прио-арта → мета-план
   → фазы). Суть не в снятии гарда, а во ВТОРОМ энкодере: сегодня запись и эфир делят один, поэтому
   «файл в хорошем качестве, эфир в сжатом» физически не выходит. Смыкается с Phase 5 (мультикодек).
3. **`bugs/53` ждёт ГЛАЗ Криника** — измеримое доказано (ширина плашки 383 px в 10 пробах подряд),
   осталось перцептивное: битрейт гуляет в последнем знаке (5,1/5,2/5,3). Вопрос вкуса → его вердикт.
4. **`bugs/65`** — аудит выполнен, инвентарь в документе: **к работе 5 строк в 3 файлах** (а не
   «многие элементы UI»). Ш4 сделан и ПРИНЯТ глазами на ru-локали. Ш2/Ш3 упираются НЕ в текст, а в
   тип: имена живут `String`-полями модели сцены и **уходят в персист** — перевод на `UiText`
   требует миграции сохранённых сцен Криника.
5. **`bugs/76` — только на Titan 1.** Контрольный прогон на A51 НЕ воспроизвёл баг при ПОЛНОМ наборе
   симптомов гонки → версия «гонка» подтверждена, «камера сломана» опровергнута. На A51 прогон
   зелёный и БЕЗ фикса, поэтому принимать фикс там нельзя в принципе.
6. 🆕 **Пройти ЛАНДШАФТ ТЕЛЕФОНА по остальному UI.** `bugs/78` вскрыл класс «UI принимался только на
   планшете», и грепом по нему тут же нашлись два близнеца. Модалки закрыты, но панели, оверлеи и
   экраны настроек в ландшафте телефона никто не смотрел.
7. **ReStream-сервер** по-прежнему разблокирован и НЕ начат: эпик → `/plan-epic`
   (`researches/restream_server.md`, `ideas/38`). Там же ждут **C2b** (`plans/21`) и **догон**.

**Что сделано в этом чате (2026-08-02, 15:00 — короткая сессия про КОНТУР):**

- ⏳ **Контур согласований ждёт БЕСКОНЕЧНО** (заказ Криника дословно: «чтобы сервер ждал ответов от
  владельца бесконечно долго»). `timeoutSec: 0` — бесконечность сделана ОТСУТСТВИЕМ таймера, а не
  большим числом. Прежний час был единственной смертью контура, при которой решение теряется, а не
  записывается. В канон навыка `/owner-reviews` добавлен инвариант **I9** (+ §7.1 в `build-spec.md`).
- 🔴 **Сторож пульса научился отличать закрытую страницу от УСЫПЛЁННОЙ МАШИНЫ.** Приговор вынесен
  чистой функцией `judgeBeat` (`tools/owner.mjs`); улика сна — не часы, а СОБСТВЕННЫЙ проспавший тик
  сторожа. Без этого ноутбук, закрытый на ночь, казнил бы живое окно, и утреннее «Записать» Криника
  упёрлось бы в мёртвый сервер.
- 🔔 **Зов повторяется раз в час** (`remindEverySec: 3600`, флаг `--remind`), в тихие часы молча по
  I6. Строка в консоль печатается ВСЕГДА, даже когда звук подавлен, — именно её проверяет QA.
- **Доказано, а не заявлено:** `selftest` **72/72** (5 новых на пульс; мутация «убрал ветку сна» роняет
  РОВНО одну адресную проверку), `verify` живым браузером **73/73** (было 68; 5 новых на бесконечность
  и повторный зов), осиротевших процессов нет, реестр `_serving.json` пуст. Урок — **EXP-0052**.
- **Решение Криника по развилке (в чате, задача уровня инструмента):** сторож пульса — ОСТАВИТЬ, но
  сделать сон-устойчивым; напоминание — **раз в час**.

**Грабли этой сессии — не наступать снова:**

- **Снятие срока — это не удаление таймера, а инвентаризация ВСЕХ способов умереть.** Таймаут был не
  единственным сторожем ожидания: пульс убивал контур ровно так же, и при бесконечном ожидании начал
  бы ловить ложную цель. Спрашивай про каждый сторож: «на какую улику он смотрит и переживёт ли она
  сон машины?»
- **Проверка может покраснеть не по делу.** Первый прогон QA дал красное на «контур объявляет
  бесконечность» — я читал вывод сразу после адреса страницы, а строка печатается позже. Это гонка
  ВЫВОДА, а не дефект инструмента: жди сам факт, который утверждаешь, а не его соседа по времени.

- 🔴 **Слепой тап по координате чуть не удалил сцену Криника** (прошлая сессия, но грабля живая).
  Координата взята «на глаз» вместо `ui.mjs dump` → попадание в строку соседней сцены; спас только
  диалог подтверждения. **Координаты — ТОЛЬКО из дампа**, даже когда «очевидно».
- **Зелёный прогон на «неправильном» устройстве ничего не доказывает** (`bugs/73` и `bugs/76` — оба
  об этом, с разных сторон). Спрашивай не «прошло ли», а «мог ли этот прогон ВООБЩЕ покраснеть».

> Грабли прошлых сессий, перенесённые в `PROJECT_HISTORY.md`: гард, обманутый строкой объявления ·
> фильтр, отсеивающий вместе с шумом находки.

### 1. Обстановка

- На **A51** debug-APK **0.8 (49)** — свежий, с фиксами `bugs/78`/`bugs/79`. На **планшете** —
  старый **0.8 (35)** (крыша адаптива `interview_016`, фиксы `bugs/75` Ф1+Ф5–Ф7): планшет не
  подключался с утра, фиксы этого дня на него НЕ накатывались. Версия не различает сборки —
  **перед любой проверкой пересобери и переустанови**, иначе тестируешь неизвестно что.
- 🆕 **`tools/viewer-tape.mjs`** — приёмка ГЛАЗАМИ: пишет поток С СЕРВЕРА, переживает обрывы и
  склеивает «ленту зрителя» с честными провалами (`rec` → `stop` → `build --gaps a,b`). Длительность
  провала брать из ЛОГА приложения (`watchdog[0]` → `connected ✓`), а не из меток ffmpeg.
- ⚠️ **В профиле YouTube (Room `stream_profiles` id=7) ПУСТОЙ URL сервера** — эфир из приложения по
  нему уйдёт в никуда. Вписать `rtmp://a.rtmp.youtube.com/live2` через UI. Ключ там валидный.
- ⚠️ **В активной сцене Криника остались 2 моих оверлея** — `remove-layer overlay_N` не совпал по id,
  снять через UI (длинный тап по слою → удалить). Причина накопления устранена (гигиена смоука).
- 🔴 **Titan 1 сейчас НЕ ПОДКЛЮЧЁН** (срез 2026-08-02, день). Он нужен для `bugs/76` и для
  сетевых прогонов по USB (`plans/21` §10) — без него эти работы не двигаются.
- 🆕 **В ПАРКЕ ВТОРОЕ УСТРОЙСТВО — Samsung Galaxy A51** (`SM-A515F`, serial `R58N615KXZZ`), заведено
  2026-08-02. **ADB по Wi-Fi: `ADB_DEVICE=192.168.1.41:5555`** (IP динамический — сверяй). Ценность не в мощности, а в противоположности планшету:
  **Android 13 / SDK 33 — это ровно наш `minSdk`**, Exynos 9611 (2019), 3.6 ГБ ОЗУ, экран 1080×2400.
  Titan 1 — потолок наших возможностей, A51 — **пол**: что работает здесь, работает везде, куда мы
  поставляемся. Вторая независимая ценность — **другой вендор кодеков**: у A51 legacy-**OMX**
  (`OMX.Exynos.AVC/HEVC/VP8/VP9.Encoder`, AV1 нет), у Titan 1 — Codec2 (`c2.mtk.*`); это разные пути
  внутри MediaCodec. OTG есть (`android.hardware.usb.host`) → UVC пойдёт. Проверено живьём:
  обе device-камеры дают картинку, **`smoke.mjs` → PASS** (1920×1080, 140 кадров/6 с).
  Навигация переведена Криником на **жесты** (`navigation_mode=2`) — три кнопки перекрывали UI.
  ⚠️ **A51 НЕ УМЕЕТ две камеры одновременно** — камера-сервис печатает у открытой камеры
  `Conflicting Client Devices: {0, 2, 20, 23, 3, 4, 50, 52}`, то есть ВСЕ остальные. Флага
  `android.hardware.camera.concurrent` нет. `LOGICAL_MULTI_CAMERA` у задней камеры (5 физических
  сенсоров) — это одна логическая камера с несколькими стёклами, НЕ два потока. Сцена из двух живых
  источников на A51 = **UVC + одна камера устройства** (UVC идёт мимо `camera2` и не конфликтует).
- 🟢 **A51 подключён через ПИТАЕМЫЙ USB-хаб** — это снимает конфликт «один порт»: телефон и
  заряжается, и видит камеру. На хабе: **Emeet Piko+** (UVC, до 3840×2160), **USB-Ethernet
  10/100/1000** и кардридер. ⚠️ **Ethernet на телефоне ещё НЕ пробовали** — для сетевых прогонов
  проводной канал управляемее Wi-Fi, это стоит проверить.
- 🟢 **Экран A51 НЕ ГАСНЕТ** (`settings put global stay_on_while_plugged_in 7` +
  `screen_off_timeout 2147483647`). Проверять фактом: `dumpsys power | grep mStayOn` → `mStayOn=true`.
  Вторая настройка не перестраховка: она держит экран, когда телефон не на питании, — без неё
  Wi-Fi-ADB умирает вместе с экраном (EXP-0033).
- 🔴 **При ДВУХ подключённых устройствах харнес ОТКАЖЕТСЯ выбирать сам** (`tools/device.mjs`) и
  напечатает готовые строки `ADB_DEVICE=<serial>`. Это не поломка, а гард: молчаливый выбор «первого
  попавшегося» означал бы тест не на том железе. Одно и то же устройство по USB и по Wi-Fi — тоже два.
- 🟢 **Мобильные данные ВЫКЛЮЧЕНЫ — это решение Криника на день** (2026-07-31, дословно: «мне они
  сегодня не нужны. я потом сам включу»). **Не включай обратно** и не «чини» это. Заодно закрыта
  ловушка `bugs/74`: `dumpsys connectivity` показывает 0 агентов MOBILE, уводить маршрут некуда.
  ⚠️ Проверять надо `settings get global mobile_data1`, а не `mobile_data` — легаси-ключ врёт
  (остаётся `1`). Иконка «4G» в шторке при этом видна: это регистрация SIM для голоса, не данные.
- 🔴 **Сеть переехала на другую подсеть:** Мак `192.168.50.246`, планшет `192.168.50.190`
  (было `192.168.1.x`). IP Мака **меняется** — всегда бери из `node tools/rtmp-server.mjs url`.
- **Контур `/owner-reviews`** развёрнут и обкатан (`tools/owner.mjs`, планы `plans/22`+`plans/23`).
  Главное для исполнителя: **любая запись ответа БУДИТ агента** (инвариант I8), поднимать
  страницу заново — обязанность агента; голос Криника — **eugene**; в конце сессии
  ОБЯЗАТЕЛЕН `node tools/owner.mjs stop`. Детали — `plans/23` и навык `/owner-reviews`.
  ⏳ **С 2026-08-02 контур ждёт БЕСКОНЕЧНО** (`timeoutSec: 0`, инвариант I9): таймаута нет,
  зов повторяется **раз в час** (в тихие часы молча), а усыплённая машина больше не считается
  закрытой страницей. Следствие для исполнителя: **`ask` запускать ФОНОВЫМ процессом** — ждать его
  в переднем плане значит блокироваться до ответа Криника.
- Сборка: `JAVA_HOME=JBR` **И отдельный JDK 17** (EXP-0020), иначе падает на тулчейне.
- Полигон: `node tools/rtmp-server.mjs start|stop|status|url|freeze|thaw`.
  Дроссель: `node tools/net-chaos.mjs start|set|mode|cut|status|stop|selftest` — **работает и
  доказан** (`selftest`: 300 кбит/с → 299). Эфир публиковать **на порт дросселя 1936**, канал
  сужать уже на живом эфире (`set --kbps`).

### 2. Приоритет Криника (не меняется): СЕТЬ И СТАБИЛЬНЫЙ СТРИМ

Рабочий документ — **`plans/21_stream_resilience_levels_2_4.md`**; порядок работ и обоснование там.
Состояние: уровни 0–1, Ш0, работы A и B — сделаны; C1–C3 приняты живьём; A4b закрыт; **C6 частично**.
⚠️ **`bugs/75` (петля реконнекта) НЕ закрыт** — STATUS утверждал обратное, а сам баг-док говорит:
фиксы Ф1+В1/В2 применены и проверены (период петли 12 с → 55 с, эфир на полу живёт на канале 450),
**но петля не исчезла совсем**; измеренный остаток вынесен в `interview_016` §10. Расхождение
поймано на закрытии чата 2026-08-02 — верь баг-доку, он форензика.

Дальше по плану: **доснять C6** (плашка слейта на кадре С СЕРВЕРА, полоса 450) → **C4** (экономный
режим) → C5, C7 → **D** (индикация) → **E** (сетевой колбэк). Отдельными эпиками ждут **буфер+догон**
(`researches/stream_catchup_gold_standards.md`) и **ReStream-сервер** — оба через `/plan-epic`.

> ⚠️ Число, меняющее проектирование: **лестница адаптива проигрывает гонку по построению** — от
> обнаружения затыка до смерти сокета 6 с, а до пола ей нужно ~26 с. Это свойство схемы, а не
> настройка порогов (`researches/network_resilience.md` §14.4).
> ⚠️ Честный остаток: **замершая картинка длится дольше самого обрыва** (17.7 и 22.5 с против 2 с
> реконнекта) — окно `WATCHDOG_STALL_MS`, отдельная работа. И **возврат качества медленный**:
> +20% раз в минуту, с 274 до 4000 ≈ 15 минут.
> Приёмка ГЛАЗАМИ — `tools/viewer-tape.mjs` (лента зрителя с честными провалами).

### 3. Сетевые прогоны — условие корректности

Пока в планшете активна SIM, Android вправе увести дефолтную сеть на LTE посреди эфира, и прогон на
LAN-полигон даст ЛОЖНЫЙ «дефект приложения» (так был разобран `bugs/74`, 2026-07-29 — не дефект
приложения). Гоняй сеть **по USB** или с выключенными мобильными данными. Разбор и методика —
`bugs/74` и `PROJECT_HISTORY.md`.

### 4. Технические факты, проверенные и НЕ требующие перепроверки

- **Энкодер в CBR** — замерший кадр сам по себе полосу не экономит; `setVideoBitrateOnFly` молча
  выходит при незапущенном энкодере (наш лог = намерение, не факт); `getSentVideoFrames()` растёт ДО
  записи в сокет (доставку им не доказать).
- **ADB по Wi-Fi умирает при гашении экрана** и это выглядит как «приложение замерло» (EXP-0033);
  сетевые прогоны — по USB (`plans/21` §10).
- **Глубина буфера дросселя — параметр эксперимента** (`--queue-ms`, дефолт 1000), а не деталь
  реализации: очередь 8 МБ маскировала сужение канала целую минуту.
- **`git checkout -- <файл>` на документе с ответами Криника уничтожает его работу** (EXP-0030):
  первоисточник владельца коммитится ПЕРВЫМ действием.

### 5. Лёгкое, если нужен быстрый результат

`ideas/45` (подложка меню видна на чёрном — брошено Криником 2026-07-31; класс ВКУСА → мокапы) ·
`bugs/53` (плашка эфира пляшет) · `bugs/52` (толщина рамки слоя) · `bugs/65` (локализация UI по
системной локали) · гигиена смоука (работать на своей сцене, а не в сценах Криника) ·
`/check-backlog` · `bugs/72` (краш в простое — начать с починки FileLogger, он теряет хвост перед крашем).

> ~~Написать `tools/net-chaos.mjs`~~ ✅ **сделано и доказано 2026-07-31**: дроссель работает, есть
> гард `selftest`. Не переписывай его заново.

## Где мы (одним взглядом)

**Фазы 0–4 закрыты** (фундамент → USB UVC превью → RTMP-стрим и дев-харнесс → GL-композитор
«камера = слой» в дефолте → мультиисточники, сцены, переходы). Таблица вех целиком перенесена в
`PROJECT_HISTORY.md` — она дублировала `MASTER_PLAN.md` §7, который и есть источник правды о карте фаз.

| Веха | Статус |
|------|--------|
| **Phase 5 — мультистрим, кодеки, ЖИВУЧЕСТЬ СЕТИ** | 🔥 ТЕКУЩАЯ. Мультистрим и адаптив боевые; живучесть: уровни 0,1 + Ш0 + работы A и B сделаны (`plans/21`); буфер+догон ждут E4 |

Дорожная карта: `MASTER_PLAN.md` (ревизия 2026-07-02 — перевыведен из обновлённого `GOAL.md`
«мобильный OBS»). Полная летопись сессий — `PROJECT_HISTORY.md`.

---

## ❓ Ждёт КРИНИКА

Порядок важен: **вопросы поднимает АГЕНТ страницей** (`node tools/owner.mjs ask …`), а не Криник —
инвариант I8. Переспрашивать в чате канон запрещает.

1. 🆕 **`bugs/53` — вердикт ГЛАЗАМИ:** измеримое доказано (ширина плашки 383 px в 10 пробах),
   осталось перцептивное — битрейт гуляет в последнем знаке (5,1/5,2/5,3). Достаточно или округлять
   до целых Мбит? Артефакт готов: включить эфир, плашка в левом верхнем углу.
2. ⏳ **`interview_018` В1 и В2** — ядро ReStream-сервера (MediaMTX + свой оркестратор или datarhei)
   и где живёт правда о платформах. Без них эпик сервера не начинается.
3. **`ideas/05` (SEO README)** — артефакт ОДОБРЕН, но одобрение привязано к sha256 текущих байтов:
   правка черновика (убрать имена конкурентов, `interview_019` В1) **аннулирует его сама**.
   Порядок: переписать → одобрить новые байты → публиковать.
4. **Выбор 1 из 4 мокапов плашки** (мокапы рисует агент) — до кода работы C.
5. **`ideas/44`** — состав звуковых паттернов виртуальной камеры (отладка догона числами).
6. **UX-решения, когда дойдут руки:** `ideas/20` (выбор источника), иконки приложения и уведомления.
7. **Приёмка глазами** (фикс есть, харнес зелёный): bugs 60, 61, 62, 63, 64, 33 + UI сцен и живость
   уходящей сцены при переходе.

> Закрытое и не требующее возврата: `homeworks/09` (E4 проведён — догон разблокирован) ·
> `interview_014`/`015`/`016`/`017`/`019`–`023` отвечены · `interview_006`, `interview_008` отвечены
> и реализованы. Подробности — `PROJECT_HISTORY.md`.

## 🐛 Открытые баги (срез 2026-08-02, вечер)

**Закрыто вечером 2026-08-02:** **79** (FAB врал «ЭФИР» при записи в файл) — один источник бейджа +
слово Криника «ФАЙЛ», обе локали приняты глазами · **78** (модалка сцены в ландшафте телефона
обрезала чипы) — прокрутка + `FlowRow`; **два близнеца** в `StreamLayersOverlay` закрыты тем же
фиксом и доказаны под искусственной теснотой. Раньше в этот день закрыты **73** (краш FGS из фона) и
**77** (петля переоткрытия UVC в эфире) — подробности в `PROJECT_HISTORY.md`.

**Продвинуто без кода:**
- **76** — контрольный прогон на A51 **НЕ воспроизвёл** баг при ПОЛНОМ наборе симптомов гонки
  (двойное закрытие, `GL already released`, `no current context`). Версия «гонка» подтверждена,
  «device-камера сломана» опровергнута. Окно гонки на A51 в 7 раз шире (1346 мс против 181).
  ⚠️ **Воспроизводить и принимать фикс можно ТОЛЬКО на Titan 1** — на A51 прогон зелёный и БЕЗ фикса.
- **53** — оба пункта плана уже в коде; ширина плашки измерена из DOM: **383 px в 10 пробах подряд**
  при тикающих секундах и битрейте 5,1–5,3. Осталось перцептивное «мерцает ли число» → **вердикт
  Криника** (класс ВКУСА), альтернатива — округление до целых Мбит.
- **65** — ✅ шаг 1 (аудит) выполнен, инвентарь в документе: **к работе 5 строк в 3 файлах**, не
  «многие элементы UI». Блокер не в тексте, а в типе: имена живут `String`-полями модели сцены и
  уходят в ПЕРСИСТ — перевод на `UiText` требует миграции сохранённых сцен Криника.

> Гард на класс «одно решение в двух местах»: `node tools/lint-source.mjs` (ресурсы бейджа допустимы
> только в `ui/SessionBadge.kt`; `selftest` доказывает, что гард умеет краснеть).

## 🤖 Автономный пул (без камеры, стрима и Криника)

Ревизия 2026-08-02. Что можно грайндить в одиночку на A51, виртуалке и полигоне:

- 🆕 **Ландшафт телефона по ОСТАЛЬНОМУ UI** — модалки закрыты (`bugs/78` + два близнеца), но панели,
  оверлеи и экраны настроек в ландшафте на A51 никто не смотрел. Класс уже дал 3 дефекта за час,
  значит он ещё не выработан. Приём для доказательства превентивных фиксов — EXP-0049 (`font_scale`).
- **`bugs/65` Ш2–Ш3** — перевод имён слоёв/источников на `UiText`. Автономно, но начинать с Ш2:
  выяснить, уходят ли эти имена в персист сцен, и как мигрировать БЕЗ потери сцен Криника.
- **`bugs/72`** — начать с починки **FileLogger** (теряет хвост перед крашем): без этого следующий
  краш снова придёт без контекста.
- **`bugs/52`** (толщина рамки слоя) — класс ВКУСА: не вердикт агента, а мокапы Кринику.
- **`plans/11` S1–S2** (USB-мост и дедупы) — автономная часть. Свежая репродукция кластера на A51
  лежит в `bugs/35` (hot-plug при живом приложении не даёт НИ камеры, НИ диалога).
- **Мокапы плашки** для работы C (4 варианта, `tools/graphics`).
- 🆕 **Проверить USB-Ethernet на A51** — адаптер уже висит на хабе; проводной канал управляемее Wi-Fi
  для сетевых прогонов `plans/21`.

> Перед взятием задачи прогони `/check-backlog` — список собирается из файлов без тега `DONE`,
> держать его в голове не нужно.

## Ключевые константы проекта

- **ПАРК УСТРОЙСТВ (с 2026-08-02 их ДВА — всегда указывай, на каком гоняешь):**

  | Роль | Устройство | Serial | ОС | Чип / кодеки |
  |---|---|---|---|---|
  | Потолок | **Headwolf Titan 1** | `DHF8256GB25442874` | Android 16 / SDK 36 | Dimensity 8300, Codec2 `c2.mtk.*` |
  | **Пол (= наш `minSdk`)** | **Samsung Galaxy A51** (`SM-A515F`) | `R58N615KXZZ` | Android 13 / **SDK 33** | Exynos 9611, OMX `OMX.Exynos.*`, без AV1 |

  Titan 1 — ADB WiFi, IP динамический (переподнятие через USB: `adb tcpip 5555` →
  `adb connect <ip>:5555`). A51 — по USB-кабелю.
  🔴 **При двух подключённых устройствах ВСЕГДА задавай `ADB_DEVICE=<serial>`** для `ui.mjs`/`smoke.mjs`
  — иначе тест уедет не на тот девайс (гард добавлен, см. ниже).
- **Экран «не гаснет никогда» — ставится ADB, в UI такого нет** (в настройках потолок 10 минут):
  `settings put global stay_on_while_plugged_in 7` (не гаснет, пока есть питание — это и есть
  «бесконечность») + `settings put system screen_off_timeout 2147483647`. Проверять не возвратом
  настройки, а фактом: `dumpsys power | grep mStayOn` → **`mStayOn=true`**. Применено на A51 2026-08-02.
- **Камера-тест:** Разные. Иногда UVC Emeet Piko+ 4K, иногда noname 2K USB Camera, иногда виртуальная камера - больше это не играет значимой роли, так как реализуем слои, и слоем может быть источник видео - в нём источником видеопотока может быть любая камера, хоть виртуалка, хоть UVC, хоть одна из встроенных камер устройства - настройка источника. Тестировать нужно на разных в зависимости от задачи.
- **Сборка:** `JAVA_HOME=JBR` (`/Applications/Android Studio.app/Contents/jbr/Contents/Home`)
  + `node tools/build.mjs --no-ui`. ⚠️ ЭТОГО МАЛО: проект компилится тулчейном 17, а JBR уехал на 21 —
  нужен ОТДЕЛЬНЫЙ JDK 17 в `~/Library/Java/JavaVirtualMachines/` (EXP-0020).
- **Git:** только `main`, коммит+пуш = `node tools/commit.mjs "..."`.
- Решения Криника по продукту — `MASTER_PLAN.md` §10 и летопись (`PROJECT_HISTORY.md`, секция
  «Важные решения»).
