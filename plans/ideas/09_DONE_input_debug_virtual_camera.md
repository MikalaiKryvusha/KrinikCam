> ✅ РЕАЛИЗОВАНО 2026-06-29 — тумблер «Виртуальная камера» в dev-меню (Idea 07). Включает софтверный
> `VirtualVideoSource` (:feature:streaming, как `StandbyVideoSource`): рисует синтетический 16:9
> тест-паттерн в GL SurfaceTexture вместо физ. USB-камеры — круг (детектор искажений: должен
> остаться кругом), сетка, угловые маркеры, «▲ TOP», «16:9», **движущаяся** полоса-свип + живой
> счётчик кадров/время. Через `DeviceManager` источник → `VideoSource.Virtual` (приоритет: реальная
> UVC > Virtual > телефон > None). Проверено на устройстве БЕЗ камеры: паттерн рисуется (круг круглый,
> леттербокс корректный), меню поворота доступно; логи `VirtualVideoSource started 1920x1080 @15fps`.
> Файлы: `VirtualFrameRenderer.kt`, `VirtualVideoSource.kt`, `VideoSource.Virtual`,
> `DeviceManager.setVirtualCamera`, `DevSettings`, `DevMenuScreen`, `NavGraph`, `MainActivity`, `MainScreen`.
> Снимает блокер «нет камеры» для автономной отладки видеопайплайна (вкл. поворот / Bug 10).

---

Нужно для отладки приложения иметь возможность иммитировать видео поток виртуально с заглушки - словно подключена USB веб камеоа с неё идёт пейзажная картинка в 4К 16 на 9 соотношение сторон - на самом деле камера не подключена физически, а это виртуальный источник видео, который нужен для того, чтобы приложение почти полноценно работало, и можно было бы дебажить его механизмы работы с входящим потоком видео, управление им и отправка его в стрим каналы.