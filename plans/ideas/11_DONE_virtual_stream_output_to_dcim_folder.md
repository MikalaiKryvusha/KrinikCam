> ✅ РЕАЛИЗОВАНО 2026-06-29 — после остановки записи файл копируется в публичную `DCIM/KrinikCam` через
> MediaStore (scoped storage, minSdk 33 — прямой путь к public dirs нельзя). Виден в галерее/Files.
> Реализация: `RtmpStreamer.publishRecordingToDcim()` + публикация в `recordListener` на статусе STOPPED.
> Проверено харнессом: `DCIM/KrinikCam/krinikcam_rec_*.mp4` появляется. Это и общий пайплайн под будущую
> фичу «сохранение видео/фото в галерею». Приватная копия в `files/rec/` остаётся для adb-харнесса.

Перенаправить запись с "виртуального стрима" из приватной директории приложения в публичую директорию DCIM/KrinikCam, чтобы Криник мог их видеть и анализировать.

(Этот пейплайн всё равно нужно будет делать, когда будет реализовываться фича видео записи и фото снимков в файлы - можно начать уже сейчас делать общий пейплайн под это)