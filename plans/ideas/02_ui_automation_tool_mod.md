> ✅ РЕАЛИЗОВАНО 2026-06-28 — `ui.mjs allow` (одобрение системных диалогов CAMERA/микрофон/USB
> по resource-id, многоязычно), `ui.mjs kill/start/restart` (освобождение камеры между dev/release),
> `ui.mjs screen` + `adb.mjs screen` (скриншот в сжатый JPEG q80 через `sharp`). Проверено на
> устройстве: одобрил русский CAMERA-диалог автономно. Док: `tools/UI_AUTOMATION_GUIDE.md`.

Фича:
- Расширить возможности инструмента ui.mjs так, чтобы с его помощью ИИ агент по ADB мог определять системный диалог с запросом доступа к подключенной веб камере, и чтобы мог нажать кнопку одобрения в этом системном Андроид диалоге - чтобы ИИ агент сам это мог выполнить без необходимости звать на помощь Криника. Также агент должен быть способным видеть системные диалоги на пермиссии к камере, микрофону - и тоже быть способным их одобрять.

- Также нужно уметь убивать приложение и перезапускать, чтобы освобождать камеру между дев и релиз приложениями.