# KRINIKS — Генеральный план проекта

> **Автор:** Mikalai Kryvusha aka KOT KRINIK (Николай Кривуша, Кот Криник)
> **Тип проекта:** OpenSource, vibe-code
> **GitHub:** https://github.com/MikalaiKryvusha/KrinikCam
> **Дата создания плана:** 2026-06-27

---

## 1. Название и брендинг

**Полное название:** KrinikCam

**Варианты аббревиатуры / торгового имени** (выбрать одно):

| Вариант       | Расшифровка                           | Плюсы                                       |
|---------------|---------------------------------------|---------------------------------------------|
| **KRINIKS**   | уже используется как префикс          | уже консистентно с repo, папками            |
| **KrinikCam** | Krinik's Camera                       | коротко, запоминаемо, легко гуглится        |
| **KAWS**      | Krinik Android Webcam Streamer        | аббревиатура из 4 букв, техничный стиль     |

> Рекомендую: **KrinikCam** как название в UI и в README, **KRINIKS** как технический идентификатор (пакет, папки).
> Финальное решение остаётся за автором.

**Package name:** `com.kriniks.kcam`
**App label:** `KrinikCam`
**Short name / abbreviation:** `KCam`

---

## 2. Принципы разработки

_(подробно описаны в [goal.md](goal.md), здесь — краткое операциональное резюме)_

| Принцип         | Операциональный смысл                                                                                     |
|-----------------|-----------------------------------------------------------------------------------------------------------|
| OpenSource      | Только библиотеки с MIT / Apache 2.0 / LGPL / GPL — без проприетарных SDK                               |
| Умность         | Приложение само сканирует устройство и камеру, сохраняет оптимальный конфиг                               |
| Модульность     | Multi-module Gradle проект; каждый модуль — независимый и заменяемый                                     |
| Документированность | Каждая фича — сначала план в `/plans`, потом реализация                                               |
| Автоматизация   | Рутинные операции (build, commit, release) — Node.js инструменты в `/tools`                              |
| Доступность     | 10 языков; RU и EN — приоритет по качеству перевода                                                      |
| Версионирование | SemVer `x.y` (major.minor), версия кода в скобках                                                        |

---

## 3. Технологический стек

### 3.1 Платформа

| Параметр         | Значение                      |
|------------------|-------------------------------|
| Язык             | Kotlin                        |
| Min SDK          | API 33 (Android 13)           |
| Target SDK       | API 35 (Android 15)           |
| ABI              | arm64-v8a, x86_64 (64-bit only)|
| UI Toolkit       | Jetpack Compose + Material 3  |
| Архитектура      | Clean Architecture + MVVM     |
| Async            | Kotlin Coroutines + Flow      |
| DI               | Hilt                          |
| Navigation       | Compose Navigation            |
| Persistence      | Room (profiles/settings) + DataStore (prefs) |
| Build System     | Gradle (Kotlin DSL)           |

### 3.2 Ключевые OpenSource библиотеки

#### USB / UVC

| Библиотека                     | Назначение                                                   | Лицензия   |
|--------------------------------|--------------------------------------------------------------|------------|
| `jiangdongguo/AndroidUSBCamera`| UVC camera driver для Android, поддержка USB-OTG            | Apache 2.0 |

> Альтернатива: `saki4510t/UVCCamera` (LGPL). `jiangdongguo/AndroidUSBCamera` — активно поддерживаемый форк с Kotlin API.

#### Стриминг

| Библиотека                  | Назначение                                               | Лицензия   |
|-----------------------------|----------------------------------------------------------|------------|
| `pedroSG94/RootEncoder`     | RTMP/RTMPS/SRT/RTSP стриминг, аппаратные кодеки, мультипоток | Apache 2.0 |

> RootEncoder поддерживает параллельный вывод на несколько серверов, аппаратный H.264/HEVC/AV1 через MediaCodec, SRT через libsrt.

#### Видеообработка

| Библиотека              | Назначение                                           | Лицензия   |
|-------------------------|------------------------------------------------------|------------|
| `libyuv` (via JNI/AAR)  | Конвертация форматов YUV, масштабирование            | BSD        |
| `GPUImage-for-Android`  | GPU-акселерированные фильтры изображения             | Apache 2.0 |
| `OpenCV Android`        | Анализ кадра для авторегулировки (яркость, гистограмма) | Apache 2.0 |

#### Аудиообработка

| Библиотека                    | Назначение                                              | Лицензия   |
|-------------------------------|---------------------------------------------------------|------------|
| `Oboe` (Google)               | Низкозадержкнный аудио I/O                             | Apache 2.0 |
| WebRTC Audio Processing (AAR) | Шумоподавление, AGC, AEC                               | BSD        |

#### Утилиты

| Библиотека                   | Назначение                                         | Лицензия   |
|------------------------------|----------------------------------------------------|------------|
| Coil                         | Загрузка изображений (иконки камер, превью)        | Apache 2.0 |
| Timber                       | Логирование                                        | Apache 2.0 |
| kotlinx.serialization        | Сериализация профилей (JSON)                       | Apache 2.0 |

---

## 4. Архитектура приложения

### 4.1 Схема потока данных (Data Flow)

```
[ USB Webcam ]  [ Internal Camera ]  [ Audio Sources ]
      │                  │                   │
      ▼                  ▼                   ▼
┌─────────────────────────────────────────────────┐
│              Source Manager                     │
│  (горячее подключение, инициализация, профили)  │
└─────────────────────┬───────────────────────────┘
                      │  Raw frames (YUV/MJPG/H264)
                      ▼
┌─────────────────────────────────────────────────┐
│           Video Processing Pipeline             │
│  AutoRegulation → Crop/Frame → Filters → Mix   │
└──────────┬──────────────────────────────────────┘
           │  Processed frames
           ▼
┌──────────────────────────────────────────────────┐
│              Codec Manager                       │
│  Сканирует доступные HW MediaCodec блоки         │
│                                                  │
│  ┌───────────────┐    ┌───────────────────┐     │
│  │  Encoder #1   │    │    Encoder #2     │     │
│  │  H.264 (HW)   │    │   HEVC (HW)      │     │
│  └───────┬───────┘    └────────┬──────────┘     │
└──────────┼─────────────────────┼────────────────┘
           │                     │
           ▼                     ▼
┌──────────────────┐   ┌──────────────────────────┐
│  RTMP → YouTube  │   │  RTMP/SRT → Instagram    │
└──────────────────┘   └──────────────────────────┘
           │                     │
           ▼                     ▼
┌─────────────────────────────────────────────────┐
│             Streaming Monitor                   │
│  Битрейт, дроп кадров, пинг, состояние потоков  │
└─────────────────────────────────────────────────┘
```

### 4.2 Структура модулей (Gradle Multi-Module)

```
KRINIKS/
├── app/                          # :app — точка входа, DI граф, MainActivity
│
├── core/
│   ├── common/                   # :core:common — базовые модели, utils, константы
│   └── ui/                       # :core:ui — Design System, тема, Material3, shared composables
│
├── feature/
│   ├── usb/                      # :feature:usb — USB детекция, UVC драйвер, hot-plug
│   ├── capture/                  # :feature:capture — захват видео/аудио, Source Manager
│   ├── codec/                    # :feature:codec — HW кодек сканер, MediaCodec обёртки
│   ├── streaming/                # :feature:streaming — RTMP/SRT клиенты, мультипоток
│   ├── recording/                # :feature:recording — запись в файл, MediaMuxer
│   ├── processing/               # :feature:processing — фильтры, GPU обработка, аудио DSP
│   ├── autoregulation/           # :feature:autoregulation — умная авторегулировка камеры
│   ├── overlay/                  # :feature:overlay — on-screen контролы, рамки кропа
│   ├── monitoring/               # :feature:monitoring — live статистика, HUD overlay
│   ├── profiles/                 # :feature:profiles — профили устройств и стриминга
│   └── settings/                 # :feature:settings — настройки приложения, язык
│
└── data/
    ├── profiles/                 # :data:profiles — Room DB для профилей
    └── devices/                  # :data:devices — хранилище возможностей устройства
```

---

## 5. Описание модулей

### :feature:usb
- Обнаружение подключённых USB-устройств через Android UsbManager
- Запрос разрешения на доступ к USB
- Инициализация UVC камеры через AndroidUSBCamera
- Hot-plug: мониторинг подключения/отключения, автоматический запуск сканирования
- Выдача событий через Kotlin Flow: `UsbDeviceEvent.Connected`, `.Disconnected`, `.PermissionGranted`

### :feature:capture
- Source Manager: реестр всех активных источников (USB камеры, встроенная камера, микрофоны)
- Получение raw фреймов от UVC камеры и передача в Processing Pipeline
- Поддержка нескольких источников одновременно
- Интерфейс `VideoSource` и `AudioSource` для унификации

### :feature:codec
- Сканирование доступных аппаратных кодеков через `MediaCodecList`
- Определение поддерживаемых форматов, разрешений, FPS, битрейтов
- `CodecProfile` — набор параметров для одного кодека (кодек, разрешение, FPS, битрейт, GOP)
- Предупреждение при выборе CPU-кодирования (высокая нагрузка)
- Управление параллельными экземплярами `MediaCodec`

### :feature:streaming
- Абстракция `StreamingTarget` (RTMP / RTMPS / SRT / RTSP)
- `PlatformProfile` — профиль платформы (URL, ключ стрима, кодек, разрешение, протокол)
- `MultiStreamManager` — запуск N потоков одновременно, каждый на свой таргет
- Встроенные шаблоны: YouTube Live, Instagram Live, TikTok Live, Twitch, Kick, Custom RTMP
- Быстрое редактирование stream key без пересоздания профиля

### :feature:autoregulation
- Анализ кадра (гистограмма яркости, перео/недоэкспонирование) через OpenCV или встроенные средства
- Регулировочные профили: Balanced, Fast, DarkPriority, LightPriority, Manual
- Обратная связь с камерой: отправка UVC control-запросов (яркость, экспозиция, баланс белого, насыщенность)
- Работает в отдельном coroutine с настраиваемым интервалом (по умолчанию 500 мс)

### :feature:processing
- Видео: кроп по рамке платформы, масштабирование, наложение sources (PiP)
- GPU-фильтры: яркость, контрастность, насыщенность, температура, чёткость (GPUImage)
- Аудио: шумоподавление, AGC, эхоподавление, микширование нескольких источников, уровни каналов
- Фоновые медиа: воспроизведение MP3-файла или URL-потока как источник аудио

### :feature:overlay
- Плавающие on-screen кнопки и меню (Jetpack Compose Popup / Canvas)
- Быстрые регулировки: яркость, контраст (смещение поверх авторегулировки)
- Рамки кропа для каждой платформы (разные цвета), перетаскиваемые жестом
- Реакции: анимации сердечек, лайков, конфети (GIF/Lottie)
- Наклейки и видеооверлеи (MP4 с прозрачным фоном)

### :feature:monitoring
- HUD overlay (настраиваемый, сворачиваемый): битрейт по каждому потоку, дроп кадров, CPU/GPU, заряд, WiFi/LTE сигнал
- VU-метры аудиоканалов
- История событий (отключение камеры, потеря сети и т.д.)

### :feature:profiles
- `DeviceProfile` — конфиг под конкретное устройство (HW кодеки, USB hub, оптимальные настройки)
- `StreamProfile` — конфиг стрима (платформа, кодек, разрешение, FPS, битрейт)
- Экспорт/импорт профилей в JSON-файл
- Библиотека готовых профилей для популярных устройств

### :feature:settings
- Переключение языка (RU, EN, ES, PT, ZH, HI, BN, FR, DE, AR) в 2 тапа
- Проверка обновлений библиотек (GitHub Releases API, фоновый WorkManager)
- Ссылки на GitHub, README.pdf, README.md (открываемые тапом)
- Тема (светлая / тёмная / системная)

---

## 6. Экраны приложения (высокоуровневый UI map)

```
MainActivity
└── NavHost
    ├── MainScreen (главный экран — видео во весь экран)
    │   ├── CameraPreview (полноэкранный)
    │   ├── FloatingOverlay (on-screen контролы, рамки)
    │   ├── MonitoringHUD (нижняя полоска статистики)
    │   └── QuickPanel (боковая шторка — быстрые настройки)
    │
    ├── DeviceManagerScreen (менеджер источников)
    │   └── USB / внутренняя камера / микрофоны / медиа
    │
    ├── StreamTargetsScreen (платформы стриминга)
    │   └── Добавить / редактировать профиль платформы
    │
    ├── ProfilesScreen (профили устройства и стрима)
    │
    └── SettingsScreen
        ├── GeneralSettings
        ├── CodecSettings
        ├── AutoregulationSettings
        └── About (автор, версия, ссылки)
```

---

## 7. Дорожная карта (фазы)

### Phase 0 — Фундамент
> Цель: проект компилируется, CI работает, структура на месте

- [ ] Multi-module Gradle проект (Kotlin DSL)
- [ ] Подключить Hilt, Compose, Material3, Navigation
- [ ] Базовая тема и Design System в `:core:ui`
- [ ] GitHub Actions: сборка APK на каждый push
- [ ] Node.js инструменты: `tools/build.mjs`, `tools/commit.mjs`, `tools/release.mjs`
- [ ] Базовая структура README.md (заголовок, бейджи, скриншоты-placeholder)

**Milestone:** `Hello World` APK публикуется как GitHub Release 0.1

---

### Phase 1 — MVP: камера работает
> Цель: пользователь подключил USB камеру и видит превью

- [ ] `:feature:usb` — UVC детекция, запрос разрешения, превью
- [ ] `MainScreen` — полноэкранный превью
- [ ] `:feature:codec` — базовый сканер кодеков
- [ ] `:feature:streaming` — один RTMP поток (YouTube)
- [ ] `:data:profiles` — Room DB, сохранение одного профиля
- [ ] `:feature:profiles` — автодетект устройства, сохранение DeviceProfile
- [ ] Базовый `SettingsScreen`

**Milestone:** `0.2` — стрим на YouTube с USB камеры работает

---

### Phase 2 — Мультистриминг и кодеки
> Цель: стрим на несколько платформ одновременно, параллельные кодеки

- [ ] `:feature:streaming` — MultiStreamManager, SRT, RTMPS
- [ ] Шаблоны платформ: YouTube, Instagram, TikTok, Twitch, Kick, Custom
- [ ] `:feature:codec` — параллельное кодирование (N кодеков одновременно)
- [ ] UI: `StreamTargetsScreen`, быстрое редактирование stream key
- [ ] Предупреждение при CPU-кодировании

**Milestone:** `0.3` — YouTube (H.264) + Instagram (HEVC) одновременно

---

### Phase 3 — Умная авторегулировка
> Цель: качество изображения поддерживается автоматически

- [ ] `:feature:autoregulation` — анализ кадра, UVC control feedback
- [ ] Профили регулировки: Balanced, Fast, DarkPriority, LightPriority, Manual
- [ ] UI настройки авторегулировки в `SettingsScreen`
- [ ] Инфо-кнопки `[i]` у каждого параметра настроек

**Milestone:** `0.4`

---

### Phase 4 — Мультиисточники и продвинутый UI
> Цель: несколько камер/микрофонов, PiP, on-screen контролы

- [ ] `:feature:capture` — Source Manager, несколько USB камер + внутренняя камера
- [ ] `:feature:processing` — PiP compositor, GPU-фильтры
- [ ] `:feature:overlay` — рамки кропа (draggable), плавающие кнопки
- [ ] `DeviceManagerScreen`
- [ ] Аудио: микширование нескольких источников, VU-метры

**Milestone:** `0.5`

---

### Phase 5 — Мониторинг и фоновый режим
> Цель: пользователь может свернуть приложение, не прерывая стрим

- [ ] `:feature:monitoring` — HUD overlay
- [ ] Android Foreground Service для фонового стриминга
- [ ] Android PiP (picture-in-picture) режим
- [ ] Persistent notification controller

**Milestone:** `0.6`

---

### Phase 6 — Контент-инструменты
> Цель: инструменты для блогеров поверх стрима

- [ ] Фоновое аудио (MP3 файл, URL-поток)
- [ ] Видео-оверлеи (MP4 с alpha / GIF / Lottie)
- [ ] Реакции: анимации лайков, конфети, сердечек
- [ ] Запись в файл (одновременно со стримингом)

**Milestone:** `0.7`

---

### Phase 7 — Локализация и документация
> Цель: приложение говорит на 10 языках, документация блестит

- [ ] Все строки в `strings.xml`, перевод: RU (base), EN, ES, PT, ZH, HI, BN, FR, DE, AR
- [ ] README.md: красивый, двуязычный (EN + RU), скриншоты, GIF демо
- [ ] README.pdf (генерация Node.js инструментом)
- [ ] Obtainium-совместимый манифест
- [ ] Финальный README.pdf в GitHub Releases

**Milestone:** `1.0 Release Candidate`

---

### Phase 8 — Updater и поддержка
> Цель: приложение само предлагает обновить свои библиотеки

- [ ] WorkManager task: проверка новых версий зависимостей на GitHub (раз в неделю)
- [ ] Уведомление "Доступно обновление библиотеки X"
- [ ] UI диалог обновления

**Milestone:** `1.1`

---

## 8. Инфраструктура и автоматизация

### 8.1 Node.js инструменты (`/tools`)

| Файл                   | Назначение                                                                          |
|------------------------|-------------------------------------------------------------------------------------|
| `tools/build.mjs`      | Сборка APK/AAB через Gradle; параметры: `--release`, `--debug`, `--abi`            |
| `tools/commit.mjs`     | git add + commit с автоповышением версии кода + push                               |
| `tools/release.mjs`    | Сборка Release APK + создание GitHub Release с Release Notes + загрузка артефактов |
| `tools/version.mjs`    | Чтение/запись текущей версии (SemVer) из `version.json`                            |
| `tools/translate.mjs`  | Утилита для удобного добавления/проверки переводов                                 |

### 8.2 GitHub Actions (`.github/workflows`)

| Файл                       | Триггер             | Действие                                             |
|----------------------------|---------------------|------------------------------------------------------|
| `build.yml`                | push / PR           | Компиляция + lint + unit tests                       |
| `release.yml`              | push tag `v*`       | Release build + загрузка APK в GitHub Releases       |

### 8.3 Версионирование

- Формат: `X.Y (build NNN)` → например, `1.0 (42)`
- `version.json` — единый источник правды:
  ```json
  { "major": 1, "minor": 0, "build": 42 }
  ```
- `tools/commit.mjs` — повышает `build` на каждый коммит
- `tools/release.mjs` — повышает `minor` (или `major` при флаге `--major`), сбрасывает `build`

---

## 9. Документация

### README.md структура

1. Логотип + бейджи (CI, License, Platform, Language)
2. Краткое описание (EN)
3. Скриншоты / GIF демо
4. Список фич (кратко, с иконками)
5. Установка (APK direct + Obtainium)
6. Быстрый старт (5 шагов)
7. Требования (Android 8.0+, OTG cable, USB webcam)
8. Конфигурация и профили (с примерами)
9. PRO советы (для продвинутых)
10. Поддерживаемые устройства (таблица)
11. Лицензия (MIT или Apache 2.0 — уточнить)
12. Автор + ссылки на соц.сети
13. --- разделитель ---
14. То же самое на русском языке (перевод)

### Ссылки внутри приложения (экран About)

- GitHub репозиторий
- README.md (открыть в браузере)
- README.pdf (открыть локальным просмотрщиком)
- Instagram, YouTube автора

---

## 10. Открытые вопросы (требуют решения до реализации)

| # | Вопрос                                                  | Статус    | Решение                                                  |
|---|---------------------------------------------------------|-----------|----------------------------------------------------------|
| 1 | Название / аббревиатура                                 | ✅ Решено | **KrinikCam** / **KCam**                                 |
| 2 | Лицензия                                                | ✅ Решено | **MIT**                                                  |
| 3 | Min SDK                                                 | ✅ Решено | **API 33 (Android 13)** — только современное железо      |
| 4 | Протоколы стриминга                                     | ✅ Решено | **RTMP/RTMPS + RTSP**. SRT — v2+ опция                  |
| 5 | Иконка/логотип                                          | ✅ Решено | Временная: кислый розовый, камера + KCam. OpenSource.    |
| 6 | Целевые платформы (приоритет)                           | ✅ Решено | YouTube → Instagram → Twitch → TikTok                    |
| 7 | Основное тест-устройство                                | ✅ Решено | Headwolf Titan1 (Dimensity 8300) + Samsung S21 FE        |
| 8 | USB камера для тестов                                   | ✅ Решено | Emeet Piko+ 4K с автофокусом                             |

---

## 11. Процесс принятия решений — Интервью с Криником

Перед каждой значимой фазой или при появлении открытых вопросов проводится **интервью с автором проекта**.

Интервью фиксируются в `/interviews/interview_NNN_тема.md`.

**Правило:** не начинать реализацию фичи, пока открытые вопросы по ней не закрыты через интервью.

Текущие интервью:
- [interview_001_vision_and_foundation.md](../interviews/interview_001_vision_and_foundation.md) — Vision, название, лицензия, Min SDK, протоколы, устройства

---

## 12. Следующий шаг

> **Сначала:** закрыть открытые вопросы через [интервью #001](../interviews/interview_001_vision_and_foundation.md).
> **Затем:** [phase_0_foundation.md](phase_0_foundation.md) — скелет Android проекта _(создаётся после интервью)_.

## 13. Стиль кода

Код комментируем на английском языке, желательно почаще, важные модули, блоки - хоть чуть-чуть, хоть немного (где требуется - много) пишем комментации, что код делает и зачем, для сего, с какими файлами и блоками он связан - нужен для прозрачности, отслеживаемости, поддерживаемости в будущем.