# План 22 — интерактивный контур согласований «агент ↔ Криник» (`/owner-reviews`)

> ➡️ **ПРОДОЛЖЕНИЕ: `plans/23_owner_reviews_contour_rewrite.md`** (2026-08-01) — контур переписан по
> полевому отчёту соседнего проекта (`researches/28`): добавлен инвариант **I8 «сохранение будит
> ждущего»**, базовая линия долга у гарда, детектор протухшего статуса, счётная проверка вариантов,
> QA живым браузером. Инварианты I1–I7 этого плана остаются в силе; здесь ничего не отменено.

**Статус:** ✅ КОНТУР РАБОТАЕТ — пилот пройден вживую 2026-07-31 (Криник ответил на 3 вопроса
`interview_015` со страницы; гейт открылся; висящее интервью ушло из гарда) · **Заведён:** 2026-07-31 ·
**Тип:** обычная задача (`/plan-task`)
**Источник:** прямая просьба Криника в чате 2026-07-31: *«давай по KAIF выполним процедуру перевода
запросов ИИ агента к владельцу в форму открытия интерактивных HTML страниц со звуком. И чтобы ии
агент получал ответы оттуда, и продолжал работать, получив ответы. Сейчас важные вопросы, на которые
ИИ агент хотел получить от владельца ответы, могут теряться в md документах по всему проекту.»*
**Методология:** навык `/owner-reviews` (KAIF 2.1) — он задаёт ИНВАРИАНТЫ и имена; инструменты пишет
агент проекта. Полигон обучения — живое `interviews/interview_015_bitrate_after_network_drop.md`.

---

## Тест тяжести (почему один операционный план, а не лестница эпика)

| Признак тяжести | Наш случай |
|---|---|
| Трогает ≥3 подсистемы или канон-документа | ✅ да (`tools/`, `AGENT_GUIDE.md`, `STATUS.md`, ритуалы навыков) |
| Опирается на внешнюю правду / отраслевой стандарт | ❌ нет — методология уже дистиллирована в навыке `/owner-reviews`, он и есть разведдок |
| Не влезает в одну сессию | ❌ нет |
| Меняет поставляемый состав или публичные контракты | ❌ нет — dev-обвязка, в APK не попадает |
| Требует решений уровня Криника | ❌ нет — голос/звук/тихие часы это ПАРАМЕТРЫ с дефолтами |

**1 признак из 5 → задача обычная → один операционный план.** Церемония не должна весить больше работы
(`AGENT_GUIDE.md` → «Дисциплина планирования»).

---

## Цель

Вопрос агента к Кринику перестаёт быть строкой в md, которую нужно найти. Он становится **страницей,
которая сама зовёт голосом и звуком**, принимает ответ в один клик и возвращает его в проект в
машинно-проверяемом виде — так, что дальнейшая работа, зависящая от этого ответа, **не может
стартовать без него**.

Главный урок поля, который здесь важнее HTML: **HTML — это транспорт, а цель — ГАРД.** Правило «место
вопросов — только `interviews/`» нарушают даже агенты, которые его знают (в чате дешевле в моменте),
поэтому первым делается не красивая страница, а механическая проверка «нет висящих вопросов».

## Критерии готовности (проверка наблюдением)

- [x] **К1.** `node tools/owner.mjs guard` печатает висящие вопросы и выходит `1`; при чистом беклоге —
      пусто и `0`. Гард **проверен на сломанной версии** (`selftest`), включая ловушку подстроки
      (`ЗАКРЫТ` внутри слова «за**крыт**ие» не должен считаться статусом).
      ✅ *Наблюдение: до ответа гард печатал `interview_015`, после ответа Криника — не печатает.*
- [x] **К2.** `node tools/owner.mjs render <любое из 15 интервью>` строит самодостаточную HTML-страницу
      без единой внешней загрузки; прогон по ВСЕМ 15 живым интервью — без падений.
      ✅ *15/15, падений 0; самодостаточность проверяется в `selftest`.*
- [x] **К3.** Ответ, отправленный со страницы, оказывается в ТРЁХ местах: исходный md · файл решения
      рядом · копия в архиве с `by` и `at`.
      ✅ *Пилот: md + `decisions/interview_015…decision.json` + `archive/…--2026-07-31T16-37-32-03-00.json`.*
- [x] **К4.** `node tools/owner.mjs gate <док>` выходит **не-нулём**, когда ответа нет, ответ отклонён
      или текст вопроса изменился после ответа (sha тела разъехался). Любое сомнение = отказ.
      ✅ *Четыре причины отказа проверены в `selftest`; на живом документе после ответа — exit 0 по В1/В2/В3.*
- [ ] **К5.** Звук и голос слышны Кринику из другой комнаты; **факт доставки подтверждает Криник
      словами**, а не код возврата процесса.
      ⏳ *Сигнал отработал (звук + голос Milena), но Криник был У КОМПЬЮТЕРА — проверка «из другой
      комнаты» ещё не состоялась. Exit 0 доказательством не считаем.*
- [x] **К6.** Тихие часы работают через полночь (23:00–09:00): проверено на обеих сторонах полуночи.
      ✅ *`selftest`: 23:30 и 08:00 — тихо; 12:00 и 22:59 — громко; обычное окно 09:00–23:00 тоже.*
- [x] **К7.** Пилот на `interviews/interview_015`: Криник отвечает на 3 вопроса со страницы, агент
      продолжает работу по `bugs/75`, **не задав ни одного уточняющего вопроса в чате**.
      ✅ *По СОДЕРЖАНИЮ интервью — ни одного уточнения в чате: ответы пришли машинно-читаемыми и
      гейт открылся. Честная оговорка: правки в чате были, но про UX САМОЙ СТРАНИЦЫ (снятие выбора,
      частичный ответ, лишнее поле «кто отвечает») — это доводка инструмента, а не переспрашивание
      вопросов.*

### Что ответил Криник (пилот, 2026-07-31, со страницы)

| Вопрос | Ответ | Совпал с рекомендацией агента? |
|---|---|---|
| **В1** — на каком битрейте возобновлять эфир после сетевого обрыва | **(а)** с пола профиля (250 кбит/с), дальше поднимать как обычно | да |
| **В2** — приводить ли восстановление к форме снижения | **(в)** от текущего (+20% от current) **И пауза** после каждого обрыва | нет — Криник выбрал строже: агент рекомендовал (б), без паузы |
| **В3** — что показывать зрителю, пока эфир скачет | **(в)** не трогать: петля чинится по В1, симптом исчезнет сам | да |

> **Критерий приёмки — это К7.** Не «страница открылась», а «владелец ответил, и агенту не пришлось
> переспрашивать» (канон навыка `/owner-reviews`).

---

## Инварианты (нормативные — контур без них разваливается)

| # | Инвариант | Как держим |
|---|---|---|
| **I1** | md — источник, HTML — производное. **Всегда** | страница строится из документа при каждом вызове; руками её не правим |
| **I2** | Ответ пишется в ТРИ места | `interviews/<док>.md` · `interviews/decisions/<док>.decision.json` · `interviews/decisions/archive/<док>--<ISO>.json` (имя решения ВЫВЕДЕНО из имени документа — общий файл затрёт следующее интервью) |
| **I3** | Одобрение привязано к **sha256 ТЕЛА**, а не к клику | одна функция нормализации `normalizeBody()` на обе стороны; текст изменился после ответа = ответ недействителен |
| **I4** | Гейт стоит на стороне ПРИМЕНЕНИЯ, fail-closed | `gate` отказывает не-нулём, если решения нет, оно `rejected` или sha разъехался. Ответ **никогда** не самоодобряется по таймауту |
| **I5** | Сигнал идёт ПОСЛЕ успешно открытой страницы | зовём Криника из рендерера, когда сервер уже слушает и браузер открыт — иначе класс «позвал, а показать нечего» |
| **I6** | Тихие часы важнее всего, включая явно запрошенный уровень голоса | окно **пересекает полночь** — наивное `from <= now <= to` молчит весь день и орёт всю ночь; у этого сравнения свой гард в `selftest` |
| **I7** | Автономные циклы **копят, а не блокируются** | `queue` паркует документ, `inbox` зовёт ОДИН раз на пачку карточек |

**Ноль внешних зависимостей** — соблазн взять генератор статики велик, выигрыш нулевой. Только stdlib
Node + системные утилиты macOS (`say`, `afplay`, `osascript`, `open`).

---

## Порядок работ (полевая коррекция: «наш порядок был хуже»)

- [ ] **Ш1. Гард места вопросов** — ни от чего не зависит, окупается сразу, показывает реальный масштаб.
- [ ] **Ш2. Рендер + запись решения** — ядро; здесь же живёт контракт метаданных.
- [ ] **Ш3. Гейт на стороне применения** — делает одобрение механическим; без него страница — декорация.
- [ ] **Ш4. Сигнализация** — бесполезна, пока нечего показывать.
- [ ] **Ш5. Накопление для автономных циклов** — нужно ровно тогда, когда практика входит в лупы.
- [ ] **Ш6. Пилот на РЕАЛЬНЫХ данных** — единственное, что ловит дефекты стыков.

---

## Инструмент: `tools/owner.mjs`

| Команда | Что делает |
|---|---|
| `guard` | ищет висящие вопросы: неотвеченные интервью + вопросы-сироты ВНЕ `interviews/`. Exit 1 при находках |
| `render <док>` | md → самодостаточный HTML в `tools/owner-pages/` (gitignored) |
| `ask <док>` | `render` + локальный сервер (жить секунды: поднялся → записал → умер) + браузер + сигнал; ждёт ответ |
| `gate <док> [--q В1]` | fail-closed проверка: ответ есть, не отклонён, sha тела совпал |
| `queue <док>` | припарковать документ для автономного цикла (не блокируя цикл) |
| `inbox` | одна страница «накопилось N» с карточками, один зов на пачку |
| `selftest` | доказывает, что каждый гард СПОСОБЕН покраснеть (канон: гард, который ни разу не краснел, ничего не доказывает) |

### Разбор живых документов (а не выдуманного формата)

Формат интервью в проекте **разнородный** — это факт, снятый с 15 живых файлов, а не предположение:

- заголовки вопросов: `## В1.` · `### Q1:` · `### Q1.` · `## Q1.` · `### Q1 — ` · и документы вообще без
  заголовков вопросов (001, 006, 011);
- строка статуса: `**Статус:** …` · `> Статус: **…**` · `> **Статус:** …` · инлайн после `·` · отсутствует;
- слот ответа: `**Ответ Криника:**` · `**Ответ:**`; отвеченный — текст на ТОЙ ЖЕ строке или в цитате
  под ним, неотвеченный — пусто или пустая цитата `>`.

**Следствие для гарда:** главный сигнал — СТРУКТУРНЫЙ (пустой слот ответа), а не текстовый (слово в
статусе). Именно текстовый греп однажды промолчал, потому что `ЗАКРЫТ` совпал с «за**крыт**ие».
Классификация: `висит` = статус «ждёт» ИЛИ (статуса нет И есть пустые слоты); `частично` = статус
«отвечен», но остались пустые слоты (предупреждение, не нарушение — так устроено интервью 014).

---

## Риски и как снимаем (в порядке убывания цены — оплачено полем)

| # | Риск | Снятие |
|---|---|---|
| 1 | **Хеш без договорённости о нормализации** → гейт отказывает ВСЕГДА при зелёных самотестах (страница считала байты файла, отправитель — нормализованный текст) | ОДНА функция `normalizeBody()`, обе стороны зовут её; её алгоритм записан в этом плане и в шапке инструмента |
| 2 | **Инструмент построен, агент им не пользуется** — в тот же день, когда страница заработала, агент пересказал вопросы в чате: чат дешевле в моменте | инструмент считается ПРИНЯТЫМ, только когда в ритуале записана исполняемая команда, показывающая нарушения → `guard` попадает в `AGENT_GUIDE.md`, `/interview`, лупы и `/end-chat` |
| 3 | **Exit 0 ≠ человек получил сигнал** — нативные уведомления молча глушатся настройками фокуса с кодом успеха | обязательный путь — ЗВУК (`afplay`+`say`), он не зависит от настроек уведомлений; доставку подтверждает Криник словами |
| 4 | **Фикстуры не ловят живые документы** — три дефекта рендерера вылезли только на реальных файлах | условие сдачи: прогон по ВСЕМ 15 живым интервью |
| 5 | **Ложная тревога в гарде хуже пропуска** — она учит игнорировать инструмент | исключения явные, с причиной в строке (`tools/owner-guard-exceptions.json`), а не ослабление команды |
| 6 | **Обе темы ОС** — тёмный код на тёмном фоне поймал владелец, а не самопроверка | `prefers-color-scheme` + ручной тумблер темы на странице |
| 7 | **Не-ASCII регулярки:** в Node `\w`/`\b` — ASCII-only даже с `u` | границы через `\p{L}` / `(?!\p{L})` с флагом `u`, иначе гард молча слеп к собственному языку |
| 8 | `git checkout -- <файл>` на документе с ответами Криника = уничтожение его работы (EXP-0030) | первоисточник владельца коммитится ПЕРВЫМ действием после ответа |

---

## Что делает контур с существующей практикой

Ничего не ломает. Интервью без контура работают ровно как раньше — Криник может по-прежнему вписать
ответ прямо в md, и это **тот же по силе ответ**: `HTML = md = чат` (правило эквивалентности,
`/interview`). Контур — сахар поверх жёсткого канонного правила «место вопросов — `interviews/`», а не
новая обязанность.

## Решения, принятые без владельца

Каждое микрорешение, принятое агентом сольно по ходу работы (канон KAIF 1.6) — чтобы расхождение с
твоим видением стоило одну строку, а не переделку.

| # | Решение | Как выбирал | Как отменить |
|---|---|---|---|
| 1 | Инструмент назван `tools/owner.mjs` | имя ФАЙЛА dev-инструмента, а не бренд-строка (нейминг продукта агент не решает — KAIF 2.1); выбрал описательно, в ряд к `ui.mjs`/`adb.mjs`/`net-chaos.mjs` | `git mv` + правка ссылок в каноне |
| 2 | Гард ищет сирот только в `plans/ bugs/ ideas/ researches/` | канон-доки и летопись ГОВОРЯТ о правиле, а не содержат живых вопросов — их скан давал бы только ложные тревоги, а ложная тревога учит игнорировать гард | `ORPHAN_DIRS` в шапке гарда |
| 3 | Тег `DONE` в имени файла выводит документ из охоты | не ослабление: канон объявил файл закрытым, вопрос внутри — история. Снял 5 ложных хитов из 11 | одна строка в `guard()` |
| 4 | Решения живут в `interviews/decisions/` (не россыпью рядом с md), архив — внутри | директория интервью остаётся читаемой человеком; имя файла решения ВЫВЕДЕНО из имени документа (общий файл затирался бы следующим интервью) | `decisionPathFor()` |
| 5 | Голос **Milena**, звук **Submarine.aiff**, тихие часы **23:00–09:00**, порт **8787**, таймаут **3600 с** | голос — единственный русский из 186 на машине; остальное — разумные дефолты | всё в `tools/owner.config.json`, правится одной строкой без кода |
| 6 | Решения (`interviews/decisions/`) версионируем, страницы (`tools/owner-pages/`) — нет | решение = слово владельца, оно ценность; страница — производное от md (инвариант I1) | `.gitignore` |
| 7 | Ответ подписывается строкой `_записано контуром …_` в самом документе | она же служит границей блока при повторном ответе — без неё копились бы хвосты прошлых ответов | `PROVENANCE_RE` |
| 8 | Путь ИСХОДЯЩИХ артефактов (черновик → одобрение → `gate --artifact`) реализован, но на живом артефакте **не обкатан** | покрыт только `selftest`; первый живой кандидат очевиден — `researches/readme_seo_draft.md` (`ideas/05`) | — |
| 9 | 4 найденных вопроса-сироты **НЕ переведены** в `interviews/` автоматически | это 3–4 новых интервью разом на стол владельцу; канонный ход очевиден, но момент выбирает Криник | по его отмашке |
| 10 | Поле «кто отвечает» с страницы убрано (правка Криника в пилоте) | отвечает всегда владелец проекта; имя берётся из `owner` в конфиге | — |

**Незакрытое допущение (`PENDING`):** К5 — доставка сигнала «из другой комнаты» ещё не подтверждена
человеком; exit 0 доказательством не считается (нативные уведомления глушатся настройками фокуса
МОЛЧА и с кодом успеха). Проверить в первом же случае, когда Криник не за компьютером.
