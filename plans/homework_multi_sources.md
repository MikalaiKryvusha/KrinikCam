# Домашнее задание — Мульти-источники (слои) + добивание Bug 13

> APK уже стоит на устройстве (свежий debug-билд). Если надо переустановить:
> `app/build/outputs/apk/debug/app-debug.apk`.
> Пиши наблюдения ПРЯМО ЗДЕСЬ под каждым пунктом (после `>`).
> Логи — в `plans/logs/`, скриншоты — в `tools/screenshots/`.
> Если что-то крешнётся: лог приложения через Settings → Share logs; файл краша на устройстве —
> `/storage/emulated/0/Android/data/com.kriniks.kcam.debug/files/crash_*.txt`.

---

## Контекст (что я сделал, что проверяем)

Заложил **фундамент мульти-источников**: панель «Scene layers» (FAB → **Layers**). Сейчас слои —
это камера (базовый) + картинки-оверлеи поверх. Можно: добавить картинку, вкл/выкл «глазом»,
двигать по порядку ↑↓, удалять (у оверлеев).

⚠️ **Важно про текущую модель (временную):** камера сейчас — «база» (всегда снизу). Поэтому есть
известные ограничения, которые я уже зафиксировал и буду переделывать по твоей модели (Idea 21,
«камера — обычный слой над чёрной базой»): у камеры нет удаления (Bug 16), z-order камеры
эмулируется (Bug 15). Эти пункты ниже проверяем в рамках ТЕКУЩЕЙ модели — чтобы убедиться, что
хотя бы она работает без крашей, пока делаю большую переделку.

---

## Шаг 1. Слой-картинка из ФАЙЛА (главный новый тест)

Открой **FAB (+) → Layers → «Add image overlay»** → выбери PNG/JPG из файлов (лучше **PNG с
прозрачным фоном** — логотип/рамка).

- [ ] Открылся системный выбор файла? Картинка выбралась?
- [ ] Появилась поверх кадра? Видна и в превью?
- [ ] Картинка НЕ растянута/не искажена (я вписываю её по центру с сохранением пропорций)?
- [ ] В строке слоя видна миниатюра именно твоей картинки?
- [ ] Добавь 2–3 разные картинки — все ли встают слоями, корректен ли порядок?

**КЛЮЧЕВОЙ вопрос по UX (нужен твой ответ — влияет на след. фазу «трансформа»):**
сейчас картинка вписывается ЦЕЛИКОМ по центру кадра (letterbox-fit). Тебе как надо по умолчанию:
- (A) по центру с сохранением пропорций (как сейчас), позицию/размер потом руками;
- (B) сразу маленькой в углу (типа лого), потом двигать;
- (C) на весь кадр (для полупрозрачных рамок/фонов);
- (D) другое — опиши.

**Твои наблюдения:**
>

---

## Шаг 2. Поведение слоёв (порядок, видимость, удаление)

В панели «Scene layers» (верх списка = поверх кадра):

- [ ] «Глаз» у картинки-оверлея вкл/выкл — оверлей появляется/исчезает в кадре?
- [ ] Стрелками ↑↓ переставь камеру НАД оверлеем → оверлей должен ПРОПАСТЬ (камера непрозрачная,
      перекрывает). Так и есть?
- [ ] Верни камеру вниз → оверлей снова виден?
- [ ] Выключи «глаз» у КАМЕРЫ → должно стать: чёрный кадр + включённые оверлеи. Так и есть?
- [ ] Удаление (🗑) у оверлеев работает? (у камеры кнопки удаления пока нет — это Bug 16, чиню.)
- [ ] **Покрутил порядок/видимость много раз подряд при ОТКРЫТОЙ панели — крешей нет?**
      (был баг с переработанным битмапом — Bug 14, я починил, нужно подтверждение.)

**Твои наблюдения:**
>

---

## Шаг 3. Bug 13 — отключение камеры ВО ВРЕМЯ ЖИВОГО СТРИМА (важно)

Раньше приложение крешилось при выдёргивании USB-камеры. Я поставил защиту, но прошлый твой тест
был БЕЗ стрима (детач прошёл чисто, краш не воспроизвёлся). Нужно проверить именно **в эфире** —
там был «шторм», который и ронял.

Сценарий:
1. Подключи камеру, дождись превью.
2. Нажми **Go Live** на рабочий YouTube-профиль, дождись LIVE.
3. На живом стриме **физически выдерни USB-камеру** на ~5–10 сек.
4. Воткни обратно.

- [ ] Приложение ВЫЖИЛО (не закрылось)?
- [ ] Стрим на YouTube не оборвался (показалась заглушка «Please stand by»)?
- [ ] После возврата камеры картинка вернулась в эфир?
- [ ] Если крешнулось — на каком шаге? (выдёргивание / обратное втыкание / спустя время)

**Сразу после теста сними логи** (через Settings → Share logs ИЛИ ADB) и положи путь сюда.
Мне нужен файл лога приложения за время теста + (если был креш) файл `crash_*.txt`.

**Твои наблюдения:**
>

---

## Шаг 4. Idea 21 — подтверждение модели слоёв (мне нужно для большой переделки)

Я записал твою модель: **неявная чёрная база + все слои (включая камеру) равноправны поверх неё,
камера удаляемая/переставляемая как любой слой.** Перед переделкой уточни:

- [ ] Когда камеру удалили/выключили и под ней (ниже по z) есть включённый оверлей — он ДОЛЖЕН
      проявиться (камеры нет → видно нижние слои вплоть до чёрной базы)? (да/нет)
- [ ] Камера по умолчанию в новой сцене — присутствует как слой сверху? (да/нет)
- [ ] Нужна ли тебе в списке слоёв сама «чёрная база» (видимой строкой) или она невидима/неявна?

**Твои ответы:**
>

---

## Что мне прислать (резюме)

1. Заполненные поля выше.
2. Лог приложения за тест Шага 3 (детач в эфире) → положи в `plans/logs/` и дай путь.
3. Если был креш — путь к `crash_*.txt` с устройства.
4. 1–2 скриншота с твоими картинками-оверлеями (как они легли) → `tools/screenshots/`.

> ADB-команда для лайв-логов (фильтр по нашим тегам):
> `adb logcat | grep -E "RtmpStreamer|UsbDeviceRepository|SceneCompositor|UsbViewModel"`
