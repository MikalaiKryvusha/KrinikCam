---
description: Дать Кринику прямо в чате ясный структурный мануал по KAIF — что это (кратко) и главное КАК этим пользоваться в KrinikCam - структура, договорённости, документы, директории, навыки/команды. Вызывается когда Криник говорит «как пользоваться KAIF», «помощь по KAIF», «мануал KAIF», «что умеет KAIF», «справка KAIF», «help kaif», «explain KAIF».
---

# /help-kaif — объяснить KAIF оператору, в чате

Выдай **мануал пользователя KAIF прямо в чат**, на русском. Это обучающий момент для Криника — навык
**не меняет файлы**, только ясное структурированное объяснение, по которому можно действовать.

## Рамка (важно)
- **Сначала прочитай `.kaif/KAIF_REFERENCE.md` — это авторитетный справочник по фреймворку.** Отвечай
  по нему и ЦИТИРУЙ его секции («Справочник §10.2»), чтобы Криник мог сам копнуть глубже; на вопрос
  про конкретный механизм — приведи точный абзац. Никогда не отвечай про фреймворк по памяти о
  старой версии: справочник на диске описывает ту версию, что реально развёрнута здесь (поймано в
  поле: оператор спросил «что у меня есть» и получил ответ двухверсионной давности).
- KAIF в KrinikCam **уже развёрнут**. НЕ рассказывай про распаковку/установку — это позади. Говори в
  ключе «вот как *пользоваться* тем, что уже здесь».
- «Что такое KAIF» — **пара предложений**. Основной объём — **как пользоваться**: структура,
  договорённости, документы, директории, навыки.
- Имена `/команд` и файлов — канонические (английские), текст — русский.
- Опирайся на развёрнутую реальность ЭТОГО проекта (справочник + `KAIF_FRAMEWORK.md`,
  `AGENT_GUIDE.md`, README директорий, ФАКТИЧЕСКИЙ инвентарь `.claude/skills/` — никогда не
  зашитый в память список) — не на общий питч.

## Что выдать (структура сообщения в чате)

1. **Что такое KAIF (2–3 предложения).** Устойчивая к потере контекста, дисциплинированная методология
   тандема «человек–ИИ»: Криник — визионер, агент — исполнитель, память и дисциплина проекта живут в
   файлах репозитория, и ни одна сессия не начинается с нуля. Одна строка — чем это полезно здесь.

2. **Ключевые документы — что читать/вести и кто хозяин.** Списком: `AGENT_GUIDE.md` (канон),
   `PHILOSOPHY.md` (мышление агента), `BUG_FIXING_FRAMEWORK.md`, **`GOAL.md`** (видение — документ
   Криника), `STATUS.md` (живая сводка «сейчас»), `PROJECT_HISTORY.md` (летопись — археология по
   требованию), `MASTER_PLAN.md` (генплан), внешняя и внутренняя карты,
   `KAIF_FRAMEWORK.md` (сводка «что развёрнуто»).

3. **Директории — где живут знания и где действует владелец.** `plans/`, `ideas/` (в основном твои),
   `bugs/`, `researches/`, `interviews/` (ты отвечаешь здесь), `homeworks/` (задания тебе). Одной
   строкой — про тег DONE.

4. **Навыки — команды, которые ты набираешь.** Сгруппированным списком с одной строкой назначения —
   группы собирай по ФАКТИЧЕСКОМУ инвентарю `.claude/skills/` (никогда не по этому примеру дословно):
   сессия (`/resume`, `/pause` — мягкая парковка, чат продолжается, `/end-chat` — полное завершение с
   передачей эстафеты), автономия (`/autoloop`, `/dayloop`, `/nightloop`, `/guarded-loop`), гигиена
   (`/refresh-context`, `/check-backlog`), знания и память (`/report-bug`, `/bug-research`,
   `/propose-idea`, `/experience`), владелец (`/interview`, `/fix-vision`, `/what-next`,
   `/owner-voice`, `/owner-reviews`), планирование (`/plan-task`, `/plan-epic`, `/revision`),
   защитные контуры (`/derive-styleguide`, `/code-revision`), дисциплина исполнения
   (`/fable-method`, `/fable-loop`, `/fable-judge`, `/fable-domain`), помощь (`/help-kaif`),
   релиз (`/release`), жизненный цикл (`/kaif-version`, `/kaif-update`, `/kaif-fork`,
   `/kaif-switch-origin`, `/kaif-remove`).

5. **Как выглядит обычный воркфлоу.** Короткий пример: *«`/resume` для старта → я работаю и веду
   `STATUS.md` → ты кидаешь идеи в `ideas/` или отвечаешь на `/interview` → `/pause` чтобы прерваться
   (чат продолжится позже) или `/end-chat` чтобы закрыть чат с передачей эстафеты»*.
   Роли: Криник — визионер (GOAL, идеи, ответы), агент — исполнитель.

6. **Куда глубже.** Укажи на `KAIF_FRAMEWORK.md` и `AGENT_GUIDE.md`.

## Примечания
- Навык «прочитай-и-объясни» — не редактируй файлы, не меняй состояние.
- Держи текст сканируемым: короткие секции, списки вместо простыней. Цель — Криник дочитал и точно
  знает, какой документ открыть и какую команду набрать.
- Если Криник спросил про одну конкретную часть («как работают интервью?») — ответь сфокусированно,
  затем предложи полный мануал.
