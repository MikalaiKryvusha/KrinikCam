Билд 0.5 (21)

Запускаю приложение в портретной ориентации устройства - появляется видео с фронтальной камеры устройства в превью

Баги:
-Видео повёрнуто на 90 градусов по часовой стрелке
-если повернуть устройство - приложение меняет ориентацию в пейзаж а видео с камеры из превью на главном экране исчезает.
---

## Статус верификации (2026-07-06, Phase 3 единый пайплайн)

🟢 **Оба симптома АРХИТЕКТУРНО закрыты Phase 3** (композитор = дефолт, plans/02). Проверено на живом
планшете (2K USB, скриншоты):

1. **«Превью исчезает при повороте устройства»** — ИСПРАВЛЕНО. Убрано ветвление `when(activeSource)`
   в MainScreen (оно рушило TextureView) → превью-TextureView живёт ВСЕГДА. Повернул устройство
   портрет↔пейзаж (`ui.mjs orient`) — камера остаётся видна (леттербокс-полоса), процесс жив, краша
   нет. Это же закрывает bug 20/23 (EGL_BAD_ALLOC при отрыве/смене источника).
2. **«Видео повёрнуто на 90° CW» (встроенная камера)** — по модели interview_006 физкамеры отдают
   СЫРОЙ поток; выпрямление — поворотом СОДЕРЖИМОГО слоя (`set-transform camera … <rotation>`),
   проверено записью (plans/02 §5, кейс 3). Останется ручным до idea 20 (UX выбора/трансформы).

DONE-тег — после финальной сверки Криником на его физ-камерах.

## ✅ СТАТУС: DONE (2026-07-18, ночная ревизия беклога)
Что сделано: оба симптома закрыты Phase 3 (превью-TextureView живёт всегда, ветвление when(activeSource) убрано).
Как проверено: живая верификация 2026-07-06 на планшете (2K USB, скриншоты, orient-прогоны) — см. секцию выше; 2026-07-18 подтверждён смежный bug 20 репро-прогоном.
