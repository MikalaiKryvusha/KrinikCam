# Bug 65 — Часть UI не слушается языковой настройки приложения (глобальная локализация)

> Заведён: 2026-07-19 (Криник, живой тест). Устройство: Titan 1, `com.kriniks.kcam.debug` 0.8 (4)-debug.
> Приоритет: СРЕДНИЙ (качество/бренд, широкий охват). Глобальный проход по UI.

## Симптом

Многие элементы UI **не переключаются** на язык, выбранный в Настройках (Settings → Язык приложения,
bug 46). Часть строк остаётся на одном языке / не реагирует на смену глобальной настройки.

## Что нужно (задача)

1. **Пройтись по ВСЕМУ UI** и найти строки, которые не локализуются:
   - хардкод-строки прямо в Composable/диалогах (не через `stringResource`/`strings.xml`);
   - строки, резолвящиеся ОДИН раз на старте (не реагируют на смену локали без рестарта).
2. Вынести хардкод в `strings.xml` (EN дефолт + `values-ru`), заменить на `stringResource`.
3. Убедиться, что смена языка в Настройках **корректно переключает весь UI** согласно глобальной
   настройке приложения (в идеале — без перезапуска; минимум — консистентно после применения).

## Гипотезы / зоны риска (проверить)

- **Хардкод-строки** в композаблах/диалогах/оверлеях (grep по кириллице и англ-литералам в `.kt` UI).
- **Строки, локализуемые на старте:** напр. `DeviceCameraEnumerator.enumerate` формирует имена камер из
  ресурсов ОДИН раз при инициализации (в коде помечено: смена локали отразится после перезапуска —
  пересоздания источников). Аналогичные «снимки» строк.
- **Механизм смены локали (bug 46):** проверить, применяется ли выбранный язык ко всем экранам/оверлеям
  и пересобираются ли строки при смене (recompose vs кэш).
- **contentDescription / динамически собранные строки** (конкатенация в коде вместо форматных ресурсов).

## План (первый шаг — аудит)

1. `grep` по UI-модулям на кириллицу и «голые» строковые литералы в Composable → список хардкода.
2. Проверить путь применения языка (bug 46 / plans/16) — где ставится локаль, что триггерит пересборку.
3. Вынести найденное в ресурсы, заменить на `stringResource`, повторно проверить переключение.

---

## ✅ Шаг 1 плана — АУДИТ ВЫПОЛНЕН (2026-08-02). Инвентарь хардкода

Метод: поиск кириллических строковых литералов в `.kt` (кириллица в коде = гарантированный хардкод,
потому что дефолтные ресурсы проекта английские), с отсевом комментариев и лог-строк, затем ручная
проверка КОНТЕКСТА каждого попадания. Проверялось на A51 — первом устройстве парка с системной
локалью `ru_RU` (видно в логе: `[ru_RU] … 420dpi`), то есть на нём этот баг вообще можно наблюдать.

**Правило инвентаря: сдача судится ПО СТРОКАМ.**

| # | Место | Строка | Вердикт |
|---|---|---|---|
| 1 | `feature/streaming/scene/Layer.kt:98` | `name = "Устройство захвата видео"` | 🔴 **ДЕФЕКТ** — имя слоя, видно в панели слоёв (подтверждено скриншотом эфира 2026-08-02) |
| 2 | `feature/streaming/scene/Layer.kt:41` | `displayName = "Виртуальная камера"` | 🔴 **ДЕФЕКТ** — имя источника в UI выбора |
| 3 | `feature/streaming/scene/Layer.kt:46` | `displayName = "Нет источника"` | 🔴 **ДЕФЕКТ** — то же |
| 4 | `ui/overlay/ScenesManagerOverlay.kt:168` | `"%.1f с".format(ms / 1000f)` | 🔴 **ДЕФЕКТ** — суффикс единицы «с» вшит по-русски |
| 5 | `ui/overlay/StandbyPlaceholder.kt:33-39` | 5 языков разом | ✅ **НЕ дефект** — заглушка многоязычна ПО ЗАМЫСЛУ (ответ Криника на Q6): все строки показываются одновременно |
| 6 | `feature/streaming/scene/StandbyImage.kt:31` | `"Please stand by", "Пожалуйста, подождите"` | ✅ **НЕ дефект** — тот же замысел, но в композите кадра |
| 7 | `feature/streaming/scene/NetworkSlateImage.kt:50` | `LINE_RU = "Связь восстанавливается"` | 🟡 **УЖЕ ЗАПЛАНИРОВАНО** — это работа **C2b** (`plans/21`, решение `interview_014`: текст ленты собирается из локали приложения + EN). Здесь НЕ дублировать |
| 8 | `MainActivity.kt:360,428` | `CaptureSource.Builtin(it, "Камера $it")` | 🟠 **ДЕФЕКТ, но debug-путь** — имя источника собирается по-русски в CMD-приёмнике (харнес). В обычном UI имена встроенных камер идут из `DeviceCameraEnumerator`. Чинить вместе с 1–3 (тот же тип поля) |
| 9 | `RtmpStreamer.kt`, `CompositorVideoSource.kt`, `UsbDeviceRepositoryImpl.kt`, `DeviceCamera.kt`, `SceneSnapshot*.kt` + остаток `MainActivity.kt` | **122** кириллических литерала суммарно по этим файлам (замер `grep -cE '^[^/*]*"[^"]*[а-яА-ЯёЁ][^"]*"'`) | ✅ **НЕ дефект** — лог-сообщения, их продолжения и внутренние технические строки; канон (`AGENT_GUIDE.md` → Локализация) прямо запрещает выносить логи в ресурсы |

**Итого к работе: 5 строк в 3 файлах.** Аудит оказался куда меньше, чем предполагала формулировка
бага («многие элементы UI»): основная масса кириллицы в коде — это логи, а видимые пользователю
многоязычные заглушки сделаны намеренно.

> ⚠️ **Как считалось (чтобы число можно было перепроверить, а не поверить).** Первый проход
> отбрасывал строки с `KLog`/`Log.`/`TAG` и дал 47 — я чуть не записал это число в документ как
> «логов». Оно неверно: оно считало литералы ВНЕ лог-вызовов, а не логи. Пересчёт без фильтра дал
> **122**, а выборочная проверка «остатка» вскрыла позицию 8, которую первый проход прятал. Урок в
> одну строку: **фильтр, придуманный для отсева шума, отсеивает и находки** — прежде чем публиковать
> число, скажи, что именно оно считает, и загляни в то, что фильтр выбросил.

### Что мешает закрыть это в один присест (честно)

Строки 1–3 живут в `feature:streaming` как **`String`-поля модели сцены** (`CaptureSource.displayName`,
`Layer.VideoCapture.name`), а не в UI. Правильный фикс — не подмена литерала, а перевод их на `UiText`
(механизм уже принят проектом, `plans/13`), потому что feature-модуль не должен резолвить ресурсы сам.
Это касается всех потребителей поля, включая сериализацию профилей сцен
(`SceneSnapshotMapper`/`SceneProfileRepository`) — там имя УХОДИТ В ПЕРСИСТ, и просто так менять тип
нельзя: сохранённые сцены Криника не должны сломаться. Отсюда порядок работ:

- [ ] Ш2. Проверить, попадает ли `name`/`displayName` в сохранённые снимки сцен (Room/JSON) и как
      мигрировать без потери сцен владельца.
- [ ] Ш3. Перевести три поля на `UiText`, добавить строки в `values/strings.xml` + `values-ru/`.
- [ ] Ш4. `ScenesManagerOverlay.kt:168` — суффикс единицы через форматный ресурс (`R.string.seconds_fmt`);
      `MainActivity.kt:360,428` — «Камера $it» тем же ресурсом.
- [ ] Ш5. Приёмка на A51 (`ru_RU`) и с переключением языка приложения в Настройках.

## Связи

- bug 46 / plans/16 (селектор языка приложения), Idea 14 (все UI-тексты — через strings.xml), plans/13
  (локализация EN/RU, UiText для VM). Этот баг — «добор» того, что мимо этих правил просочилось.

## Статус
🔴 ОТКРЫТ (СРЕДНИЙ, глобальный проход). Симптом от Криника. Первый шаг — аудит хардкода + проверка
механизма смены локали.
