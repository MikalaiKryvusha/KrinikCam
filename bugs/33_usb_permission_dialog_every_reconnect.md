# Bug 33 — Навязчивый системный USB-диалог «Разрешить доступ к камере» на КАЖДОМ переподключении

> Заведён: 2026-07-07 (Криник, дома, live). Приоритет: ВЫСОКИЙ (бесит пользователя ежедневно).
> Связано: plans/04 §Фикс 29.1 (S1 не сделан), bug 09 (манифест-автогрант).

## Симптом (со слов Криника)

«Когда отключаю зарядку, подключаю камеру — этот диалог "Можно ли использовать камеру" ПОСТОЯННО
появляется». Договаривались, что он не будет навязчиво показываться — а он лезет на каждом
переподключении USB-камеры (отключил зарядку → воткнул камеру через OTG → снова диалог).

## Причина (гипотеза — plans/04 §29.1 S1, НЕ реализован)

`AUSBC/USBMonitor` при `DeviceAttached` зовёт системный `requestPermission` БЕЗ предварительной
проверки `usbManager.hasPermission(device)`. Грант разрешения на устройство (VID/PID) персистентен,
но код всё равно каждый раз просит → системный диалог каждый раз. S2 (дебаунс дубликатов в окне 3с,
2026-07-06) убрал СТОПКУ одинаковых, но не убрал ОДИН диалог на каждое подключение.

Манифест-автогрант (bug 09, `device_filter.xml` class 239/2) покрывает запуск приложения при уже
воткнутой камере, но НЕ hot-plug при уже открытом приложении.

## План фикса (plans/04 §29.1 S1)

- [ ] Перед `requestPermission` проверять `usbManager.hasPermission(device)`:
      - если TRUE → сразу открывать камеру, НЕ звать `requestPermission` (нет диалога);
      - если FALSE → запросить один раз (грант персистентный, «всегда разрешать» для устройства).
- [ ] Проверить, что грант реально ПЕРСИСТИТ (пользователь мог не отмечать «всегда»; на некоторых ОС
      грант живёт до отключения устройства — тогда hot-plug всё равно спросит один раз). Изучить, можно
      ли шире покрыть `device_filter.xml` / intent-filter USB_DEVICE_ATTACHED (авто-грант при attach).
- Место: `UsbDeviceRepositoryImpl.requestPermission` / `UsbViewModel` (перед запросом).

## Как воспроизвести

1. Приложение открыто. Отключить зарядку, воткнуть USB-камеру через OTG (hot-plug).
2. Наблюдать системный диалог «Разрешить … доступ к USB-устройству?» — и так каждый раз.

## Уточнение root cause по байткоду libuvc 3.2.7 (ревизия 2026-07-12)

Дизассемблирован `USBMonitor.requestPermission` (Gradle-кеш): он **САМ проверяет
`UsbManager.hasPermission(device)`** и при true идёт сразу в `processConnect` БЕЗ диалога. Значит
«просто добавить проверку перед запросом» (гипотеза S1) НЕ поможет — диалог на каждом replug означает,
что **грант реально отсутствует в момент вызова**:
- Персистентный грант появляется только если пользователь отметил «Открывать по умолчанию» в диалоге
  (иначе грант живёт до отсоединения устройства).
- Плюс гонка: автогрант от манифест-интента `USB_DEVICE_ATTACHED` может примениться ПОЗЖЕ, чем
  `onAttachDev` → auto-requestPermission (UsbViewModel.kt:69) — мы спрашиваем раньше, чем система
  успела выдать.
- Смежный усилитель: `startMonitoring()` не идемпотентен → дубли attach-событий → дубли запросов
  (bugs/35 п.5).

**Скорректированный план фикса:**
- [ ] Обрабатывать `onNewIntent`/launch-intent с `USB_DEVICE_ATTACHED` — в этот момент грант уже
      выдан системой, открывать камеру напрямую (диалога не будет).
- [ ] При `hasPermission=false` на attach — отложенная перепроверка (300–800 мс) перед запросом
      (дать автогранту примениться).
- [ ] В UI-подсказке направлять пользователя отметить «Открывать по умолчанию» (единственный
      механизм персиста гранта).
- [ ] Идемпотентный startMonitoring (bugs/35).
- Диагностика 2026-07-12: билд 0.6(12) с логом `hasPermission=` стоит на планшете; при старте с уже
  воткнутой Piko+ камера открылась БЕЗ диалога и БЕЗ requestPermission (манифест-автогрант покрыл).
  ⏳ Ждёт живого hot-plug Криника для лога.

## Новый симптом 2026-07-18 (Криник, live) — диалог на ЛЮБОЕ USB-устройство

«Каждый раз, когда ЧТО-ТО по USB подключается к планшету — приложение открывает модальный диалог,
спрашивает разрешение работать с подключенным девайсом». То есть не только камера: любое устройство.
Root cause ПОДТВЕРЖДЁН кодом: `UsbViewModel.kt:69` безусловно зовёт `requestPermission` на КАЖДЫЙ
`DeviceAttached`, а `onAttachDev` эмитил событие для любого устройства — класс-фильтра на пути
запроса не было вовсе (`device_filter.xml` работает только на манифест-интент, не на наш запрос).

## Фикс 2026-07-18 (ночной цикл, build 0.7(3), сверено dumpsys versionName) — ждёт живой приёмки

`INTENT:` код слал requestPermission на attach любого устройства и мог спросить раньше автогранта;
ожидание Криника — не-камеры молчат всегда, камера максимум один диалог; спека (device_filter.xml +
байткод USBMonitor: сам скипает диалог при hasPermission) — расхождение в нашем коде.

Всё в `UsbDeviceRepositoryImpl.kt` (хирургически, +65/−7, bug 35-кластер не тронут):
1. **S1 — UVC-фильтр в `onAttachDev`** (`isUvcCamera()`: девайс 239/2 IAD или class 14, или скан
   интерфейсов на class 14 — зеркало device_filter.xml). Не-камеры игнорируются ЦЕЛИКОМ: ни диалога,
   ни фантома в списке (смежно bug 35). USB-микрофоны не страдают — аудио-класс обслуживает ОС.
2. **S2 — гонка автогранта в `requestPermission`**: hasPermission есть → просим сразу (USBMonitor
   уйдёт в processConnect БЕЗ диалога); нет → поллим грант 150мс-шагами до 900мс (даём автогранту от
   USB_DEVICE_ATTACHED примениться) и только по таймауту показываем диалог — один, легитимный.

**Проверено наблюдением (ночь, без рук):** сборка зелёная; монитор стартует без краша (лог 00:51:44);
`smoke.mjs` PASS (1920×1080, 117 кадров) — пайплайн не сломан. **[NOT-TESTED] на живом hot-plug**
(ночью в USB-хост ничего не воткнуто, attach-событий не было) → приёмка руками = `homeworks/07`.

**Границы (честно):** совсем без единого диалога нельзя — USB-грант это механизм безопасности Android.
Итоговое поведение: не-камеры — тишина всегда; камера — диалог один раз на устройство (с галочкой
«всегда открывать» — грант персистит); галочка слетает при переустановке/обновлении приложения
(штатное поведение Android, для dev-циклов диалог вернётся один раз после установки билда).

## Статус
🟡 ФИКС РЕАЛИЗОВАН (2026-07-18, S1 фильтр + S2 гонка), smoke PASS, [NOT-TESTED] живой hot-plug.
Ждёт утренней приёмки Криника — `homeworks/07_usb_dialog_acceptance.md`. После подтверждения → DONE.
