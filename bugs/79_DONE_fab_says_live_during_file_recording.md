# Bug 79 — Главный FAB пишет «ЭФИР» во время ЗАПИСИ В ФАЙЛ: кнопка врёт о том, что происходит

> **Статус:** ✅ ЗАКРЫТ 2026-08-02 — фикс принят живьём, вердикт Криника получен и применён
> **Приоритет:** СРЕДНИЙ-ВЫСОКИЙ (ложная индикация состояния стримеру)
> **Заведён:** 2026-08-02 со слов Криника (живое использование) · **Фикс:** 2026-08-02, версии 0.8 (47)–(49), debug

## Симптом (слова Криника, 2026-08-02)

> «*состояние FAB главной при записи в файл до сих пор не исправлено. Пишет ЭФИР, когда это не эфир,
> а запись в ФАЙЛ*»

Главная круглая кнопка внизу-справа во время записи в файл показывает бейдж **«ЭФИР»** (EN: `LIVE`)
и красится в эфирный красный, хотя эфира нет — идёт запись на устройство.

**«до сих пор»** — Криник сообщает об этом не впервые.

## Root cause — одно решение принимается в ДВУХ местах, и только одно знает про запись

| Место | Код | Знало про запись? |
|---|---|---|
| Плашка статуса, `MainScreen.kt:746` | `stringResource(if (live.isRecording) R.string.fab_rec_badge else R.string.fab_live_badge)` | ✅ ДА — показывала «ЗАПИСЬ»/`REC` |
| **Главный FAB, `FloatingActionMenu.kt:125`** | `Text(stringResource(R.string.fab_live_badge), …)` | ❌ **НЕТ — «ЭФИР» безусловно** |

Ресурсы для обоих состояний давно существовали и были переведены:
`fab_live_badge` = «ЭФИР» / `LIVE`, `fab_rec_badge` = «ЗАПИСЬ» / `REC`.

### Почему это тот же КЛАСС, что bugs/77

Ровно ситуация из **EXP-0047**: один экземпляр решения сделан правильно, а его близнец — нет, и
правильный экземпляр создаёт ложное ощущение, что вопрос закрыт. Поэтому фикс — не «дописать `if` в
FAB», а **снять дублирование**.

## Фикс (0.8 (47)) — сделан ФОРМОЙ

- [x] **Ш1.** `app/src/main/kotlin/com/kriniks/kcam/ui/SessionBadge.kt` — единственное место, где
      выбирается бейдж: `sessionBadgeRes(state)` + `StreamState.isRecordingSession`.
- [x] **Ш2.** `FloatingActionMenu.kt` зовёт помощника вместо константы.
- [x] **Ш2б.** `MainScreen.kt` (плашка) зовёт того же помощника — чтобы источник был ОДИН, а не «один
      правильный и один общий».
- [x] **Ш3.** Инвентарь потребителей: `grep -rn "fab_live_badge\|fab_rec_badge"` → до фикса два места
      в `.kt`, после — **ноль вне `SessionBadge.kt`**.
- [x] **Ш4.** Приёмка живьём (ниже).
- [x] **Гард** — `tools/lint-source.mjs`: `R.string.fab_live_badge` / `fab_rec_badge` допустимы ТОЛЬКО
      в `SessionBadge.kt`. Правило якорится полной формой, а не подстрокой.

### Гард проверен на СЛОМАННОЙ версии

`node tools/lint-source.mjs selftest` — 7 проверок, включая скармливание гарду ровно дефекта bug 79
(константа снова уехала в `FloatingActionMenu.kt`) → гард **краснеет** и указывает верный файл и
строку. Отдельно проверено, что законный владелец нарушением НЕ считается и что `build/` не сканируется.
Зелёный прогон по репозиторию после этого что-то значит.

## Приёмка — наблюдением, A51 (0.8 (47), ru_RU)

| Сценарий | FAB | Плашка | Вердикт |
|---|---|---|---|
| Запись в файл, 0.8 (47) | «ЗАПИСЬ» | «ЗАПИСЬ • 00:26 • 1,7 Mbps» | ✅ (до фикса FAB писал «ЭФИР») |
| Эфир на локальный RTMP-полигон | **«ЭФИР»** | «ЭФИР • 00:09 • 1,7 Mbps» | ✅ ветка эфира не сломана |
| Запись в файл, 0.8 (49), **ru_RU** | **«ФАЙЛ»** | «ФАЙЛ • 00:08 • 1,5 Mbps» | ✅ слово Криника применено |
| Запись в файл, 0.8 (49), **English** | **`FILE`** | `FILE • 00:26 • 1.4 Mbps` | ✅ зеркало локали сходится |

Проверены ОБЕ ветки состояния и ОБЕ локали: правка условия, у которой посмотрели одну сторону, —
половина проверки; а строку, которую агент выбрал сам (`FILE`), тем более надо было увидеть на
экране, а не в diff. Английская локаль проверена переключением в Настройках и возвращена в
«Следовать системе».
Скриншоты: `bug79_recording_fab.jpg`, `bug79_live_fab.jpg`, `bug79_file_badge.jpg`,
`bug79_file_badge_en.jpg` (`tools/screenshots/`, gitignored).

## ✅ Вердикт Криника (interview_023 В1, 2026-08-02T12:58)

> «*так же, как и ЭФИР. И писать не ЗАПИСЬ, а "ФАЙЛ"*»

Два решения в одной строке, и второе я не предвидел:

1. **Цвет — как у эфира.** Изменений в коде НЕ потребовалось: `fabColor = if (streamState.isLive)
   LiveRed else AcidPink`, а `isLive` истинно и при записи (запись переиспользует `StreamState.Live`)
   — то есть красный там уже стоял. Мокапы 2–4 отклонены, принят вариант 1 (`#FF1A1A`).
2. **Слово на бейдже — «ФАЙЛ», а не «ЗАПИСЬ».** Этого в вопросе не было: я спрашивал только про цвет,
   а Криник заодно переименовал состояние. Логика его слова читается — кнопка называет ФАКТ («идёт
   файл»), а не действие; и рядом с «ЭФИР» это ровная пара из одного слова.

**Что изменено:** `fab_rec_badge` — RU «ЗАПИСЬ» → **«ФАЙЛ»**, EN `REC` → **`FILE`**.
Имя ресурса (`fab_rec_badge`) оставлено: это идентификатор, а не то, что читает пользователь.

⚠️ **Английское слово выбрал агент, не Криник.** `FILE` — прямое зеркало его «ФАЙЛ»; оставить `REC`
значило бы, что две локали говорят о состоянии разное, а это ровно тот класс расхождений, который
баг и закрывал. Помечено комментарием прямо в `values/strings.xml` — правится одной строкой, если
Криник захочет другое слово.

## 📦 В2 → в беклог: «запись + эфир одновременно»

Вердикт Криника (interview_023 В2): **(в) «Хочу, но не сейчас — положи в беклог»**, дословно
«*Это, как мне кажется. очень большая фича, которая тянет на Эпик, ресёрч*».
Оформлено идеей `ideas/46` со статусом «одобрено направление, срока нет» и пометкой, что вход в
работу — через `/plan-epic` с обязательной разведкой прио-арта.

## ❌ Снят как несуществующий: вопрос «запись + эфир одновременно»

При заведении бага вторым вопросом Кринику стояло «что показывать на ОДНОЙ кнопке при записи И эфире
одновременно». **Проверка кода показала, что такого состояния сегодня нет — ни через UI, ни через движок:**

- `FloatingActionMenu.kt:75` — пока сессия активна (`isActive`), меню показывает ТОЛЬКО «Стоп»:
  ни «Запись» в эфире, ни «В эфир» при записи;
- `RtmpStreamer.kt:1790` — `startRecordToFile` отказывает: `if (stream.isStreaming || stream.isRecording) return null`.

Спрашивать индикацию для недостижимого режима — значит просить владельца спроектировать
несуществующую фичу. Вопрос переформулирован в `interview_023` как вопрос **о самой фиче**
(нужна ли комбинация вообще), и блокером фикса он больше не является.

⚠️ Побочная находка на будущее: `startStream` НЕ имеет симметричного гарда против `isRecording` —
если комбинацию когда-нибудь откроют в UI, старт эфира поверх записи пойдёт по пути
`stopPreview` + `prepareVideo` на живом мухере. Отдельной задачей, пока путь закрыт сверху.

## Решения, принятые без владельца

| Решение | Как выбрано | Если не так — цена правки |
|---|---|---|
| Не ждать ответов по интервью, а починить подпись сразу | Подпись — не вопрос вкуса, а ложь; ресурсы для обоих состояний давно существовали | — |
| Свести решение в один источник, а не дописать `if` в FAB | `BUG_FIXING_FRAMEWORK.md` → «закрывай класс, чини формой»; иначе третий потребитель разъедется снова | локальная |
| Положить помощник в `:app`, а не рядом со `StreamState` | `R.string` живёт в `:app`; тащить ресурсы в `:feature:streaming` ради одной функции — лишняя сущность | перенос файла |
| Цвет НЕ трогать до вердикта Криника | Класс ВКУСА: агент делает мокап, а не вердикт | одна строка |
| **Английское `FILE` поставлено агентом** | Криник назвал русское слово; оставить `REC` = две локали говорят разное (тот самый класс, что чинил баг). Помечено в `strings.xml` как решение агента | одна строка |
| Имя ресурса `fab_rec_badge` НЕ переименовано вслед за словом | Идентификатор ≠ то, что читает пользователь; переименование тронуло бы гард и историю без выгоды | — |
| Соседние строки («Запись» в меню, «ПОДГОТОВКА») НЕ трогал | Криник высказался о БЕЙДЖЕ; менять заодно кнопку действия — додумывание за владельца. Вынесено ему отдельной строкой | одна строка каждая |
| Завести общий `tools/lint-source.mjs`, а не разовую проверку | Класс «одно решение в двух местах» уже стоил проекту bugs/77, bugs/79 и EXP-0047 — правилу нужна таблица, куда дописывать | — |

## Связи

- `EXP-0047` — урок ровно про этот класс («починенный экземпляр маскирует живой класс»).
- `bugs/45` — родня по смыслу: индикация не должна врать о фактическом состоянии.
- `bugs/53` — соседняя плашка состояния эфира/записи (ждёт вердикта Криника по мельканию битрейта).
- `interviews/interview_023_fab_state_indication.md` — цвет (мокапы) + вопрос о самой фиче «запись+эфир».
