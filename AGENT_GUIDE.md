# KrinikCam — AI Agent Guide

Этот файл читается AI-агентом перед каждой задачей.

> 🧠 **ГЛАВНЫЙ ПРИНЦИП — ПРОСТОТА (читай `PHILOSOPHY.md`).** Если что-то долго не получается — это НЕ
> сложная задача и НЕ баг библиотеки, а агент ДЕЛАЕТ СЛИШКОМ СЛОЖНО, потому что НЕ ПОНЯЛ ЗАДАЧУ.
> Всё должно делаться просто (KISS + Бритва Оккама). Не получается → переосмысли понимание задачи,
> ищи штатный простой путь, НЕ усложняй. Затык ≠ «копать сложнее», затык = «упростить понимание».

> 🤖 **АВТОНОМНЫЙ РЕЖИМ — сам запускай `/autoloop`.** Если Криник отошёл / дал автономию и нет
> активной интерактивной задачи, а в `STATUS.md` есть незакрытый «🤖 АВТОНОМНЫЙ ПУЛ ЗАДАЧ» (задачи без
> камеры/стрима/Криника) и устройство доступно — агент ДОЛЖЕН сам, по своей инициативе, войти в навык
> **`/autoloop`** (через Skill-инструмент) и грайндить пул серией, коммитя прогресс и самоперезапуская
> цикл после каждой задачи. Останавливайся только когда пул исчерпан, нужна камера/стрим/Криник, или
> Криник сказал стоп. Не уходи в луп, если Криник дал конкретную интерактивную задачу прямо сейчас.
> Когда Криник просит защищённый прогон (с будильниками и вахтёром) — вместо этого `/guarded-loop`
> (KAIF 2.1): внешние побудки каждые N минут, heartbeat-файл как доказательство прогресса, политика
> перезапуска с эскалационным капом — чтобы зависший чат или упавшая сеть не убили цикл молча.

---

## Перед каждой задачей — чеклист

```
1. Прочитать STATUS.md                # текущий статус: что сделано, где находимся, что дальше
2. Вспомнить опыт                     # grep EXPERIENCE.md по тегам задачи — не повторять известные тупики (навык /experience)
3. git status                         # что изменено, что не закоммичено
4. git log --oneline -5               # где находимся в истории
5. Прочитать MEMORY.md                # /memory/MEMORY.md — профиль пользователя, ключевые решения
6. Грузить ТОЛЬКО нужный срез         # контекст-роутер ниже: обязательный минимум + доки под тип задачи, не всё подряд
7. Исполнять по fable-циклу           # /fable-method: гейты + принудительные артефакты (INTENT/AUTH/TWINS/PENDING); /fable-loop — оркестрация; /fable-judge — перед «готово»
8. Прочитать нужный план из plans/    # если задача связана с конкретной фичей. Кодируй, ЦИТИРУЯ план:
   перед реализацией шага процитируй якорную строку плана, которую делаешь прямо сейчас — не можешь
   назвать строку = дрейф скоупа, пойманный ДО диффа (KAIF 1.6). ТЯЖЁЛАЯ задача, у которой плана ещё
   нет → сначала построй лестницу (раздел «Дисциплина планирования» ниже: `/plan-task` для обычной
   работы, `/plan-epic` для эпика), и только потом код (KAIF 2.1)
9. Разведка до кода (внешняя правда)  # (KAIF 1.6) задача опирается на внешнюю правду (старая/эталонная
   система, чужой API, поведение прода, док вендора)? ПЕРВЫЙ артефакт — разведдок в researches/ — код
   запрещён, пока его нет; дальше код пишется ПО документу, а не по памяти. Разведдоки переиспользует
   каждая будущая сессия. См. «Разведартефакты» ниже.
9а. Разведка ПРИО-АРТА перед ЭПИКОМ   # (правило Криника, 2026-07-28) Перед КРУПНОЙ фичей — обязательный
   гуглёж отраслевых золотых стандартов и научных работ + разведдок в researches/. «Почти на всё в
   индустрии есть золотые стандарты и научные работы» — сначала выясняем, КАК ЭТО УЖЕ РЕШЕНО, и только
   потом проектируем своё. Код и план запрещены, пока документа нет. См. «Разведартефакты» ниже.
10. Запустить build (если трогаешь код)# JAVA_HOME=AS_JBR node tools/build.mjs --no-ui
11. Для тестирования приложения — ОБЯЗАТЕЛЬНО загляни в `tools/UI_AUTOMATION_GUIDE.md` и активно
   пользуйся `tools/ui.mjs` (dump/tap/find + `allow` — сам одобряет системные диалоги
   разрешений, `kill/start/restart` — освобождает камеру между dev/release, `screen` — JPEG-скрин).
   Инструмент активно развивай под нужды тестирования.
12. Обязательное комментирование кода, отдельных его блоков, классов, модулей, важных строк — с
    маркером тест-статуса (TESTING_FRAMEWORK.md): свежесгенерированное получает [NOT-TESTED],
    проверенное наблюдением переключай на [TESTED: дата · как проверено].
13. Сбор знаний, обдумывание и рефлексия по багам, которые фикшу, в отдельных md файлах по каждому багу в директории bugs. Руководствоваться гайдлайном BUG_FIXING_FRAMEWORK.md при фиксинге багов.
14. Зафиксировать опыт                # после значимого успеха/провала — короткий урок в EXPERIENCE.md (навык /experience)
15. Регулярно перечитывать ключевые гайдлайн документы:
- PHILOSOPHY.md  ← принцип простоты (KISS + Оккам); если застрял — сначала сюда
- AGENT_GUIDE.md
- STATUS.md
- BUG_FIXING_FRAMEWORK.md
При необходимости их редактировать, чтобы повысить эффективность автономной работы ИИ агента над проектом в условиях периодического забывания контекста между отдельными сессиями работы ИИ агента. Ключевые гайдлайн документы нужно обновлять и наполнять, чтобы они были полезны эффективно включиться в работу и контекст проекта даже с пустым начальным контекстом новой сессии работы ИИ агента.
16. В процессе работы хоть немного но писать в чат на естественном языке о том, что сейчас делаю, чтобы давать Кринику понимание того, над чем работаю.
17. Документы от Криника с идеями, багами, фичами и прочим — СНАЧАЛА закоммить оригинал ДОСЛОВНО
    (git add + commit), и только потом, СЛЕДУЮЩИМ коммитом, править опечатки и минимально
    редактировать под структурный ИИ формат для оптимального понимания ИИ агентами (голос Криника и
    каждая мысль сохранены; исходная формулировка остаётся достижимой в истории git — KAIF 1.6).
    После реализации фичи или фикса бага по таким документам, пиши в документах статус, дату реализации.
18. Развил/переделал фичу — синхронно обнови раздел **User Manual** (встроенное руководство, Idea 32 /
    `plans/06`): тезисно, высокоуровнево, для новичка и «под капотом». Это часть definition-of-done, а
    не отдельная задача. См. раздел «User Manual — сопроводительная документация фич».
19. Пишешь в артефакт КРИНИКА?          # (KAIF 2.1) текст, который Криник подписывает или читает как
    свой (README и тексты бренда, GOAL, посты, описания на витринах) → открой портрет его голоса, если
    он снят (`/owner-voice`), и прогони чеклист портрета ПЕРЕД сдачей. Портрета нет, а Криник ВТОРОЙ
    раз отверг текст из-за языка/стиля — предложи снять портрет, а не правь наугад в третий раз.
```

→ **`STATUS.md`** — главный файл состояния проекта. Обновляй его после каждой значимой задачи.

### Контекст-роутер (прогрессивная загрузка) — читай только нужный срез

Не читай все документы «на всякий случай» — это съедает контекст, который мы бережём. Всегда читай
**обязательный минимум**, затем только доки под тип задачи; остальное — по требованию.

| Тип задачи | Читать (минимум сверх обязательного) |
|------------|--------------------------------------|
| **Обязательный минимум (всегда)** | `STATUS.md` · `PHILOSOPHY.md` (свод принципов) · этот роутер · `EXPERIENCE.md` (grep по тегу) |
| Баг | `BUG_FIXING_FRAMEWORK.md` · `bugs/<этот>` · карта (радиус поражения) |
| Тестирование / проверка чего-либо | `TESTING_FRAMEWORK.md` (7 принципов · маркеры `[NOT-TESTED]`/`[TESTED]`) · секции верификации сферы (`.kaif/spheres/programming.md`) |
| Фича / идея | `ideas/<эта>` · `MASTER_PLAN.md` · релевантный `plans/<этот>` |
| Рефактор / правка | `AGENT_GUIDE.md` · обе карты (радиус поражения) |
| Планирование | `MASTER_PLAN.md` · `GOAL.md` · открытый беклог · раздел «Дисциплина планирования» (тяжёлое → `/plan-epic`) |
| Внешняя правда в деле (старая система / чужой API / прод / док вендора) | разведдок в `researches/` — **сначала создай его**, если не существует (шаг 9 чеклиста) |
| Пишем в артефакт Криника (текст, который он подписывает или читает как свой) | портрет голоса Криника, если снят (`/owner-voice`) · стайлгайд артефакта (`/derive-styleguide`) |
| **ЭПИК-фича** (новая подсистема · ≥L · «сделай, чтобы <желаемое поведение>») | **разведка прио-арта** — гуглёж отраслевых стандартов и научных работ → разведдок в `researches/` ДО кода и плана (шаг 9а; правило Криника 2026-07-28) |

Обращайся к срезу (`DOC.md#якорь`), а не перечитывай файл целиком. Обязательный минимум лени
НЕ подлежит: `PHILOSOPHY.md` применяется всегда.

### Разведартефакты — когда у задачи есть внешняя правда (KAIF 1.6)

Четыре типа артефактов живут в `researches/`, каждый заменяет свой вид выдумывания наблюдением
(сессия, которая «помнит» предметку, выдумывает её):

- **Разведдок** (шаг 9 чеклиста) — *описывает*, как внешняя правда работает на самом деле, читая
  живой источник (код старой системы, работающий прод, док вендора) — никогда не по памяти. Первый
  артефакт любой задачи, которая на такую правду опирается; переиспользуется каждой будущей сессией.
- **Разведка ПРИО-АРТА / отраслевого стандарта** (шаг 9а; правило Криника, 2026-07-28) — отвечает на
  вопрос «**как эту задачу уже решило человечество**»: отраслевые золотые стандарты, открытые
  эталонные реализации, научные работы, патенты, принятые по умолчанию параметры. Отличие от обычного
  разведдока: тот описывает КОНКРЕТНУЮ систему, с которой мы стыкуемся, а этот — КЛАСС ЗАДАЧИ вообще.
  Подробности ниже.
- **Канон-карта** — для любого домена с фактологией (мир игры, продукт, бренд, API): таблица
  сущности → их роли → соответствия, **утверждается Криником**. Карта первичнее канона: каждая правка
  сверяется с ней, менять её может ТОЛЬКО Криник, а конфликт текста с картой = стоп и вопрос.
  Ключевые факты карты заслуживают гардов (`BUG_FIXING_FRAMEWORK.md` → Гарды).
- **Инвентарь паритета** — где есть эталон (старая система, конкурент, брендбук): **исчислимый**
  чеклист, одна строка на элемент — `элемент → поведение эталона → есть у нас? → OK/баг`. Правило:
  **нет строки инвентаря — нет кода**; сдача судится ПО СТРОКАМ, а не по впечатлению. Разведдок
  *описывает*; инвентарь *считает* — прочитав описание, сессия всё ещё может выдумать, но со строкой
  она спорить не может.

Рядом, но НЕ четвёртый тип: **портрет голоса Криника** (`/owner-voice`, KAIF 2.1). Он заменяет тот же
род выдумывания наблюдением — берёт СОБСТВЕННЫЕ тексты Криника вместо сессии, «помнящей» его стиль, —
но это КАНОН-документ, который принимает Криник, и подключается он по типу задачи («пишем в артефакт
владельца»), а не по внешней правде. Каркас лежит в `.kaif/_owner-voice-template.md`.

#### 🔬 Разведка прио-арта перед эпик-фичей — ПРАВИЛО КРИНИКА (2026-07-28)

> Дословно: «почти на всё в индустрии есть золотые стандарты и научные работы. давай зафиксируем в
> канон ИИ агента, что перед крупными эпик-фичами, нужно проводить гуглёж, разведку и написание
> research документа».

**Правило.** Перед КРУПНОЙ фичей агент ОБЯЗАН провести веб-разведку отраслевых стандартов и научной
базы и написать разведдок в `researches/`. **Пока документа нет — код и детальный план запрещены.**
Это не «почитать для общего развития», а обязательный вход в задачу, как разведдок по внешней правде.

**Что считать эпиком (триггеры — достаточно ОДНОГО):**
- фича трогает архитектуру или вводит новую подсистему (буферизация, синхронизация, сеть, кодеки,
  многопоточность, хранение, безопасность);
- оценка ≥ L, либо работа растянется больше чем на одну сессию;
- задача сформулирована владельцем как ЖЕЛАЕМОЕ ПОВЕДЕНИЕ («чтобы ничего не терялось», «чтобы эфир не
  умирал»), а не как конкретная правка — значит, способ достижения выбирает агент, и выбирать его
  надо из известных индустрии, а не из головы;
- заводится новый `plans/` уровня фазы или новая `ideas/` крупного масштаба.

**Что обязано быть в документе:**
1. **Как эту задачу решает индустрия** — золотой стандарт(ы) с названием, и КТО именно так делает.
2. **Открытые эталонные реализации** — читаемый исходник, если он есть (он ценнее любой статьи).
3. **Научная база** — классические работы, их алгоритмы и формулы, если задача имеет теорию.
4. **Таблица принятых по умолчанию параметров** — каждый ЧИСЛОМ и СО ССЫЛКОЙ (числа без источника
  запрещены, см. правило трёх дверей в `PHILOSOPHY.md`).
5. **Применимость к нам** — что переносится в наш контекст, а что нет и ПОЧЕМУ. Самая ценная секция:
  честное «нам это не подходит, потому что…» дороже, чем красивый пересказ чужого решения.
6. **Чего мы не знаем** — открытые вопросы и как их закрыть замером/экспериментом.

**Зачем это правило существует (чтобы будущая сессия его не «оптимизировала»):**
- **Агент по умолчанию изобретает.** Получив задачу «сделай, чтобы X», модель проектирует механизм с
  нуля — и получается кустарный аналог того, что отрасль отладила за 20 лет, но без её граничных
  случаев. Гуглёж превращает изобретение в *выбор из известного*.
- **Живой пример этого проекта.** Криник попросил «копить и догонять ускоренным плейбеком, но не
  дёргаться на мелочи — нужен гистерезис». Ровно это и есть **адаптивный jitter buffer с растяжением
  времени** (WebRTC NetEQ и родня) — золотой стандарт видеосвязи с готовыми алгоритмами и
  параметрами. Интуиция владельца совпала с каноном отрасли; не проверив прио-арт, мы бы писали своё
  и переоткрывали чужие грабли. (Разведка заведена 2026-07-28.)
- **Дешевле в разы.** Час поиска экономит недели: то же самое доказал EXP-0028 — весь арсенал
  устойчивости RootEncoder лежал КУПЛЕННЫМ и невключённым, и один греп по именам API это вскрыл.
- **Разведдок переиспользуется вечно.** Он остаётся в `researches/` и работает на каждую будущую
  сессию, у которой контекст пуст.

**Дисциплина поиска (иначе разведка вырождается в пересказ):**
- Порядок доверия источникам: **исходный код и официальные спеки > рецензируемые статьи и патенты >
  инженерные блоги вендоров > вторичные пересказы**; уровень каждого источника помечай в документе.
- **Каждое число — со ссылкой.** Не нашёл — пиши «не найдено», а в код заноси только после замера.
- **Проверяй находки скептиком**, а не доверием: открой URL повторно, сверь цитату и параметр
  (`BUG_FIXING_FRAMEWORK.md` → «Находка — не находка, пока не проверена»).
- Ищи **несколькими разными углами** (эталонная реализация · наука · вендоры · смежная отрасль ·
  инструменты), а не одним запросом: один угол систематически слеп.
- Отдельно спрашивай себя: **в чужом решении та же расстановка сил, что у нас?** (у видеосвязи догон
  делает приёмник, а у стримера — отправитель, и приёмник чужой — это меняет всё).

### Дисциплина исполнения задач — fable-цикл (KAIF 1.5)

Любая нетривиальная задача исполняется по циклу **fable-method** (`.claude/skills/fable-method/`):
классифицируй запрос → определи «готово» → собери свидетельства → реши → действуй хирургически →
проверь НАБЛЮДЕНИЕМ → отчитайся результатом-вперёд. У цикла есть гейты и **принудительные артефакты**
(строки `INTENT:` / `AUTH:` / `TWINS:` / `PENDING:` в точках решений — правила В точках решений,
а не правила списком, — то, что слабые сессии реально соблюдают). Оркестрируемая работа (параллельный
сбор свидетельств, состязательные проверяющие) — `/fable-loop`; в автономных циклах — по элементу
беклога. Когда работа объявляется завершённой (твоя или чужая) — прогони **`/fable-judge`** ДО
предъявления её как «готово»; в лупах и в `/release` judge-проход ОБЯЗАТЕЛЕН, а с KAIF 1.6 — ещё и
**перед КАЖДЫМ push и каждым деплоем** (самая дешёвая точка, где всё ещё откатывается). Навыки вендорены
дословно из [fable-method](https://github.com/Sahir619/fable-method) (Sahir619, MIT) — ритуал
синхронизации в их шапках; роль их доменных адаптеров играет библиотека сфер (`.kaif/spheres/`).

### Дисциплина планирования — лестница задач (`/plan-task` · `/plan-epic`) — KAIF 2.1

Почти на всё в индустрии есть золотые стандарты, лучшие практики и опубликованные исследования (это
же говорит правило Криника про прио-арт, шаг 9а). **Поэтому крупная эпик-фича начинается с веб-разведки
отраслевых практик и разведдока в `researches/`** — это расширение правила «разведка до кода» с
*внешней правды* на *знание индустрии*: состояние отрасли — тоже внешняя правда, а сессия, пропустившая
разведку, переизобретает уже решённое, только хуже.

**Тест тяжести** (проверяемый, не на вкус). Задача ТЯЖЁЛАЯ, когда выполняются **≥2** признака:
трогает ≥3 подсистемы или канон-документа · опирается на внешнюю правду или отраслевой стандарт ·
не влезает в одну сессию · меняет поставляемый состав или публичные контракты · требует решений
уровня Криника. Иначе — обычная.

- **Обычная → `/plan-task`:** ОДИН операционный план — цель, критерии готовности, шаги с чекбоксами,
  проверка наблюдением, риски. Совсем мелкая? План живёт секцией прямо внутри документа идеи/бага.
  Церемония не должна весить больше самой работы.
- **Тяжёлая → `/plan-epic`** — полная лестница, каждая ступень = артефакт:
  1. **Разведка** — отраслевой гуглёж (веб) + локальная разведка + требования проекта, сведённые в
     разведдок в `researches/`. Ни кода, ни мета-плана, пока его нет.
  2. **Мета-план** — один эпик-план в `plans/`: фазы, порядок, гейты, критерии приёмки; развилки
     уровня видения — в `/interview` (работа по незаблокированным фазам идёт параллельно).
  3. **Операционные планы по фазам** — R&D · тестирование · мокапы · разработка · отладка · приёмка.
     Детализируй ТОЛЬКО следующую фазу; план фазы N+1 пишется, когда закрылась фаза N — никогда все
     сразу (к моменту, когда до них дойдёт, они уже фантастика).
  4. **Трассировка** — каждый операционный шаг цитирует якорную строку мета-плана (правило цитирования
     из шага 8 чеклиста); шаг, который нечем заякорить, — дрейф скоупа, пойманный до диффа.

Лестница — не церемония ради церемонии: разведка даёт эпику доказательную базу, мета-план один раз
показывает Кринику всю форму работы, а пофазовые операционные планы — то, что удерживает теряющую
контекст сессию на ПРАВИЛЬНОМ следующем шаге вместо перевывода эпика заново.

### Языки — две аудитории

> 🔴 **ЖЁСТКОЕ ПРАВИЛО КРИНИКА (2026-07-18): в ЧАТЕ с Криником — ТОЛЬКО РУССКИЙ. ВСЕГДА. Ни одного
> английского предложения в ответах чата.** Криник русскоязычный и требует общения на русском. Английский
> в чате = грубое нарушение. (Технические токены — имена файлов, команды, идентификаторы кода, `git`,
> `ffprobe`, названия классов — остаются как есть; но ВСЯ проза, объяснения, отчёты — на русском.)

Канон KAIF 1.5: агентские внутренние доки — **English** (модели читают его надёжнее всего); доки
владельца (`GOAL.md`, `KAIF_FRAMEWORK.md`, README директорий) и все отчёты в чат — **русский**.
**Локальная кастомизация KrinikCam:** исторически ВСЕ доки проекта на русском, и Криник читает
агентские доки тоже (его руководство живёт прямо в этом файле) — поэтому существующие русскоязычные
агентские доки НЕ переводим и продолжаем вести на русском. Канонические маркеры/теги при этом всегда
английские и greppable: `DONE`, `[NOT-TESTED]`, `[TESTED: …]`, `INTENT:`/`AUTH:`/`TWINS:`/`PENDING:`.

### Журнал опыта — `EXPERIENCE.md`

`EXPERIENCE.md` — растущий греп-дружелюбный журнал уроков агента (вынесенная память, что работает, а
что нет). **Вспомни** релевантные записи перед задачей (grep по тегу); **зафиксируй** короткий урок
после любого значимого успеха или провала — в циклах делай и то и другое, не дожидаясь Криника.
Навык: `/experience`. Граница: `bugs/` = док на дефект; `EXPERIENCE.md` = короткие кросс-задачные
уроки уровня подхода (включая успехи). Живой справочник — тегом DONE не помечается.

---

## Цель проекта

KrinikCam — Android-приложение (Kotlin, Compose) для Mikalai Kryvusha (KOT KRINIK, стример).
Подключает USB-вебкамеру через OTG → fullscreen превью → RTMP-стрим на YouTube/Twitch/etc.

---

## Названия и идентификаторы (КАНОН — используй эти, не выдумывай)

| Где | Имя |
|-----|-----|
| **Имя приложения / бренд** (UI, README, доки, тексты) | **KrinikCam** |
| **Краткое имя** | KCam |
| **GitHub репозиторий** | `KrinikCam` → https://github.com/MikalaiKryvusha/KrinikCam |
| **Android package** | `com.kriniks.kcam` |
| **Gradle rootProject.name** | `KrinikCam` |
| **Локальная папка проекта** | `KrinikCam` → `/Users/kryvusha/ai_sandbox/KrinikCam` |

> ⚠️ Старое длинное имя `KRINIK-S-ANDROID-USB-WEB-CAMERA-FOR-STREAMING` (слаг репо) и
> `KRINIKS_ANDROID_USB_WEB_CAMERA_FOR_STREAMING` (старое имя папки) **больше не используются** —
> и репозиторий, и локальная папка переименованы в `KrinikCam` (2026-06-28). В новых текстах/URL/путях
> пиши только `KrinikCam`. Если встретишь старое имя в исторических доках — можно заменить на `KrinikCam`.

---

## Текущее состояние

Единственный источник правды о состоянии — **`STATUS.md`** (живой срез: где мы, что ждёт Криника,
открытые баги, с чего продолжить). Здесь состояние НЕ дублируем (DRY — копии разъезжаются).
Дорожная карта фаз — `MASTER_PLAN.md` §7. Летопись сессий — `PROJECT_HISTORY.md` (append-only; в обязательный минимум чтения НЕ входит — открывай под археологию).

---

## Архитектура — модули

```
:app                    ← главный модуль, зависит от всех feature
:core:common            ← базовые утилиты (нет внешних зависимостей)
:core:ui                ← Compose тема, цвета, типографика
:core:logging           ← KLog + FileLogger (write-to-file + share intent)
:data:profiles          ← Room DB + DataStore (StreamProfile, DeviceProfile)
:feature:usb            ← AndroidUSBCamera, USB hot-plug, UvcPreviewView
:feature:capture        ← DeviceManager (приоритет источника: UVC→phone→none)
:feature:codec          ← CodecScanner (MediaCodecList → DeviceProfile)
:feature:streaming      ← RootEncoder RTMP, StreamViewModel, StreamPlatformsOverlay
```

**ПРАВИЛО:** feature-модули не зависят друг от друга. Только `:app` зависит от всех.
Мосты между feature — через `:app` (например, `LaunchedEffect` в `MainScreen.kt`).

---

## Сборка

```bash
# ВАЖНО: system java_home может вернуть Java 25 (Temurin) — она сломает сборку
export JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"
node tools/build.mjs --no-ui        # headless (для скриптов)
node tools/build.mjs                # с UI прогресс-баром в браузере
```

> ⚠️ **ДВА требования к Java (EXP-0020, 2026-07-25):** `JAVA_HOME=JBR` задаёт JVM ЗАПУСКА Gradle, но
> проект компилится ТУЛЧЕЙНОМ `jvmToolchain(17)` — нужен ОТДЕЛЬНЫЙ **JDK 17**. Android Studio обновил
> встроенный JBR до 21 → если сборка падает `Cannot find a Java installation matching {languageVersion=17}`
> / `No locally installed toolchains match`, значит JDK 17 нет. Поставить без sudo: скачать tar.gz с
> Adoptium (`api.adoptium.net/v3/binary/latest/17/ga/mac/aarch64/jdk/hotspot/normal/eclipse`) и распаковать
> в `~/Library/Java/JavaVirtualMachines/` (стандартное место автодетекта — и для CLI, и для VS Code Gradle).
> Проверка: `/usr/libexec/java_home -V | grep 17`.

Проверить только ошибки:
```bash
export JAVA_HOME="..." && node tools/build.mjs --no-ui 2>&1 | grep "^e:"
```

---

## Ключевые библиотеки и их особенности

### AndroidUSBCamera 3.2.7 (`com.github.jiangdongguo.AndroidUSBCamera:libausbc`)
- Класс камеры: `MultiCameraClient.Camera` (не `CameraUVC` — её нет в 3.x)
- Callback: `com.jiangdg.ausbc.callback.IDeviceConnectCallBack` (не вложен в `MultiCameraClient`)
- Опечатка в API: `onDetachDec` / `onDisConnectDec` (не `Dev`)
- Состояние: `ICameraStateCallBack.State` (OPENED/CLOSED/ERROR), не `Code`
- Открыть превью: `camera.openCamera(textureView, cameraRequest?)`
- `libuvc` нужен как `compileOnly` — в POM он `runtime`, но его тип торчит в публичном интерфейсе

### RootEncoder 2.4.7 (`com.github.pedroSG94.RootEncoder:library`)
- Мы используем `RtmpStream` (`com.pedro.library.rtmp.RtmpStream`) + наш `CompositorVideoSource`
  (единственный базовый источник, Phase 3 — камера идёт СЛОЕМ композитора),
  а НЕ `RtmpCamera1` (RtmpCamera1 открывает Camera1/Camera2 API → конфликт с USB UVC → краш)
- Интерфейс: `com.pedro.common.ConnectChecker`
- Коллбэки без суффикса: `onConnectionSuccess()`, `onConnectionFailed(reason)`, etc.
- ⚠️ **`RtmpStream` (StreamBase).prepareVideo: `(width, height, bitrate, fps=30, iFrameInterval=2, rotation=0, ...)`**
  — BITRATE 3-й параметр, FPS 4-й! НЕ путать со старым `RtmpCamera1.prepareVideo(w,h,fps,bitrate,rotation)`.
  Перепутанный порядок (fps↔bitrate) = энкодер с битрейтом ~30 бит/с = пустой видеотрек =
  YouTube получает только звук = Broken Pipe через 15с. **(Баг 02, исправлен 2026-06-28.)**
- ⚠️ **Ориентация (Phase 3):** `prepareVideo(rotation=0)` внутри зовёт
  `glInterface.setCameraOrientation(270)` (поворот входа для телефонных сенсоров). У нас ВСЕ
  повороты делает СВОЙ композитор (глобальный поворот холста + поворот слоя, interview_006) →
  после `prepareVideo` ВСЕГДА `setCameraOrientation(0)`, библиотека ничего не крутит. **(Баг 02 A.)**
- `setCustomImageToStream()` удалён → используй `glInterface.setFilter(BaseFilterRender)` для статик-кадра
- GL-поток рисует в encoder через `streamOrientation` (`setStreamRotation`), в preview — через
  `previewOrientation` (`setPreviewRotation`); `setCameraOrientation` крутит общий вход камеры (оба)

### Compose Material3
- `ExposedDropdownMenuBox` и `menuAnchor()` — экспериментальные, нужен `@OptIn(ExperimentalMaterial3Api::class)`

---

## Интервью перед каждой новой фичей

Перед реализацией каждой Phase — проводи интервью с Krinik по шаблону:
`interviews/interview_NNN_<phase_name>.md`

Задай конкретные закрытые вопросы (A/B/C варианты), получи ответы, зафиксируй решения, потом пиши код. Никогда не делай UI/UX решения без подтверждения.

**МЕСТО ВОПРОСОВ — ЖЁСТКОЕ ПРАВИЛО (KAIF 2.1).** Всё, что агент хочет ОТ Криника — развилка, вычитка,
одобрение, ответ, — живёт ТОЛЬКО в `interviews/` (или в явно названном документе-очереди решений), и
никогда в хвосте плана, разведдока или бага. Единственное исключение остаётся прежним: ОДИН точечный
вопрос уровня задачи прямо в чате (см. ниже). Полевой факт: это правило нарушают даже агенты, которые
его ЗНАЮТ — чат в моменте дешевле, — поэтому проект, принявший практику, держит механический гард
(«нет висящих вопросов вне `interviews/`; у каждого интервью есть статус»), а практика считается
ПРИНЯТОЙ только тогда, когда в ритуале записана исполняемая команда, показывающая нарушения («покажи
все неотвеченные интервью»). У нас такая команда — **`node tools/owner.mjs guard`** (exit 1, если есть
висящее): она печатает и неотвеченные интервью, и вопросы-сироты ВНЕ `interviews/`. В поле подобный гард
вскрыл два вопроса, висевших 5 и 13 дней, которых никто не видел; **у нас первый же прогон вскрыл
четыре** — и два из них тоже висели 13 дней (`ideas/38` + `researches/restream_server.md`, 2026-07-18).
Ложные хиты снимай ЯВНЫМ исключением с причиной в `tools/owner-guard-exceptions.json`, а не ослаблением
команды.

> ⚠️ **Ловушка ПОДСТРОКИ — гард умеет молча ПРОПУСКАТЬ нарушение** (поймано 2026-07-31).
> `grep -L` ищет ПОДСТРОКУ, а не слово: свежее интервью, ждавшее ответа, стало невидимым для гарда
> просто потому, что в прозе документа встретилось слово «за**крыт**ие» (совпало с `ЗАКРЫТ`).
> Пустой вывод гарда — НЕ доказательство порядка, пока не проверено, что гард вообще способен
> покраснеть. **`mechanized: tools/owner.mjs`** (правило 2.1 «повторившийся урок обязан стать
> исполняемым»): прежний грепный гард заменён на инструмент, который (а) судит СТРУКТУРНО — по пустому
> слоту ответа, а не по слову в статусе, (б) сравнивает слова с границами `\p{L}`, а не подстроки,
> (в) имеет `node tools/owner.mjs selftest`, где каждому гарду скармливается ровно его дефект —
> включая «за**крыт**ие» и окно тихих часов через полночь.

Сила ответа НЕ зависит от транспорта: ответ на HTML-странице = ответ в md = ответ в чате, и фиксируется
он в md-документ с `by` (кто решил) и `at` (когда).

**Контур согласований развёрнут у нас как `tools/owner.mjs`** (навык `/owner-reviews`, планы
`plans/22` и `plans/23`). Рабочий цикл агента:

```bash
node tools/owner.mjs guard                   # ← в ритуалы: ВСЕ висящие вопросы + долг числом
node tools/owner.mjs ask interviews/<док>.md # страница + окно-приложение + сигнал; ЛЮБАЯ запись БУДИТ агента
                                            # ⏳ ждёт БЕСКОНЕЧНО (I9) → запускать ФОНОМ, зов повторяется раз в час
node tools/owner.mjs gate interviews/<док>.md --q В1   # ПЕРЕД зависимой работой; fail-closed
node tools/owner.mjs queue interviews/<док>.md         # в автономных циклах: копить, НЕ вставать
node tools/owner.mjs inbox [--no-serve]                # один зов на всю пачку (--no-serve = собрать и выйти)
node tools/owner.mjs selftest && node tools/owner.mjs verify   # ядро + QA ЖИВЫМ браузером
```

Четыре вещи, которые контур гарантирует и о которых нельзя договариваться «по-джентльменски»:
ответ пишется в ТРИ места (исходный md · `interviews/decisions/<док>.decision.json` · копия в архиве
с `by`/`at`), причём **уже написанное Криником не перезаписывается никогда** — повторный ответ ложится
датированным уточнением; одобрение привязано к **sha256 тела вопроса** — текст изменился после ответа,
и ответ недействителен; **гейт fail-closed** — нет решения, отклонено или текст уехал → выход не-нулём;
и **сохранение БУДИТ ждущего** (инвариант I8, 2026-08-01): любая запись завершает контур, а поднять
страницу заново, если что-то осталось, — обязанность АГЕНТА, а не Криника. Ответ НИКОГДА не
самоодобряется по таймауту.

> 🔴 **Почему I8 важнее, чем кажется.** Агент узнаёт о событии по ЗАВЕРШЕНИЮ процесса, который сам
> запустил, — значит долгоживущая страница и побудка взаимоисключающи, и выбирать надо побудку.
> В соседнем проекте контур принимал ответ и НЕ дёргал агента: владелец отвечал, ответ ложился на
> диск, и за ним никто не приходил («я дал тебе там ответы, но оно не дёрнуло тебя»). Ни одна
> проверка этого не видела — все проверяли путь ТУДА, к человеку.

> ⏳ **ОЖИДАНИЕ БЕСКОНЕЧНО (инвариант I9, правка Криника 2026-08-02).** У контура НЕТ таймаута:
> он ждёт ответа сутками, потому что срок — единственная смерть, при которой решение теряется, а
> не записывается (вопрос, заданный в неудачный час, истекал в пустоту, и агент возвращался к нему
> сам). I8 это не отменяет: контур не смеет пережить свою НАДОБНОСТЬ, а пока ответа нет — надобность
> есть. Закрывают контур пять честных способов: записанный ответ · кнопка «Закрыть» на странице ·
> закрытая страница (пульс) · выгон новым `ask` по тому же документу · `node tools/owner.mjs stop`
> в `/end-chat`. Две платы за бесконечность, обе обязательны: **сторож пульса отличает закрытую
> страницу от УСЫПЛЁННОЙ МАШИНЫ** (иначе ноутбук, закрытый на ночь, казнит живую страницу, и утреннее
> «Записать» упрётся в мёртвый сервер) и **зов ПОВТОРЯЕТСЯ раз в час**, в тихие часы молча (I6).
> Практика агента: `ask` запускается **фоновым процессом** — ждать его в переднем плане теперь
> значит блокироваться до ответа Криника; побудкой служит само завершение процесса (I8).

**КЛАСС ВКУСА — критерий, который агент не умеет мерить (KAIF 2.1).** Канон покрывает измеримые
критерии (проверка наблюдением, `TESTING_FRAMEWORK.md`) и развилки видения (`/interview`), а между
ними лежит третий класс: критерий приёмки — ПЕРЦЕПТИВНОЕ прилагательное (красиво, естественно,
приятно, читаемо, «звучит как надо»), и его видно грепом прямо в формулировке просьбы. Здесь агент
НЕ выносит вердикт: он **делает МОКАП и заводит homework** — найти живых лучших кандидатов → быстро
смакетить их НА НАШЕМ СОБСТВЕННОМ материале → отдать Кринику АРТЕФАКТ для восприятия (никогда не
ссылку и не чужой бенчмарк: человеку, судящему звук, нужен звук, а не оценка; в поле обе предложенные
демо-ссылки оказались мёртвыми) → записать вердикт как канон (вкус Криника агент не переспоривает).
Контракт сравнения: все кандидаты на ОДНОМ материале, метки слепые, ключ хранится рядом. В документе
homework — два постоянных поля: «*что готово посмотреть/послушать прямо сейчас*» (пути к артефактам) и
«*уже вынесенные вердикты*» (чтобы ни один вердикт не спрашивался дважды). У нас этот класс уже
работал вживую: выбор мокапа плашки слейта (4 варианта → Криник выбрал C).

**РАЗРЕШЕНИЕ НА ДЕЙСТВИЕ ≠ АВТОРСТВО ИДЕНТИЧНОСТИ (KAIF 2.1).** Широкое «делай, не спрашивай» снимает
ТРЕНИЕ подтверждений на действиях, но никогда не передаёт авторство ИДЕНТИЧНОСТИ: нейминг — кодовые
имена релизов, названия продукта и фич, слоганы, любая брендовая строка, которую человек читает первой
(тест: читается первой и говорит, как продукт себя подаёт). Идентичность НИКОГДА не решение агента,
при любой широте одобрения — широкое «да» тихо маскирует вопрос вкуса под технический пустяк выкладки,
ровно так и случился полевой инцидент. Правильный ход под широким одобрением: сделать всё остальное и
задать ОДИН точечный вопрос про имя. Запасной: выкатить под нейтральным фактическим заголовком —
никогда под именем-заглушкой (это всё равно имя, которое кому-то придётся раз-решать). У каждого
выкаченного имени есть артефакт-источник (*кто · где сказал · когда*), а ошибку бренда исправляет
только владелец. (`/release` держит это в шаге 0; `/fable-judge` охотится на выкаченное имя без
артефакта-источника.)

**Write-gate на канон-артефакты Криника (KAIF 1.6)** — правила, лор, тексты бренда, продуктовые доки,
всё, где слово Криника И ЕСТЬ содержимое: **новые сущности** (механики, факты, решения) входят только
через черновик Кринику (интервью/чат) и его «да» — никогда сразу в канон; **механические правки** под
уже принятыми решениями (переименования, арифметика, ссылки, нотация) идут сразу, но остаются
видимыми, пока Криник их не просмотрел. Двухступенчатый контроль: сначала *намерение* (до записи),
потом *текст* (вычитка Криником). Ничто не растворяется в каноне молча, а коридор для механической
работы остаётся широким (см. правило трёх дверей в `PHILOSOPHY.md`).

**Провенанс-марки — `[AI]…[/AI]` / `[AI-ed]…[/AI-ed]`** (канонические английские строки,
греп-дружелюбные, как `[NOT-TESTED]`; KAIF 1.6). Всё, что ИИ пишет в канон-артефакты Криника, несёт
видимую парную пометку: `[AI]…[/AI]` — написано ИИ; `[AI-ed]…[/AI-ed]` — текст Криника, отредактированный
ИИ. **Пометка И ЕСТЬ очередь на приёмку:** снимает её только слово Криника («глава принята») — агент
НИКОГДА не снимает пометку со своего текста сам. Один механизм покупает три вещи: *доверие* (Криник
видит, что его, а что сгенерировано — вычитка становится сканом пометок, а не перечитыванием всего),
*откат* (непринятый блок безопасно удалить) и *безопасность будущих агентов* (никогда не принимать
непринятый `[AI]`-текст за канон Криника). Проверка греп-дешёвая: ИИ-текст в канон-артефакте без
пометки — или пометка, снятая без слова Криника, — фрод, на который охотится `/fable-judge`. Помечай
в момент записи. (Агентские рабочие доки — STATUS, карты, bugs/, plans/ — не канон владельца; их
маркировать не нужно.)

**Проверка провенанса МЕХАНИЗИРОВАНА (KAIF 2.0, модуль поставлен, подключение — опция).** Объяви канон
в `.kaif/kaif.json` (`"canonArtifacts": ["…"]`) и повесь `node .kaif/tools/kaif-provenance.mjs check`
на свои гейты — он проверяет парность пометок и то, что пометки стоят только в объявленном каноне;
`report` перечисляет блоки, ждущие приёмки; `accept <файл>` снимает пометки в реестр приёмки и несёт
ТОЛЬКО слово Криника. Рядом — `node .kaif/tools/kaif-canon-lint.mjs check` (запрещённые формулировки и
охраняемые строки; `selftest` доказывает, что каждый гард способен сработать). У KrinikCam канон-артефакт
пока один класс — `GOAL.md` и тексты бренда/README; подключать гейт есть смысл, когда их станет больше.

**Режимы строгости — медленно можно, если это видно (KAIF 2.0).** Называй режим, в котором пишешь:
- **draft** — быстро, ВНЕ канона Криника: наброски, разведзаписи, идеи, спайки. Ни стайлгайда, ни
  пометок, ни линтера канона — дёшево по замыслу. Черновик никогда не становится каноном молча.
- **canon** — всё, что входит в канон-артефакты Криника, идёт полным конвейером: утверждённый стайлгайд
  (`/derive-styleguide`) → запись с провенанс-марками → зелёный линтер канона → зелёный гейт провенанса
  → приёмка Криником.
Разделение по моделям (помечай в навыках и пунктах задач): механические шаги — прогон линтеров и гейтов,
переименования, арифметика, пересинхронизация — любая модель; шаги суждения — вывод стайлгайда,
формулировки канона, решения о приёмке — только сильная модель. Всё машинно-проверяемое проверяется
КОДОМ; за LLM остаётся суждение.

---

## User Manual — сопроводительная документация фич (Idea 32)

В приложении есть встроенный раздел **User Manual** (Settings → «Руководство»): описывает функционал
KrinikCam для двух читателей — новичка («как пользоваться») и прошаренного («как под капотом»,
ключевые архитектурные решения без сильной детализации). План/каркас — `plans/06_user_manual.md`.

**КОНВЕНЦИЯ (обязательна):** развивая ИЛИ переделывая ЛЮБУЮ фичу — синхронно правь соответствующий
раздел User Manual, держи его в соответствии с текущим прогрессом. Тезисно, высокоуровнево, но
раздел ВСЕГДА есть (глубокую детализацию наращиваем со временем). Правка фичи без обновления
руководства — незавершённая задача. Учитывай будущую локализацию (тексты — ресурсы, не хардкод).

**СТИЛЬ (указание Криника 2026-07-07):** User Manual пишется **нейтральным, академическим языком в духе
энциклопедии/Википедии** — описательно, в третьем лице, без сленга и жаргона проекта («лимон», «толстая
команда» и т.п.), без панибратских императивов. Технические термины при первом употреблении поясняются.
Цель — чтобы текст был понятен любому читателю, а не только команде. (Секции 10–11 уже приведены к стилю;
секции 1–9 — привести по мере правок.)

---

## Меню «Для разработчиков» (dev-функционал) — Idea 07

В приложении есть СКРЫТЫЙ экран **Developer**: открывается **лонг-тапом** по строке «KrinikCam» в
Settings → About. Доступен в ЛЮБОЙ сборке (release == debug; никакой «магии» только в debug).

**Правило:** любой отладочный/разработческий функционал выноси СЮДА (тумблеры в стиле приложения +
кнопка [i] с описанием), а НЕ делай его debug-only через `BuildConfig.DEBUG`. Так release и debug
не расходятся по фичам — кто знает про лонг-тап, тот найдёт; остальные не догадаются.

Файлы: `dev/DevSettings.kt` (персист через SharedPreferences), `ui/screens/DevMenuScreen.kt` (UI),
роут `developer` в `NavGraph.kt`. Первый пункт — тумблер «Вращение по ADB» (см. `ui.mjs orient`).

---

## Git-воркфлоу (решение Криника 2026-06-30)

**Работаем ТОЛЬКО в `main`. Feature-ветки НЕ заводим.** Криник смотрит дерево файлов и теряется при
переключении веток (файлы «пропадают»). Поэтому:
- Вся работа коммитится в `main`, инкрементально и часто.
- Нужно откатиться — смотрим историю git (`git log`, `git revert <hash>`, `git checkout <hash> -- file`),
  а не отдельные ветки.
- Допустимо WIP в `main` (Криник принял этот компромисс ради простоты и видимости всех файлов).
- Тестируем на харнесе (вирт.камера + стрим в файл), прежде чем считать готовым.

> Сверка с **гейтом авторизации** fable-method: этот развёрнутый гайд И ЕСТЬ постоянная авторизация
> Криника на рутинные коммиты/пуши по политике выше (работаем в `main`, коммитим инкрементально и
> часто). Всё сверх неё — релизы, деплои, внешние публикации/отправки, force-push, удаление общих
> данных — по-прежнему требует процитированных слов Криника (строка `AUTH:`).

**Неотменяемая git-гигиена (KAIF 1.6; каждое правило существует потому, что его нарушение обожгло
реальный проект):**

- **`git diff --stat` перед каждым коммитом.** Всё в диффе, что ты не намеревался менять, — СТОП и
  сначала объясни. Включая диффы, которые сгенерировали *твои инструменты* (lock-файлы, манифесты,
  форматтеры): агент доверяет своим инструментам ещё слепее, чем себе, — читай такие диффы построчно.
- **Сначала ignore, потом инструмент.** Любой новый инструмент, экспорт, дамп, ключ или бинарь входит
  в проект ТОЛЬКО после того, как существует его строка в `.gitignore`. Секрет, пойманный гейтом, —
  успех процедуры; секрет, пойманный Криником, — провал фреймворка.
- **Первоисточники Криника неприкосновенны.** Документ от Криника коммитится дословно ДО любой правки
  (шаг 17 чеклиста) — никогда не «улучшай» оригинал, который ещё не лежит безопасно в истории.

## Коммиты

Стиль: `feat:`, `fix:`, `docs:`, `refactor:`, `ci:` + одна строка что сделано.
Тег при завершении фазы: `v0.N` (Phase 0 = v0.1, Phase 1 = v0.2, ...).

**Коммит, трогающий файлы тестов, несёт блок обоснования (KAIF 1.6):** *почему этот тест изменён и
что он теперь стережёт*. Правка теста без блока — фрод ПО УМОЛЧАНИЮ (`/fable-judge` охотится ровно на
это — тихая подгонка тестов под новое поведение и есть самый задокументированный провал агентов).
Изменив поведение, ответь ещё на один вопрос: могли ли старые тесты теперь проходить по НЕВЕРНОЙ
причине? Если да — перестрой фикстуры так, чтобы каждый тест стерёг то, что заявляет, и скажи об
этом в коммите.

## Гигиена документов и текста (KAIF 2.1 — правила, оплаченные полем)

**Каждый документ отвечает на СВОЙ вопрос — и берёт форму у своей родни.** README: *«что это и как
этим пользоваться»* (продукт, настоящее время). Release notes: *«что изменилось в ЭТОЙ версии, стоит
ли обновляться»* (строго дельта; всё общее — ССЫЛКА на README; механическая проверка: абзац, который
можно без правок вставить в README, принадлежит README). `STATUS.md`: *«где мы сейчас»*.
`EXPERIENCE.md` и директории знаний: *«почему / как оно прошло»*. Обновляешь README — опирайся на
текущий README и на другие витрины Криника (почерк витрины его, не агента); обновляешь release notes —
опирайся на ПРЕДЫДУЩИЕ notes этого же проекта (`gh release view <prev>`). Смешение этих зон — дефект,
а не вопрос стиля.

**ТЕКСТ ХОДИТ ФАЙЛАМИ, А НЕ АРГУМЕНТАМИ КОМАНДНОЙ СТРОКИ.** Скармливаешь инструменту кириллицу (или
любой не-ASCII), типографские кавычки, эмодзи, многострочный текст, markdown, JSON? Запиши UTF-8 файл
и передай ПУТЬ. Никаких `python -c "…текст…"`, никаких `-m "…"`, никаких `echo "…" > file` с не-ASCII.
Один класс, четыре непохожих лица — узнавай ПО СИМПТОМУ:

1. `python -c` + не-ASCII → `SyntaxError: (unicode error)` — или ХУЖЕ, молча записанная в файл
   кракозябра (кодировка консоли портит аргумент раньше, чем программа его увидит);
2. обратные кавычки внутри двойных → подстановка команд шелла съедает куски текста, печатает «ok»,
   а в документе появляются ДЫРЫ — без единой ошибки; ловится только перечитыванием результата;
3. Windows-пути внутри строк → `truncated \uXXXX escape` (`\w`, `\u` читаются как escape-последовательности);
4. разные шеллы — разные миры: знай, В КАКОМ шелле находишься (у нас zsh на macOS), а прежде чем
   запускать чужой скрипт — проверь, во что реально разрешаются `tar`/`curl`/`find` в текущем PATH.

Спутники правила: после ЛЮБОЙ машинной правки не-ASCII документа — ПЕРЕЧИТАЙ РЕЗУЛЬТАТ (лицо 2 иначе
не ловится); для правки текста предпочитай файловые инструменты (Write/Edit) шеллу — шелл запускает
процессы, а не переносит содержимое.

**Граница правила.** Оно про СОДЕРЖИМОЕ документов и канона, а не про короткие однострочные ярлыки:
сообщение коммита в `node tools/commit.mjs "fix: …"` — законное исключение (одна строка, мы её сразу
видим в `git log`). Как только сообщение становится многострочным — передавай его через `$'…\n…'`
или `git commit -F <файл>`. Всё, что длиннее строки или уходит В ДОКУМЕНТ, ходит файлом.

**Реестр пар «источник ↔ зеркало».** Самые дорогие полевые дефекты — не сложный код, а РАСХОЖДЕНИЕ
между источником правды и его зеркалом: манифест деплоя, приколотый к старой версии движка, пока в
проде новая; комментарий, противоречащий файлу, который описывает; контракт продюсера, разъехавшийся
с консьюмером. Слабая сессия обновляет ту сторону, которую ВИДИТ, и не знает, что есть вторая. Держи
лёгкий реестр — таблицу, строка на пару: `источник → зеркало(а) → однострочная команда проверки`.
`/end-chat` и `/release` прогоняют команды реестра и останавливаются на расхождении; любое новое
«X должно совпадать с Y» попадает в реестр в день своего рождения. Расхождение ловится ТОЛЬКО
проверкой ПАР — никогда чтением одного файла, каким бы внимательным оно ни было.

Пары этого проекта (проверяй при касании любой из сторон):

| Источник правды | Зеркало(а) | Команда проверки |
|---|---|---|
| `.claude/skills/` (канон навыков) | `.agents/`, `.grok/`, `.cline/`, `.roo/commands/` | `node .kaif/kaif-core.mjs check` |
| `.kaif/kaif.json` (версия KAIF) | `README.md` (бейдж), `CLAUDE.md`, `AGENTS.md`, `KAIF_FRAMEWORK.md` | `node -e 'const v=require("./.kaif/kaif.json").version;for(const f of ["README.md","CLAUDE.md","AGENTS.md","KAIF_FRAMEWORK.md"])if(!require("fs").readFileSync(f,"utf8").includes("KAIF "+v))console.log("отстал: "+f)'` |
| `MainActivity` CMD-receiver (набор действий) | `tools/ui.mjs cmd`, таблицы в `AGENT_GUIDE.md` / `tools/UI_AUTOMATION_GUIDE.md` | контракт-шаг в `node tools/smoke.mjs` (bug 39) |
| `app/build.gradle.kts` (versionName/Code) | `README.md`, `README.pdf`, бейдж в UI | `node tools/release.mjs` (пречек) |

## Push / аутентификация в GitHub (git + gh)

Remote — HTTPS (`https://github.com/MikalaiKryvusha/KrinikCam.git`). `gh` CLI авторизован
(аккаунт `MikalaiKryvusha`, токен в keyring, scopes `repo`/`workflow`/`gist`/`read:org`), но
**git напрямую кредов не видит** и в неинтерактивной среде падает с
`fatal: could not read Username for 'https://github.com': Device not configured`.

**Фикс (выполнен 2026-06-30, разовый):**
```bash
gh auth setup-git    # прописывает gh как git credential-helper для github.com
git push origin main # дальше пушит без интерактива, токеном gh
```
- Это НЕ про 2FA: двухфакторка HTTPS-пуш не чинит и не ломает — нужен именно токен/credential-helper.
- Если снова `could not read Username` — повтори `gh auth setup-git`; проверь `gh auth status`.
- При `non-fast-forward` — `git pull --rebase`, затем повтор `git push`.
- `node tools/commit.mjs "..."` бампит build-номер, коммитит и пушит (использует тот же git/gh).

---

## Инструменты проекта

| Команда | Что делает |
|---------|------------|
| `node tools/build.mjs` | сборка с браузерным UI для того, чтобы Криник видел процесс сборки |
| `node tools/build.mjs --no-ui` | headless сборка |
| `node tools/smoke.mjs [--skip-build] [--portrait\|--rotation N] [--pip] [--duration N]` | **смоук-тест пайплайна одной кнопкой**: build→install→harness→запись→ffprobe-ассерты (валидный MP4 + размер кадра под поворот + кадры) → PASS/FAIL (exit 0/1). Прогонять после правок видеопайплайна вместо ручной цепочки |
| `node tools/rtmp-server.mjs start\|stop\|status\|url\|freeze\|thaw` | **локальный RTMP-полигон** (MediaMTX): приём настоящего эфира без YouTube и Криника. `freeze`/`thaw` (SIGSTOP/SIGCONT) моделируют **«зомби-сервер» K5**: соединение живо, байты не читаются → `write()` клиента блокируется, ошибка НЕ приходит вовсе. Это единственный дешёвый способ воспроизвести класс отказа, в котором приложение слепо минуты (`plans/21` Ш0/B) |
| `node tools/graphics/render.mjs --input x.svg --output x.png --width N --height N` | SVG→PNG |
| `node tools/graphics/batch.mjs --input x.svg --android` | SVG→Android mipmap set |
| `node tools/ui.mjs cmd <action> [arg]` | **⭐ ТОЛСТАЯ debug-команда (Idea 22), минует UI** — `virtual-camera on\|off`, `stream-to-file on\|off`, `go-live [1080\|2160]`, `go-live-rtmp <url1[,url2…]>` (настоящий RTMP-мультистрим на полигон), `stop`, `photo`, `set-rotation 0\|90\|180\|270` (глобальный поворот ХОЛСТА над сценой, interview_006), `add-overlay [id]`, `set-transform <id> <scale> <cx> <cy> [alpha] [rotation]` (трансформа слоя + поворот содержимого), `select-source front\|rear\|uvc\|virtual\|none\|builtin <id>`, `device-camera front\|back\|off` (Idea 24), `toggle-layer\|layer-up\|layer-down <id>`, `gesture-drag\|scale\|rotate <id> …` и `gesture-pinch in\|out [frac]` / `gesture-twist <deg> [radiusFrac]` (двухпальцевый мультитач), `rotation-mode on\|off`. Протокол аргументов bug 39: хвост клеится ЗАПЯТОЙ, ресивер парсит `[,\s]+`; контракт-шаг в smoke.mjs ловит рассинхрон. Надёжно загнать приложение в нужное состояние на харнесе |
| `node tools/ui.mjs dump` | dump DOM-иерархии с точными координатами (тонкий слой — для проверки самого UI) |
| `node tools/ui.mjs tap <query>` | найти элемент по тексту и тапнуть (без скриншота!) |
| `node tools/ui.mjs find <query>` | найти все элементы, совпадающие с query |
| `node tools/ui.mjs swipe <up\|down\|left\|right> [frac] [ms]` | жест свайпа для прокрутки экранов (ориентационно-корректный) |
| `node tools/ui.mjs orient <auto\|portrait\|landscape\|0..3>` | вращать/фиксировать ориентацию девайса по ADB (для теста ориентаций без рук) |
| `node tools/ui.mjs allow [--once]` | **сам одобрить** системный диалог разрешений (CAMERA/микрофон/USB) — без Криника |
| `node tools/ui.mjs kill [debug\|release\|both]` | force-stop сборок; по умолчанию обе → освобождает камеру |
| `node tools/ui.mjs start\|restart [debug\|release]` | запуск/перезапуск нужной сборки |
| `node tools/ui.mjs screen [out.jpg]` | скриншот → `tools/screenshots/adb_screen.jpg` (сжатый JPEG q80, full-res; папка gitignored) |
| `node tools/adb.mjs screen` | скриншот → `tools/screenshots/adb_screen.jpg` (тоже JPEG q80, через `sharp`) |
| `node tools/device.mjs list\|selftest` | **кого харнес считает целью** (парк с 2026-08-02 — ДВА устройства). Один общий разрешатель для `ui.mjs`/`smoke.mjs`/`adb.mjs`: `ADB_DEVICE` → одно подключённое → **при НЕСКОЛЬКИХ отказывается гадать** и печатает готовые строки `ADB_DEVICE=<serial>`. Раньше копии этой логики жили в двух файлах и выбирали независимо — smoke мог мерить одно железо, а `ui.mjs` тыкать в другое, и никто бы не заметил. `selftest` (6 проверок) скармливает гарду ровно его дефект — проверено и на СЛОМАННОЙ версии (краснеет) |
| `node tools/adb.mjs tap <x> <y>` | тап по координатам (устарело — используй ui.mjs tap) |
| `node tools/adb.mjs logcat [tag] [lines]` | дамп logcat с устройства |
| `node tools/avd.mjs create\|start\|stop\|status\|smoke` | **эмулятор как тест-девайс (Idea 28)**: планшет недоступен → headless AVD (Pixel_7_Pro_Android_15; KCAM_AVD=<имя> для другого) + smoke на нём (функциональный порог --min-fps 3; софт-GPU медленный). Харнес: `ADB_DEVICE=emulator-5554 node tools/ui.mjs …` |
| `node tools/adb.mjs install` | установить debug APK |
| `node tools/adb.mjs start` | запустить MainActivity |
| `node tools/adb.mjs stop` | force-stop приложения |
| `node tools/owner.mjs guard\|baseline\|ask\|gate\|queue\|inbox\|selftest\|verify` | **контур согласований «агент ↔ Криник»** (`/owner-reviews`, `plans/22`+`plans/23`): `guard` — ВСЕ висящие вопросы + число унаследованного долга (в ритуалы!; краснеет только на НОВЫХ нарушениях и на ПРОТУХШЕМ статусе); `baseline` — снять/пересобрать базовую линию долга; `ask` — открыть интервью окном-приложением, позвать тремя писками и голосом, записать ответ в три места и **разбудить агента** (I8); `gate` — fail-closed перед зависимой работой; `queue`+`inbox` — копить в лупах и звать раз на пачку (`--no-serve` = собрать и выйти); `selftest` — ядро (58 проверок); `verify` — **QA ЖИВЫМ браузером** через Chrome+CDP (57 проверок: снятие выбора, теги, цвета, побудка). Настройки — `tools/owner.config.json`; спецификация сборки — `.claude/skills/owner-reviews/references/build-spec.md` |
| `python tools/measure_stretch.py <img.png> [x y w h]` | **ОБЪЕКТИВНО измерить линейное искажение (растяг/сжатие)** кадра по круглому объекту: fitEllipse → ratio осей + угол. Для оценки геометрии видео — НЕ на глаз (см. правило ниже). venv-настройка в шапке скрипта |

> **Полное руководство по UI автоматизации:** [tools/UI_AUTOMATION_GUIDE.md](tools/UI_AUTOMATION_GUIDE.md)

### 📐 Правило: линейные искажения кадра — ИЗМЕРЯТЬ инструментом, НЕ на глаз (2026-07-07, Криник)

Агент **не умеет надёжно оценивать «на глаз», растянут/сжат ли кадр** — особенно на повёрнутых,
зеркальных кадрах и кривых предметах (проверено на bug 19: агент несколько раз ошибочно счёл растянутый
кадр «ровным»). Поэтому **когда речь про геометрию/пропорции видео (растяг, сжатие, аспект, искажение):**
1. Сними кадр (`adb exec-out screencap -p > f.png`), наведи камеру на **круглый предмет фас**.
2. Померь: `python tools/measure_stretch.py f.png [кроп]` → `ratio = major/minor` (коэффициент растяга),
   `angle` (0°/90° = осевой пайплайн-растяг; диагональ = обычно физ. наклон, не баг).
3. Несколько кругов с ОДНИМ ratio+angle = систематическое искажение пайплайна (а не перспектива).
4. **Коэффициент искажения обязан быть логически объясним** (напр. 1.67 = (16:9÷4:3)²) — это указывает
   на ПРИЧИНУ; не подгоняй магические числа, а устраняй причину. Верификация фикса — тем же замером (→~1.0).

### 🧭 Правило автоматизации (Idea 22) — как агенту действовать в приложении

Хочешь что-то сделать/проверить в приложении — выбирай слой инструмента СВЕРХУ ВНИЗ:
1. **Толстый `ui.mjs cmd <action>`** (debug-broadcast) — загнать в нужное СОСТОЯНИЕ надёжно/быстро,
   минуя навигацию. Это путь по умолчанию для тестов на харнесе.
2. **Тонкий `dump`/`tap`/`swipe`/`longpress`** — только если (а) проверяешь САМ UI, или
   (б) толстой команды для действия ещё НЕТ.
3. **После тонкого пути — ДОРАБОТАЙ инструмент:** добавь новую `cmd`-команду (action в CMD-receiver
   `MainActivity` + ветка в `ui.mjs cmd` + строка в таблицах AGENT_GUIDE/UI_AUTOMATION_GUIDE), чтобы
   впредь это делалось толстым слоем. Инструмент — живой, активно расширяем и документируем.

CMD-receiver — **DEBUG-only** (`BuildConfig.DEBUG`), action `com.kriniks.kcam.CMD`. Камеры на ночь
нет → харнес = виртуалка + запись в файл + `ffprobe`/кадры.

### UI Automation — быстрый старт

```bash
# ПРАВИЛО: никогда не угадывать координаты из скриншота — всегда использовать ui.mjs dump

# Что сейчас на экране?
node tools/ui.mjs dump

# Нажать кнопку
node tools/ui.mjs tap "go live"
node tools/ui.mjs tap "menu"
node tools/ui.mjs tap "youtube"

# Найти без тапа (проверить наличие элемента)
node tools/ui.mjs find "platforms"
```

### ADB workflow для отладки на девайсе

```bash
# 1. Видеть экран (DOM, точные координаты)
node tools/ui.mjs dump         # предпочтительно
node tools/adb.mjs screen      # резерв, если нужно визуально

# 2. Нажать кнопку по тексту
node tools/ui.mjs tap "go live"

# 3. Запустить и поймать краш
node tools/adb.mjs stop && node tools/adb.mjs start && sleep 3 && node tools/adb.mjs logcat AndroidRuntime 40

# 4. Пересобрать и переустановить
export JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"
node tools/build.mjs --no-ui && node tools/adb.mjs install && node tools/adb.mjs start
```

---

## Фреймворк KAIF (Idea 30) — рабочая методология проекта

Проект обвязан **KAIF 2.1 — Strong KAIF** (Krinik AI Framework, https://github.com/MikalaiKryvusha/KAIF) —
методологией, которая РОДИЛАСЬ в этом проекте и вернулась сюда формально оформленной. Сводка
развёртывания — `KAIF_FRAMEWORK.md`; маркер — `.kaif/kaif.json`; полный справочник механики —
`.kaif/KAIF_REFERENCE.md` (§1–16: терминология, схемы маркера/манифеста/квитанции); ручки —
`node .kaif/kaif-core.mjs <version|check|update|update-verify|diff|modules|adopt-current|sync|checkpoint>`
(или корневой `npm run kaif:*`); сферы — `.kaif/spheres/` (дисциплина исполнения сферы `programming`:
минимальный набор свидетельств, порядок авторитетов, верификация наблюдением, таблица фродов, рецепты
ремесла); опциональные модули-инструменты — `.kaif/tools/` (гейт провенанса `[AI]…[/AI]` и линтер канона).

**Ключевые документы (корень):** `AGENT_GUIDE.md` (канон), `PHILOSOPHY.md`, `BUG_FIXING_FRAMEWORK.md`,
`TESTING_FRAMEWORK.md` (7 принципов тестирования + маркеры `[NOT-TESTED]`/`[TESTED]`, новое в 1.5),
`GOAL.md` (видение Криника — его документ), `STATUS.md` (живая СВОДКА настоящего, ~200 строк),
`PROJECT_HISTORY.md` (append-only летопись закрытого — куда STATUS сбрасывает своё прошлое; НЕ входит
в обязательный минимум чтения, открывается только под археологию), `MASTER_PLAN.md` (генплан),
`EXPERIENCE.md` (журнал уроков агента, навык `/experience`),
`PROJECT_STRUCTURE_EXTERNAL_MAP.md` + `PROJECT_ARCHITECTURE_INTERNAL_MAP.md` (карты), `KAIF_FRAMEWORK.md`.

**Директории знаний (в каждой README с правилами):** `plans/` (детальные планы), `ideas/` (идеи —
реализуем после одобрения Криника), `bugs/` (по доку на дефект), `researches/` (база знаний),
`interviews/` (решения владельца — Криник отвечает в документе), `homeworks/` (ДЗ Кринику;
девайс-логи — в `homeworks/logs/`, gitignored).

**Навыки (34):** сессия `/resume` `/pause` (мягкая парковка — чат продолжится ЗДЕСЬ) `/end-chat`
(полное закрытие с передачей эстафеты) · автономия `/autoloop` `/dayloop` `/nightloop` `/guarded-loop`
(цикл под вахтёром: будильники, heartbeat, кап перезапусков) · гигиена `/refresh-context` `/check-backlog`
`/code-revision` (периодическая читающая ревизия кода сильнейшей моделью) · знания `/report-bug`
`/bug-research` `/propose-idea` `/experience` · владелец `/interview` `/owner-voice` (портрет голоса
Криника) `/owner-reviews` (контур согласований страницами) `/derive-styleguide` (стайлгайд из образца
Криника) · планирование `/plan-task` `/plan-epic` `/revision` `/what-next` · видение `/fix-vision` ·
помощь `/help-kaif` · релиз `/release` · дисциплина исполнения (fable, 1.5) `/fable-method` `/fable-loop`
`/fable-judge` `/fable-domain` · жизненный цикл KAIF `/kaif-version` `/kaif-update` `/kaif-fork`
`/kaif-switch-origin` `/kaif-remove`.

> ⚠️ **Разделение `/pause` и `/end-chat` (KAIF 2.0).** `/pause` теперь ДЁШЕВЫЙ: довести до логической
> точки, убедиться, что дерево зелёное, оставить точную парковочную заметку — без пушей и церемоний,
> потому что чат продолжится здесь же. Тяжёлое закрытие сессии KrinikCam (STATUS → PROJECT_HISTORY,
> README, README.pdf, сборка APK, коммит и **push**) переехало в `/end-chat`. Прежние фразы Криника
> («заверши сессию», «зафиксируй статус», «закоммить и запушь») ведут в `/end-chat`; «пауза»,
> «припаркуйся», «прервёмся ненадолго» — в `/pause`.

---

## Карты проекта

Внешняя карта (где что лежит) — `PROJECT_STRUCTURE_EXTERNAL_MAP.md`.
Внутренняя карта (абстракции и потоки данных) — `PROJECT_ARCHITECTURE_INTERNAL_MAP.md`.
Детальные планы — в `plans/` (Phase 1 — `plans/phase_1_mvp.md`, graphics tool — `plans/graphics_tool.md`).

---

## Бэклог: bugs/ и ideas/ — тег DONE в имени файла (Idea 15)

Чтобы по списку файлов сразу видеть, что закрыто, а что в работе — **в имя выполненного файла после
номера вставляется слово `DONE`**:

```
bugs/04_platforms_modal.md            →  bugs/04_DONE_platforms_modal.md
ideas/07_developer_menu.md      →  ideas/07_DONE_developer_menu.md
```

**Правило (делать ВСЯКИЙ РАЗ при работе с файлами багов и идей):**
- Закончил баг/идею и она ПОДТВЕРЖДЕНА закрытой (статус ✅/ЗАКРЫТ/РЕАЛИЗОВАНО/ИСПРАВЛЕНО, проверено) —
  сразу переименуй файл, добавив `DONE` после номера: `git mv <NN>_<name>.md <NN>_DONE_<name>.md`.
- Файл в работе / частично / только исследование — НЕ помечай `DONE` (статус 🔧/🟡/🔬 = ещё не готово).
- Используй `git mv` (сохраняет историю). Не меняй номер.
- Периодически пробегайся по `bugs/` и `ideas/` и помечай накопившиеся закрытые файлы.
- **Закрытие любой идеи/бага/плана требует секции «Решения, принятые без владельца»** (KAIF 1.6) —
  каждое микрорешение, которое агент принял сольно по ходу исполнения, и как выбирал (или явное
  «нет»). Агент молча принимает десятки таких решений; секция кладёт их на стол Криника, где
  расхождение с видением стоит одну строку, а не переделку, — и это лучший генератор его следующих
  вопросов. Незакрытые допущения (fable-строки `PENDING:`) тоже разрешаются здесь: каждое
  *подтверждено / опровергнуто / спрошено*, никогда не выброшено молча.

**Заметка Криника на лету → в беклог, а НЕ переключение задачи (KAIF 2.1).** Когда Криник бросает в
чат идею/улучшение/баг, пока ты работаешь над ДРУГИМ: сразу оформи это документом (`/propose-idea` →
`ideas/`, `/report-bug` → `bugs/`; в шапке пометь источник — «брошено Криником по ходу, <дата>»),
подтверди одной строкой в чат («записал в ideas/NN — продолжаю текущую задачу») и вернись к прерванной
работе. Не бросай текущую задачу ради заметки и не держи её в голове до конца сессии — голова сессии
худшее из хранилищ. Сначала классифицируй: заметка КАСАЕТСЯ текущей задачи → это уточнение, применяй;
уровень видения → `/fix-vision`; явное «переключись на это» → переключайся.

**Волна багов от Криника = один инцидент процесса (KAIF 1.6).** Когда ручной прогон Криника приносит
ПАЧКУ багов разом, сама волна — симптом протёкшего процесса, и стоит больше любого бага в ней. Чини
баги; а по явной просьбе Криника («разберись, почему так много») заведи **документ процесса** в
`plans/` — `вердикт Криника (дословно) → честный диагноз процесса → лекарства как изменения процесса
→ шаги с чекбоксами` — и исполняй его параллельно фиксам. Метрика здоровья: следующая волна Криника
МЕНЬШЕ. Волны не сжимаются — лекарства не работают, пересматривай. Цель не «ноль багов», а «Криник
перестаёт находить их пачками».

**Навык ревизии беклога — `/check-backlog`:** чтобы не делать это руками, есть навык `/check-backlog`.
Он пробегается по `bugs/` и `plans/` (включая `ideas/`), собирает всё без тега `DONE` как
открытый беклог к реализации, а реально закрытым файлам проставляет `DONE` в имени (`git mv`) и
дописывает внутрь секцию статуса «что было сделано». Референсные доки в `plans/` (master_plan,
project_map и т.п.) он не трогает. Вызывай его периодически (а в автолупах он подключается из
`/refresh-context` и из самих циклов).

**Навык репорта бага — `/report-bug`:** наткнулся в ходе разработки/теста на дефект — заведи на него
отдельный md в `bugs/` по канону (Статус/Версия/Симптом/Репро/Форензика/Root cause/План),
руководствуясь `BUG_FIXING_FRAMEWORK.md`. Агент САМ ведёт свой беклог багов — на каждый замеченный
баг документ, ничего не теряем. (Idea 29.)

**Навык предложения идеи — `/propose-idea`:** родилась стоящая идея/фича, согласующаяся с
`master_plan`/`goal`/`PHILOSOPHY` и видением Криника — оформи её md в `ideas/` по канону со
статусом «❓ НА СОГЛАСОВАНИИ КРИНИКА» и пометкой в `STATUS.md`. **Идея от агента = элемент видения
продукта → реализуем ТОЛЬКО после одобрения Криника.** Не путать с `/interview` (вопрос по уже идущей
работе): `/propose-idea` — это предложение нового направления. (Idea 29.)

---

## Стиль кода

- Активно комментируем все блоки и модули кода.
- Compose: никаких `preview` в продакшн-файлах, разбивай на мелкие `@Composable`
- Hilt везде — никакого ручного DI
- Flow/StateFlow — никогда LiveData
- Никаких magic numbers — константы с понятными именами
- **Канонический порядок для всего, что сравнивается или кэшируется (KAIF 1.6):** любой вывод,
  который диффается, дедуплицируется или кэшируется, должен быть детерминирован — сортировки с полным
  tie-break, сериализация с отсортированными ключами, без `Date.now()`/random в сравниваемом выводе.
  Недетерминизм не виден в тестах и молча обнуляет диффы и кэши на живых данных — эта строка чеклиста
  замечает его, чтобы не пришлось тебе.
- Цвет бренда: `#FF1A8C` (acid pink)
- **Секреты в логах (bug 37):** RTMP-URL со stream-ключом в ЛЮБОЙ лог — только через
  `redactRtmpUrl()` (`feature/streaming/rtmp/RtmpRedact.kt`). Лог-файл пуллится к баг-репортам;
  открытый ключ = угон эфира. Новая лог-точка с URL — сразу оборачивай.
- **Локализация (грунт, Idea 14):** USER-FACING UI-текст выносим в `app/src/main/res/values/strings.xml`
  и используем `stringResource(R.string.x)` (импорт `androidx.compose.ui.res.stringResource` +
  `com.kriniks.kcam.R`). НЕ выносим: лог-сообщения, `KLog`-теги, технические константы. `stringResource`
  зовётся только в `@Composable`-скоупе — для строк внутри `onClick`/лямбд резолвь заранее
  (`val x = stringResource(...)`). Приложение пока English-only; будущие локали = `values-<lang>/`.
  Конвертированы: SettingsScreen, DevMenuScreen, RotationMenu. Остаток (FAB-меню, MainScreen,
  feature-модули, длинные dev-инфо-абзацы) — по тому же паттерну при касании этих файлов.


## Руководство от Криника:
- Каждый раз, когда читаешь логи, проверь текущее время и проверь время лог-файла - чтобы не читать старые логи, а читать только свежие актуальные логи.
- Напоминаю, что моё Android устройство и ты в одной локальной сети, веб камера подключена - ты можешь собирать билды, накатывать их на устройство, запускать приложение, снимать с устройства любые логи, управлять устройством, снимать скриншоты - работать с ним любым образом, который позволит тебе выполнить задачу по фиксингу бага и реализации фичей.
- **`BUG_FIXING_FRAMEWORK.md`** — если нужно починить дефект, провести дебаг, отловить баг и пофиксить его, то руководствуйся этим фреймворком работы.
- В процессе работы хоть немного но пиши естественным языком в чат о том, что ты сейчас делаешь, чтобы я хоть иногда видел и понимал, в каком процессе мы находимся.
- По всем, даже мелким багам в процессе работы над ними, рефлексируй и собирай знания в отдельных mdдокументах в директории bugs.
- Старайся работать автономно без интерактивных вопросов. Если есть нужда получить от меня информацию, то создай для этого документ md с очередным интервью (пример уже проведённых интервью найди в директории interviews), а затем напиши для меня сообщение в чат, что тебе нужны от меня данные, что ты составил интервью и поставь задачу на паузу, остановить - когда ты ставишься на паузу, VS Code воспроизводит звуковой сигнал - я его слышу, где бы я ни был в доме и чем бы не занимался - я подойду к компьютеру, прочитаю твоё сообщение и отвечу на твоё интервью.
- Если находишь баги в бибилиотеках других разработкивок, то оформляй им в Github через консольное приложение gh от моего имени баг-репорт тикеты.
- Активно тестируй разрабатываемое приложение. Для этого активно пользуйся ADB для связи с Андроид устройством, и устанавливай и пользуйся любыми консольными приложениями коммандной строки, которые позолят тебе управлять Андроид устройством для эффективного тестирования.
- Регулярно перечитывай файлы своих руководств AGENT_GUIDE.md, STATUS.md - при необходимости, редактируй их, дописывай важные на твой взгляд руководства, чтобы ИИ агент мог эффективно работать в разных ссесиях, между которыми теряется контекст. Старайся сам руководить собой и настраивать себя для максимальной эффективности и автономности. Глобально цель поставлена - разработать приложения. ВИдение Криника описано - можно автономно стремиться к этой цели с минимальным привлечением Криника.
